PyQt6>=6.6.0
pyttsx3>=2.90
openpyxl>=3.1.0
SpeechRecognition>=3.10.0
pocketsphinx>=5.0.0  # local (offline) speech recognition — the ONLY recogniser since Sprint 11 (voice/query_listener.py::_recognise); PTT voice queries fail without it
sounddevice>=0.4.0
pynput>=1.7.6
requests>=2.28.0
pycryptodome>=3.19.0  # Crypto.Cipher.Salsa20 — GT7 packet decryption (telemetry/packet.py)
# PyAudio>=0.2.14 — requires PortAudio headers; use sounddevice instead
# pygame>=2.5.0  — optional, for joystick button detection; install separately if needed
