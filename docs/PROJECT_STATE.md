# GT7 VR Dashboard — Project State

> Updated: 2026-07-20 (**Engineering Brain PROGRAM 2, Phases 45-47 — Immutable Provenance, Live Validation & Opt-In Voice (combined slice).** Branch `eng-brain-phase45-47-provenance-live-voice` from the Phase-42-44 tip `ce01383`; committed locally, NOT pushed / no PR / not merged; master unchanged at `3d7c6af`; **DB v26 -> v27** (justified additive migration - the ONLY version bump; `RULE_ENGINE_VERSION` 46.0 unchanged; `_setup_constants.py` otherwise unchanged). Progresses: preserve exact historical context -> prove live advisory via replay + shadow -> opt-in OFFLINE voice for already-approved prompts only. No auto pit/strategy/apply/experiment/outcome. **Correction A:** the stale `test_phase6_wiring.py::test_no_migration_no_new_telemetry_table` (forbade `_DDL_V26`/`_migrate_v26`, i.e. the whole legit chain) rewritten to prove the REAL Phase-6 invariant vs a fresh DB (user_version==DB_VERSION; no residual/telemetry table; no telemetry table beyond the canonical base). **Correction B:** mechanism-level `MECHANISM_REQUIRED` + `build_field_working_window_trust` cap gearing/aero/suspension/LSD legacy evidence to PARTIAL when BoP/restrictions/applied-setup identity are unknown; driver_technique stays exact. **Phase 45** `strategy/engineering_context_snapshot.py` (immutable 30-field semantic snapshot; canonical digest + short fp EXCLUDING event_name/audit-time/row-id/paths/machine; dedup by content) + `strategy/historical_context_resolution.py` (per-field directly_persisted/resolved_through_reference/inferred_with_limitations/unknown; legacy=all-unknown, never back-filled). Additive **v27** tables `engineering_context_snapshots` (content-addressed) + `engineering_context_snapshot_refs`; `_migrate_v27` (fresh->v27, legacy v26->27 additive, idempotent, data-preserving, rollback-safe). SessionDB `capture_context_snapshot` (ONLY writer, explicit workflows only), `get_context_snapshot`/`get_snapshot_for_ref` (no N+1), `resolve_historical_context`. **Event-edit reconstruction proven:** event tyre x5->x8; old evidence still resolves x5, new x8, not merged. **Phase 46** `strategy/telemetry_replay.py` (deterministic replay + injected clock; LAP/PIT/STALE_GAP/SEGMENT events; speed-independent semantics) + `strategy/prompt_timing.py` (word-count duration budget; coaching cue <=2.5s, stop-critical immediate, detail deferred) + `strategy/shadow_advisory.py` (runs the Phase-44 engine in shadow - same decisions, no speech, no write; ledger; readiness NOT_READY->REPLAY_VALIDATED->SHADOW_READY->LIVE_SHADOW_VALIDATED->VOICE_ELIGIBLE_WITH_LIMITATIONS->VOICE_ELIGIBLE; VOICE_ELIGIBLE needs live-GT7 confirm). SessionDB `build_live_shadow_validation_report` (read-only). **Phase 47** `strategy/voice_delivery.py` (PURE, TTS-free: VoiceDeliveryRequest speaks EXACT approved msg; VoiceOutputPort + DisabledVoicePort default + FakeVoicePort; VoiceQueue priority/stop-critical-interrupt/routine-non-interrupt/dedup/cooldown/cancel-on-stale/flush) + `voice/advisory_voice_port.py` (Windows OFFLINE SAPI5 via win32com LAZILY; disabled+silent until enable(); failure => permanently disabled) + `voice/voice_controller.py` (opt-in driver; speaks only when enabled AND voice_gate_allows(readiness) AND gates ok; ack/repeat-once/mute; failure => visual-only fallback, no crash; test-voice blocked during a run). **Voice disabled by DEFAULT; no cloud/API/LLM/network; no pit/tyre/fuel/setup commands.** UI: AssistedRuntimePanel extended with a Context Snapshot card + a Live Validation & Voice card + opt-in voice controls (Enable/Disable/Acknowledge/Mute/Test) delegating to dashboard handlers; dashboard lazily creates the disabled VoiceController and merges its status (operational only - never in engineering fingerprints). **76 new tests** (all green). **Also updated the 47 DB_VERSION==26 / user_version==26 guard assertions to 27** (intentional bump). **Manual UAT:** Stage A (provenance) + Stage B (replay shadow) EXECUTED programmatically (old snapshot survives event edit; contexts not merged; viewing creates no snapshot; shadow SHADOW_READY, 0 high-workload/stale deliveries, no audio); **Stage C (live GT7) + Stage D (voice) NOT RUN** (headless, no telemetry/audio). Docs: PHASE45/46/47 + pre-phase corrections + UAT. **No setup ever ultimate; voice never an engineering authority; Apply gate + canonical outcome workflow untouched. Program 2 now spans Phases 12-47. Phase 48 NOT started.**)
>
> Updated: 2026-07-20 (**Engineering Brain PROGRAM 2, Phases 42-44 — Trustworthy Assisted Runtime Activation (combined slice).** Branch `eng-brain-phase42-44-assisted-runtime-activation` from the Phase-39-41 tip `a57d25a`; committed locally, NOT pushed / no PR / not merged; master unchanged at `3d7c6af`; **DB stays v26** (no migration, no persistence, no DB write), `RULE_ENGINE_VERSION` 46.0, `_setup_constants.py` byte-identical. Begins runtime activation of the race-engineer workflow, deliberately WITHOUT voice, autonomous setup application, autonomous experiment execution or automatic pit commands: material context capture -> user-confirmed practice execution -> explicit outcome capture -> safely-gated live advisory text. **Phase 39-41 file-count correction:** git-verified 28 added / 28 modified / 0 deleted = 56 total (the report's 56 is correct; any '57' is an overcount). **Phase 42** `strategy/material_context.py` (+render): per-field trust KNOWN_MATCH/KNOWN_DIFFERENT/UNKNOWN_CURRENT/UNKNOWN_HISTORICAL/UNKNOWN_BOTH/NOT_APPLICABLE/INFERRED_WITH_LIMITATIONS and overall trust EXACT_VERIFIED/EQUIVALENT_VERIFIED/PARTIAL_CONTEXT/TRANSFER_ONLY/REFERENCE_ONLY/INCOMPATIBLE/UNVERIFIABLE over a visible DOMAIN_REQUIRED map; doctrine 'unknown never proves a difference AND never proves exact equivalence' (a missing REQUIRED field caps to PARTIAL - unknown tyre_multiplier caps tyre_degradation, fuel_multiplier caps fuel_use, BoP caps gearing_aero - while identity-exact records stay exact for setup_working_windows/driver_technique). `strategy/legacy_evidence_trust.py`: legacy records stay VISIBLE (discarded=0), never upgraded by assumption. **Context-source audit: current material context is fully canonical (events table tyre_wear/fuel_mult/refuel/bop/tuning/weather + cars + applied setup); historical dev-records carry only the memory key, so material fields are genuinely unknown -> NO migration justified; DB stays v26** (persisting the snapshot on new sessions is a documented deferred candidate). Read-only `SessionDB.build_material_context_trust_report` (+ `_current_material_context` by reference). **Phase 43** `strategy/assisted_run_workflow.py`: 12-state machine (PLAN_READY..RECORDED/INVALID/ABANDONED); verify_setup compares expected vs CANONICAL applied fingerprint (trusted over a click) -> MATCH/MISMATCH/UNEXPECTED_CHANGE/UNVERIFIABLE; READY_TO_RUN blocked on wrong setup / material context mismatch / unexpected protected-field change / preflight blockers / missing session identity / stale candidate / stale run-plan fingerprint. `strategy/session_binding.py` (never auto-binds; recency only a final tie-break; ambiguous flagged) + `strategy/assisted_outcome_capture.py` (reuses canonical Phase-41; explicit-confirm gate; writes ONLY via the existing workflow; no alternative outcome table). **Phase 44** `strategy/live_advisory.py` (+render): text-only AdvisoryPrompt, explicit 1-8 priority, build_candidate_prompts (emits nothing when nothing applies). `strategy/live_advisory_engine.py`: hard staleness gates + window gates (high-workload braking/turn-in/apex delays non-stop-critical; stop-critical bypasses) + priority supersession + deterministic suppression (semantic keys, cooldown via INJECTED monotonic clock, per-lap/per-session limits, expiry). `strategy/runtime_snapshot.py` maps existing inputs (no 2nd telemetry authority). **Voice deferred; no pit/fuel/tyre/strategy commands.** Read-only `SessionDB.build_assisted_runtime_report` (context once, shared chain single read, portfolio once, constant query count 5/50/500, no writes; now_monotonic injected, never in the fingerprint). **UI** `ui/assisted_runtime_vm.py` + `ui/assisted_runtime_panel.py` (`AssistedRuntimePanel` ONE coordinated pit-wall Run State/Live Advisory/Evidence Progress; NO Apply/mutation/experiment/outcome/session/schedule/voice/AI controls; text status tags) in Development History; OFF the Qt thread via reused MechanismAnnotationWorker + stale-guard; dashboard injects time.monotonic() and carries suppression state forward. **Qt harness:** the corrected `tests/_qt_worker_wait.py::drive_worker` remains in use; new UI tests use it. **64 new tests** (all green). **Manual UAT:** guide `docs/UAT_ENGINEERING_BRAIN_PHASE42_44.md`; offscreen end-to-end verification executed (legacy not silently exact; wrong setup blocks READY_TO_RUN; high-workload delivers only stop/none; page builds 3-card panel; DB byte-identical after viewing; user_version 26) - full live-GUI UAT with real telemetry NOT run in this headless environment. Docs: PHASE42/43/44 + pre-phase corrections + UAT. **No setup is ever ultimate/optimal; the Apply gate + explicit outcome workflow are untouched; voice deferred. Program 2 now spans Phases 12-44. Phase 45 NOT started.**)
>
> Updated: 2026-07-20 (**Engineering Brain PROGRAM 2, Phases 39-41 — Closed-Loop Engineering Development (combined slice).** Branch `eng-brain-phase39-41-closed-loop-development` from the Phase-36-38 tip `2cbe077` (the 8 Phase 36-38 commits + the requested UI/UX pass); committed locally, NOT pushed / no PR / not merged; master unchanged at `3d7c6af`; **DB stays v26** (no migration, no persistence, no DB write), `RULE_ENGINE_VERSION` 46.0, `_setup_constants.py` byte-identical. A deterministic, offline, read-only, advisory-only slice proving the Engineering Brain can run a complete, trustworthy loop: context-safe evidence -> candidate selection -> controlled practice-run plan -> observed outcome -> reconciliation -> knowledge-update proposal -> promotion/rejection/rollback. **Pre-phase audit (docs/ENGINEERING_BRAIN_PHASE39_41_PREPHASE_AUDIT.md):** A (context-before-aggregation) REMEDIATION REQUIRED - Phase-22 compat group excludes track/layout/compound, so aggregates were cross-track; remediated by the context-scoped chain. B (multi-field regression) REMEDIATED via RegressionAttribution (bundle vs field). C (driver attribution independence) REMEDIATED via SetupIndependenceAssessment. D (Qt harness) TEST-HARNESS DEFECT RESOLVED (no product change) via `tests/_qt_worker_wait.py::drive_worker` - deterministic QThread.wait + processEvents replacing nested shared `app.exec()`; migrated all 23 UI off-thread tests; previously-failing combined run 10 failed/707 passed -> 717 passed/0 failed. **Phase 39** `strategy/context_scoped_chain.py` (classify-before-aggregate: exact/transferable/reference/excluded/unverifiable buckets; exact-domain summary + convergence from EXACT records only; a confounded run never strengthens convergence; `exact_content_fingerprint` invariant to any incompatible/transferable evidence - proven by adding 100 Daytona records) + `strategy/context_equivalence.py` (identity-critical=>INCOMPATIBLE, track-critical=>TRANSFER_ONLY, event-condition=>MATERIALLY_DIFFERENT, event_id-only=>EQUIVALENT_CONDITIONS) + `strategy/regression_attribution.py` (BUNDLE_REGRESSION_CONFIRMED/FIELD_DIRECTION_SUSPECT/FIELD_DIRECTION_CONFIRMED/INTERACTION_SUSPECTED/ATTRIBUTION_INSUFFICIENT; field confirmed only via single-field test / independent repeats / reversal) + `strategy/setup_independence.py` (mechanism-relevant independence; SETUP_LIKELY/DRIVER_TECHNIQUE_LIKELY/TRACK_OR_CAR_CHARACTERISTIC/COMBINED/INTERACTION_UNRESOLVED/INSUFFICIENT) + `strategy/production_history_validation.py` (read-only health check; performed_repair=False). **Phase 40** `strategy/engineering_run_plan.py` (+render): EngineeringRunPlan with objective (Base/Qualifying/Race distinct), ControlledChangeSet (SINGLE_MECHANISM vs COUPLED_BUNDLE - min effective intervention), HeldConstantSet, EvidenceCollectionPlan, RunValidityGate, stop conditions, safety/rollback, ExperimentCandidateLink (existing, preflight required, never created); `strategy/run_candidate_selection.py` (highest-value context-safe Phase-17 candidate; rejects protected-good risk; deadline-pressure PROTECT posture). **Phase 41** `strategy/engineering_run_outcome.py` (RunValidity VALID/VALID_WITH_LIMITATIONS/INVALID/INSUFFICIENT/CONFOUNDED/CONTEXT_MISMATCH; outcome IMPROVED/REGRESSED/UNCHANGED/MIXED/INCONCLUSIVE/NOT_TESTED - a faster lap alone never promotes a Race setup; promotion BEST_KNOWN_ELIGIBLE/PROVISIONAL/REQUIRES_CONFIRMATION/NOT_ELIGIBLE/ROLLBACK_RECOMMENDED/SUPERSEDED/CONTEXT_LIMITED - never ultimate, never auto-applied) + `strategy/closed_loop_report.py` (+render): read-only KnowledgeUpdateProposal (nothing written; invalid run=>no window change; coaching-only=>no setup knowledge; multi-field=>SUSPECT) + exactly one primary NextEngineeringAction. **Read-only SessionDB** `build_context_scoped_evidence_report`, `build_production_history_validation_report`, `build_engineering_run_plan_report`, `build_closed_loop_workflow_report` (resolve context once, reuse shared chain's single bounded read, portfolio composed once, constant query count, no N+1, no writes). **UI** `ui/closed_loop_workflow_vm.py` + `ui/closed_loop_workflow_panel.py` (`ClosedLoopWorkflowPanel` three-step Evidence Readiness/Practice Run Plan/Outcome Review; NO Apply/mutation/experiment/outcome/schedule/AI/editable controls; text status tags + tone) in Development History; OFF the Qt thread via reused MechanismAnnotationWorker + stale-guard; viewing passes observation=None so nothing is written. **73 new tests** across 9 files (all green); **Qt harness now green in combined runs**. **Manual UAT guide written (docs/ENGINEERING_BRAIN_PHASE39_41_MANUAL_UAT.md) but NOT executed in a live GUI this session.** Docs: PHASE39/40/41 + pre-phase-audit + manual-UAT. **No setup is ever universally optimal/ultimate; the Apply gate and explicit outcome workflow are untouched. Program 2 now spans Phases 12-41. Phase 42 NOT started.**)
>
> Updated: 2026-07-20 (**Engineering Brain PROGRAM 2, Phases 36-38 — Context-Safe Race-Engineer Activation (combined slice).** Branch `eng-brain-phase36-38-race-engineer-activation` from the Phase-33-35 tip `9f64ce7`; committed locally, NOT pushed / no PR / not merged; master unchanged at `3d7c6af`; **DB stays v26** (no migration, no new persistence, no DB write), `RULE_ENGINE_VERSION` 46.0, `strategy/_setup_constants.py` byte-identical. A deterministic, offline, read-only, advisory-only ACTIVATION slice that turns the Engineering Brain into ONE coordinated race-engineer team plan for the current event, in eight explicit layers. **Phase 36 (context):** `strategy/engineering_context_scope.py` — ONE immutable `EngineeringContextScope` (driver, car+variant, track, layout, event, discipline Base/Qualifying/Race, compound/policy, BoP, tuning-permitted tri-state, power/weight, gt7/rule/data-schema versions, race objective, tyre/fuel multipliers); missing context explicit (a KNOWN vs UNKNOWN component never collide via an internal sentinel); `context_fingerprint` over SEMANTIC identity only (a different driver/car/track/layout/discipline can never collide); `relate_context` (EXACT requires the FULL identity). `strategy/contextual_knowledge_activation.py` (+render) classifies every development record vs the current scope into EXACT_CONTEXT / EXPLICITLY_TRANSFERABLE / REFERENCE_ONLY / EXCLUDED / UNVERIFIABLE with a reason each; transferable enters ONLY via the canonical Phase-23 `evaluate_transfer` (mapped from the record's engineering domains) — same car/driver alone is never enough; other-track handling transfers, other-track gearbox/track knowledge is EXCLUDED (Daytona can't shape a Fuji window); contamination guard lists every excluded/reference item. **Phase 37 (setup + driver):** `strategy/setup_outcome_learning.py` interprets the EXACT-context lineage (delta -> outcome -> repeat/hold/reverse/BLOCK; a worsened direction is blocked and unblocks only on later equal/stronger improvement — the no-repeat guard; folds Phase-3 NEVER_MOVE_DIRECTION/KNOWN_UNSTABLE; rollback anchor = last non-worsening state; protects confirmed-good behaviour). `strategy/setup_working_window.py` derives per-field windows (union of proven values, NEVER an average; regression values marked to avoid; converged windows protected; co-change interactions; independence count; exact-context + single-discipline so Quali/Race stay separate; a mature window survives one noisy record). `strategy/driver_development_state.py` maps repeated per-corner residuals to driving dimensions (strength/development-area/emerging/insufficient by aggregate score + trend — latest session never assumed better) and attributes cause (persists across setups=>technique/track; appeared under one setup=>setup; improving under a constant setup=>technique). `strategy/coaching_priority.py` selects a few falsifiable coaching priorities (driver-attributable only) each with corner/phase, current vs desired behaviour, why, ONE technique focus, a measurable success criterion, confirming telemetry, a falsifier, hold-setup-constant, and a gearing/drive-out assessment for exit/traction/gear. **Phase 38 (brief):** `strategy/race_engineer_team_brief.py` (+render) assembles ONE coordinated crew brief with role-specific but non-duplicated Chief/Setup/Performance/Coach/Strategy sections (views over shared evidence), resolves contradictions by SEQUENCING with explicit hold-constant (coaching-vs-experiment, explore-vs-rollback), emits ONE ordered coherent development plan, references (never creates) an existing canonical experiment, never labels an incremental step an ultimate setup, and states missing evidence as a collection plan; carries subordinate fingerprints. **Read-only SessionDB** `build_race_engineer_team_brief(...)` resolves the current context ONCE, reuses the shared `_build_knowledge_chain` ONCE (single bounded evidence read), computes Phases 36-38 purely in memory, never calls the lower public builders, adds no DB reads (constant query count small-vs-large, no N+1), writes nothing, creates no experiment; honest empty programme. **UI** `ui/race_engineer_team_vm.py` + `ui/race_engineer_team_panel.py` (`RaceEngineerTeamPanel`; NO Apply/mutation/experiment/schedule/editable-grade/AI/auto-export controls) in Development History beneath the Phase-33-35 panel; heavy build OFF the Qt thread via the reused MechanismAnnotationWorker with a stale-worker guard. **Tests:** `tests/test_phase36_context.py` (11) + `tests/test_phase37_setup_learning.py` (12) + `tests/test_phase37_driver_coaching.py` (9) + `tests/test_phase38_team_brief.py` (9) + `tests/test_phase36_38_{golden[5],query_shape[4],safety[5],runtime[5],ui[8]}.py` = **68 new** (all green; Phase 32/33-35 regression green). **Runtime verified:** DB byte-identical before/after brief; user_version 26; restart/now_date-independent fingerprint; context change alters the fingerprint; constant query count small-vs-large; no lower public builders called; brief runs OFF the UI thread; stale worker ignored; no setup values. Also **corrected the Phase 33-35 fingerprint-wording** (semantic identity IS material; runtime/object/machine identity excluded; accidental source-row order excluded; canonical semantic priority order MAY be material). Docs: `docs/ENGINEERING_BRAIN_PHASE36_CONTEXT_ACTIVATION.md`, `..._PHASE37_SETUP_DRIVER_LEARNING.md`, `..._PHASE38_TEAM_BRIEF.md`. **The brief is NOT a certification and never claims a final/ultimate setup. Program 2 now spans Phases 12-38. Phase 39 NOT started.**)
>
> Updated: 2026-07-20 (**Engineering Brain PROGRAM 2, Phases 33-35 — Assurance Review Pack (combined slice) + Phase 32 audit remediation.** Branch `eng-brain-phase33-35-assurance-review-pack` from the Phase-32 tip `0e88b8e`; committed locally, NOT pushed / no PR / not merged; master unchanged at `3d7c6af`; **DB stays v26** (no migration, no new persistence, no DB write), `RULE_ENGINE_VERSION` 46.0, `strategy/_setup_constants.py` byte-identical. A deterministic, offline, read-only, advisory-only external-review slice on ONE branch: on-demand assurance-chain EXPORT (33), baseline-vs-candidate COMPARISON (34) and an external REVIEW PACKAGE (35), all sharing the Phase 22-32 chain, one canonical serializer, provenance, a fingerprint hierarchy and deterministic rendering. **Phase 32 audit remediation (commit 1):** the Phase-32 `content_fingerprint` now covers EVERY material candidate field (full ordered candidate dicts via the new `content_fingerprint_for_candidates`; 13 mutation tests prove each of evidence_requested/why_needed/current_evidence_state/discriminating_requirement/expected_assurance_impact/impact_limitations/defer_conditions/rationale/finding_types/max_severity/every dimension field/every dependency field/band/score/linked-finding-ids + membership + order changes it); the (domain, investigation_type) grouping is verified INTENTIONAL (no cross-domain over-merge) and documented; the exact git file counts (Phase 32 = 11 added, 6 modified) were verified and corrected in the Phase-32 doc. **Canonical serialization** `strategy/assurance_chain_serialization.py` — the ONE serializer (sorted-key ASCII canonical JSON, allow_nan=False, 6-dp float normalisation + non-finite rejection, enum-value normalisation, content digests + timestamp-free short fingerprints, safe-relative-name check, fixed CHAIN_PHASE_ORDER 26->32; no identity/addresses/timestamps/paths; byte-identical across restart). **Phase 33** `strategy/assurance_chain_export.py` (+render): AssuranceChainExport/Manifest/Section/ProvenanceEntry/IntegrityEntry/ExportValidationResult; `build_assurance_chain_export(chain_products, context)`; each section carries canonical content + subordinate fingerprint + a RECOMPUTED content digest; the assurance-chain fingerprint is over the recomputed digests + identity + db/rule versions + fixed order (changes on ANY material subordinate change even if a label is reused); `recompute_chain_fingerprint`/`verify_export_integrity` detect tampering. **Phase 34** `strategy/assurance_snapshot_comparison.py` (+render): explicit baseline->candidate; AssuranceCompatibility(COMPATIBLE/PARTIALLY/INCOMPATIBLE/UNVERIFIABLE), AssuranceChangeType(added/removed/unchanged/modified/improved/regressed/reopened/resolved/incomparable), per-category deltas + domain rollup; newer!=better, no timestamp authority, domain-gone=incomparable not resolved, contradiction-closed-without-independence=unverified, assumption-dropped-without-evidence!=improvement, readiness-up-needs-independence, grade movement corroboration-gated, INCOMPATIBLE shows no trend; comparison fingerprint direction-material. **Baseline loader** `strategy/assurance_manifest_loader.py` — strict PURE validator (parse rejects Infinity/NaN/non-JSON; detects export vs review-package; recomputes digests + chain fingerprint; rejects tampered/forged/path-traversal/duplicate/unknown-enum; never executes/imports/pickles/trusts-a-claimed-fingerprint). **Phase 35** `strategy/assurance_review_package.py` (+render) pure SPEC (report .md + chain manifest .json + optional comparison artifacts, each digested; package manifest + package fingerprint over sorted (kind,digest) pairs; destination NOT in the fingerprint) + `data/assurance_review_package_writer.py` — the ONLY file writer (explicit destination, no implicit default, overwrite guard, temp staging + per-byte verify + atomic move + failure cleanup; touches NO db/source/runtime file; includes NO db/setup-history/settings/keys/track-model/absolute paths; optional BYTE-DETERMINISTIC zip with fixed 1980 dates/attrs/order). **Read-only SessionDB** (`_assurance_chain_products` builds the chain ONCE + computes 26-32 purely in memory; `build_assurance_chain_export_report`, `build_assurance_snapshot_comparison_report`, `build_assurance_review_package_report`): never call the lower Phase 23-32 public builders, add no DB reads beyond the shared chain, baseline validation performs ZERO DB reads (invalid baseline short-circuits before any read), query count constant small-vs-large, no N+1, Phase-29 negative-evidence gate unchanged. **UI** `ui/assurance_review_pack_vm.py` + `ui/assurance_review_pack_panel.py` (`AssuranceReviewPackPanel`; NO Apply/experiment/campaign/schedule/editable-grade/editable-priority/API-key controls; three explicit-action buttons Preview/Compare Baseline.../Export Review Package...; build+write OFF the Qt thread via reused MechanismAnnotationWorker with stale-worker guards; export destination shown OUTSIDE report content; nothing auto-exports) in Development History beneath the Phase-32 panel. **Tests:** `tests/test_phase32_fingerprint.py` (13) + `tests/test_phase33_export.py` (14) + `tests/test_phase34_comparison.py` (19) + `tests/test_phase35_package.py` (20) + `tests/test_phase33_35_{query_shape[7],safety[34],golden[8],runtime[6],ui[14]}.py` = **135 new** (all green; Phase 32 regression green; combined Phase 26-35 UI run 85 passed). 12 golden fixtures (empty/negative-only/fully-assured/blocking-contradiction/version-sensitive/severe-blind-spot/assumption-capped/high-leverage/compatible-improvement/compatible-regression/incompatible-context/corrupted-package) - restart-identical, no timestamps/paths/random ids. **Runtime verified:** DB byte-identical before/after export+compare; user_version 26; repeated/restart/shuffled-row export byte-identical; package manifest verifies independently + corrupted artifact fails; end-to-end dashboard export OFF the UI thread mutates no DB; export refuses without a destination; no setup values/secrets/absolute paths; negative-only visible; fully-assured truthful; incompatible=no false trend; explicit export is the only write path. Docs: `docs/ENGINEERING_BRAIN_PHASE33_ASSURANCE_EXPORT.md`, `..._PHASE34_ASSURANCE_COMPARISON.md`, `..._PHASE35_REVIEW_PACKAGE.md`. **An export/review package is NOT an independent certification. Program 2 now spans Phases 12-35. Phase 36 NOT started.**)
>
> Prior update: 2026-07-20 (**Engineering Brain PROGRAM 2, Phase 32 — Assurance-Driven Engineering Priority.** Branch `eng-brain-phase32-assurance-priority` from the Phase-31 tip `4b485be`. A deterministic, offline, READ-ONLY, ADVISORY-ONLY system that converts the Phase-31 assurance FINDINGS into a PRIORITISED list of EVIDENCE INVESTIGATIONS that would most efficiently improve programme assurance ("given the assurance verdict, what evidence should the programme collect next, and why?"). It produces a priority ORDER, never a schedule: it names no dates/sessions/drivers/cars/tracks/resources, creates no experiment/campaign/setup, exposes no Apply control, invokes no AI/optimiser/scheduler, and never claims a proposed investigation guarantees an assurance-grade increase (impact is always POTENTIAL). It consumes the existing authorities (Phase-17 information-gain DOCTRINE, Phase-22 shared chain, Phase 25–31 products) and mutates none. **NO schema migration / NO new persistence / NO DB write — `DB_VERSION` stays 26**, `RULE_ENGINE_VERSION` 46.0, `strategy/_setup_constants.py` byte-identical. NEW pure `strategy/assurance_engineering_priority.py`: `InvestigationPriorityBand` (6: BLOCKING/HIGH/MEDIUM/LOW/DEFER/NO_ACTION), `InvestigationType` (10: revalidation/contradiction_discrimination/independence_improvement/context_expansion/repeated_confirmation/assumption_establishment/missing_domain_coverage/version_sensitive_confirmation/convergence_confirmation/provenance_improvement), `InvestigationCandidate` (stable id, domains, linked Phase-31 finding ids [stable af_… derived], finding types, max severity, evidence_requested, why_needed, current_evidence_state, discriminating_requirement, expected_assurance_impact, impact_limitations, transparent `dimensions`, priority_score, priority_band, dependencies, defer_conditions, rationale, advisory_statement), plus `build_investigation_candidates` + pure `build_assurance_engineering_priority(...)`. **Transparent valuation** — every dimension exposes raw · weight · contribution · rationale (info-gain weighted highest 3.0, mirroring Phase-17 `DIMENSION_WEIGHTS`): value dims information_gain 3.0/blocker_clearance 2.5/cross_finding_leverage 2.0/contradiction_discrimination 2.0/independence_gain 1.75/assumption_reduction 1.5/freshness_value 1.5/context_relevance 1.25/evidence_availability 1.0; penalties duplication 1.5/dependency 1.5/collection_cost 1.0; priority_score = Σ signed contributions. **Doctrine (test-guarded):** severity is NOT the sole rule (a blocking finding is downgraded to DEFER if infeasible or has an unresolved prerequisite; a multi-finding cross-leverage candidate can outrank a single-blocker one within a band); independent evidence outranks dependent repetition; contradictions need DISCRIMINATING evidence (each states what must differ; generic "collect more laps" insufficient; never resolved by majority/recency/count); assumptions stay assumptions; missing evidence is untested not disproven; confirmed-good is protected (no candidates); duplicate findings MERGE by (domain, investigation_type) → cross-finding leverage; deterministic `_PREREQUISITE_PAIRS` (independence→contradiction, version→context, context→convergence) apply a dependency_penalty, downgrade the dependent to DEFER, and rank it after its prerequisite; NO scheduling. Deterministic tie-break: band → priority_score desc → blocker_clearance contribution desc → cross_finding_leverage desc → information_gain desc → collection_cost asc → canonical KnowledgeDomain order → InvestigationType order → stable candidate_id; no timestamp affects ranking or fingerprint. `AssuranceEngineeringPriorityReport` (schema, source identity, assurance grade, assurance summary, total/blocking/major finding counts, candidate count, prioritised_candidates, deferred_candidates, unresolved_prerequisites, ordering metadata, no_action_statement, source-chain summary, timestamp-free content_fingerprint) + `strategy/assurance_engineering_priority_render.py` (concise structured sections; strings only, zero DB; no setup values/dates/resources). NEW read-only entry `SessionDB.build_assurance_engineering_priority_report` (reuses the shared `_build_knowledge_chain` ONCE, computes Phase-26/27/28/29/30/31 PURELY in memory; never calls the lower public SessionDB builders; no N+1 [development-record reads constant 7-vs-7 small/large history, bounded full scans]; renderer zero DB; writes nothing; DB byte-identical; user_version 26; Phase-29 chain gate UNCHANGED so negative-only programmes remain analysable; empty → truthful None). UI: pure `ui/assurance_engineering_priority_vm.py` + `ui/assurance_engineering_priority_panel.py` (`AssuranceEngineeringPriorityPanel`, NO Apply/Run/Create/Schedule buttons, NO editable priority input, NO setup-value control — asserted; `[COLLECT]`/`[DEFER]` text tags not colour alone; Phase 12/…/31 coexistence asserted) in Development History beneath the Phase-31 assurance verdict; build OFF the Qt thread via the reused `MechanismAnnotationWorker(QThread)` with stale-worker protection (`_priority_worker`). **Golden `config_id` + frozen allowlist + Apply-gate UNCHANGED; `_setup_constants.py` git-verified byte-identical; no runtime/user files staged.** Tests: `tests/test_phase32_{domain[20],scoring[13],golden[9],integration[8],safety[14],ui_construction[11]}.py` (75) — golden = the 8 mandated scenarios + shuffle-stability; scoring proves every dimension visible + exact contribution maths + deterministic tie-break; integration proves Phase-22-once + the nine lower SessionDB builders never called + constant query count (no N+1) + renderer-no-DB + DB-hash/counts/user_version unchanged + negative-only builds + empty truthful; safety proves no experiment/campaign/schedule/Apply/optimiser/scheduler/AI, no Phase-17 portfolio import, never-guarantees-grade, no setup-value/date leak. Phase 30/31 regression green. Runtime verified: DB byte-identical, restart + shuffled-legal-row-order identical rendering AND fingerprint, query count constant, no setup-value/date leak, ASCII-clean, negative-only visible, empty truthful, fully-assured no-action. Doc: `docs/ENGINEERING_BRAIN_PHASE32_ASSURANCE_PRIORITY.md`. Committed locally; NOT pushed / no PR / not merged. **Program 2 now spans Phases 12–32. Phase 33 NOT started (recommended: a deterministic read-only export / audit trail of the whole assurance chain 26→32 as an on-demand artefact — no new persistence, no scheduling, no Apply).**)
>
> Prior update: 2026-07-19 (**Engineering Brain PROGRAM 2, Phase 31 — Knowledge Assurance & Audit Report (FINAL).** Branch `eng-brain-phase31-knowledge-assurance` from the Phase-30 tip `8265ea3`. The FINAL layer of Program 2: a deterministic, offline, READ-ONLY AUDIT that examines the whole knowledge programme — re-validation (26), coverage (27), readiness (28), contradiction (29) and assumptions (30) — for assurance FINDINGS, and grades whether the engineering knowledge can be ASSURED. Reports findings ONLY; authors no setup, schedules no test, changes nothing. **Core doctrine (test-guarded):** a single BLOCKING finding PREVENTS ASSURED (grade drops to NOT_ASSURED); the defects the audit catches include hidden assumptions, unresolved contradictions/regressions, missing transfer boundaries, single-context/dependent reliance, critical blind spots, stale/version-unaddressed knowledge, unknown-attribute/unverified-proxy reliance, readiness-vs-coverage + readiness-vs-assumption inconsistency, NON-DETERMINISM (a knowledge product lacking a stable content fingerprint) and DATA_MUTATION (a read-only build that wrote); the grade is RULE-BASED over visible severity counts (NOT an opaque score) and exposes the counts + the fired rule; too little established knowledge → INSUFFICIENT_EVIDENCE; restart + shuffled rows + different legal order → byte-identical (no timestamp in fingerprint; the audit's own structural self-check flags any sub-report that would break it). **NO migration / NO persistence — `DB_VERSION` stays 26**, `RULE_ENGINE_VERSION` 46.0. AssuranceFindingType 22; AssuranceSeverity 5 (blocking/major/moderate/minor/informational); ProgrammeAssuranceGrade 5 (assured/assured_with_limitations/partially_assured/not_assured/insufficient_evidence); grade ladder no-known→INSUFFICIENT, any blocking→NOT_ASSURED, else major→PARTIALLY, else moderate/minor→WITH_LIMITATIONS, else→ASSURED. NEW pure `strategy/assurance_finding.py`, `strategy/assurance_grade.py` (`grade_assurance`), `strategy/knowledge_assurance.py` (`audit(readiness, contradiction, assumptions, coverage, revalidation)`→(findings, has_known); dedups by (type, domain) keeping the most severe; structural fingerprint self-check; CLEAN when nothing above informational), `strategy/programme_assurance_report.py` (`build_programme_assurance_report`; blocking/major/moderate-minor/informational buckets + audit summary + grade detail) + `strategy/programme_assurance_report_render.py` (strings only, zero DB). NEW entry `SessionDB.build_programme_assurance_report` (READ-ONLY; reuses the shared `_build_knowledge_chain` [Phase-22 once + records], computes Phase-26/27/28/29/30 PURELY in memory; never calls the Phase-23/24/25/26/27/28/29/30 DB entries; no N+1; renderer zero DB; writes nothing; DB byte-identical; user_version 26). UI: pure `ui/engineering_assurance_vm.py` + `ui/engineering_assurance_panel.py` (`EngineeringAssurancePanel`, NO Apply/edit/schedule controls — asserted; `[VERDICT]`/`[REVIEW]` text tags not colour alone; Phase 12/…/30 coexistence asserted) placed near the TOP of Development History beneath the Phase-28 readiness executive summary; build OFF the Qt thread via the reused `MechanismAnnotationWorker(QThread)` with stale-worker protection (`_assurance_worker`). **Golden `config_id` + frozen allowlist + Apply-gate UNCHANGED; `_setup_constants.py` git-verified byte-identical.** Tests: `tests/test_phase31_{domain[16],golden[10],integration[8],safety[13],ui_construction[12]}.py` (59) — golden = mandated behaviours 1–10; integration proves Phase-22-once + Phase-23/24/25/26/27/28/29/30 DB entries never called + constant query count (no N+1) + renderer-no-DB + DB-hash/counts/user_version unchanged + the real pipeline raises no non-determinism/mutation finding. Phase 26/27/28/29/30 regression green. Runtime verified: DB byte-identical, restart-identical fingerprint, user_version 26, no setup-value leak, ASCII-clean. Doc: `docs/ENGINEERING_BRAIN_PHASE31_KNOWLEDGE_ASSURANCE.md`. Committed locally; NOT pushed / no PR / not merged. **Program 2 engineering-knowledge-assurance stack (Phases 12–31) COMPLETE. Phase 32 NOT started (authorisation stopped before 32).**)
>
> Prior update: 2026-07-19 (**Engineering Brain PROGRAM 2, Phase 30 — Engineering Assumption Register.** Branch `eng-brain-phase30-assumption-register` from the Phase-29 tip `2254996`. A deterministic, offline, READ-ONLY advisory layer that makes explicit the ASSUMPTIONS the programme's engineering knowledge relies on but has NOT established — things a conclusion depends on that are not themselves directly evidenced (transfer assumed, single-context result assumed to generalise, dependent evidence relied on as if independent, currency assumed, confirmed-good assumed to persist, version stability assumed, one side of an open contradiction assumed, unknown vehicle attributes, unverified proxies). It classifies each, states its impact, and lists ONLY assumptions — NEVER facts. Reuses the Phase-24 playbook boundaries + Phase-25/26/27/29 authorities verbatim. **Core doctrine (test-guarded):** FACTS ≠ ASSUMPTIONS (a directly-evidenced conclusion — strong convergence + current + no coverage gaps + no open contradiction — is a FACT and produces NO assumption; `is_factual` gates it); assumptions can ONLY CAP how ready knowledge may be, NEVER create readiness (`AssumptionImpact` has no positive member; `IMPACT_READINESS_CAP` maps every impact to a ceiling in {not_ready, context_bound_only, ready_with_limitations, ready} — never above); conservative bounds are LABELLED (`is_conservative_bound`, status `EXPLICIT_AND_LABELLED`) as a deliberate caution not a defect; restart + shuffled rows + different legal order → byte-identical (no timestamp in fingerprint). **NO migration / NO persistence — `DB_VERSION` stays 26**, `RULE_ENGINE_VERSION` 46.0. AssumptionType 16; AssumptionStatus 8 (explicit_and_labelled/evidence_backed_partially/unverified/at_risk/contradicted/conservative_bound/resolved/unknown); AssumptionImpact 6 (blocks_reliance/caps_readiness/narrows_scope/weakens_confidence/informational/unknown — all non-positive). NEW pure `strategy/assumption_classification.py`, `strategy/assumption_impact.py` (`IMPACT_READINESS_CAP` never lifts), `strategy/engineering_assumption.py` (`is_factual` + `derive_domain_assumptions(domain, convergence, revalidation_item, coverage_item, contradiction_item)`→assumptions, empty when factual; CONTRADICTION_SIDE only when open-with-standing), `strategy/programme_assumption_register.py` (`build_programme_assumption_register`; per-domain assumptions + programme-level from Phase-24 boundaries; blocking/capping/narrowing/informational/conservative buckets) + `strategy/programme_assumption_register_render.py` (strings only, zero DB). NEW entry `SessionDB.build_programme_assumption_register` (READ-ONLY; reuses the shared `_build_knowledge_chain` [Phase-22 once + records], computes Phase-26 re-validation + Phase-27 coverage + Phase-29 contradiction PURELY in memory; never calls the Phase-23/24/25/26/27/29 DB entries; no N+1; renderer zero DB; writes nothing; DB byte-identical; user_version 26). UI: pure `ui/engineering_assumption_vm.py` + `ui/engineering_assumption_panel.py` (`EngineeringAssumptionPanel`, NO Apply/edit/schedule controls — asserted; `[REVIEW]`/`[BOUND]` text tags not colour alone; Phase 12/…/29 coexistence asserted) in Development History beneath the Phase-29 panel; build OFF the Qt thread via the reused `MechanismAnnotationWorker(QThread)` with stale-worker protection (`_assumption_worker`). **Golden `config_id` + frozen allowlist + Apply-gate UNCHANGED; `_setup_constants.py` git-verified byte-identical.** Tests: `tests/test_phase30_{domain[13],golden[10],integration[7],safety[13],ui_construction[12]}.py` (55) — golden = mandated behaviours 1–10; integration proves Phase-22-once + Phase-23/24/25/26/27/29 DB entries never called + constant query count (no N+1) + renderer-no-DB + DB-hash/counts/user_version unchanged. Phase 28/29 regression green. Runtime verified: DB byte-identical, restart-identical fingerprint, user_version 26, no setup-value leak, ASCII-clean. Doc: `docs/ENGINEERING_BRAIN_PHASE30_ASSUMPTION_REGISTER.md`. Committed locally; NOT pushed / no PR / not merged. Next (FINAL): Phase 31 — knowledge assurance & audit report.)
>
> Prior update: 2026-07-19 (**Engineering Brain PROGRAM 2, Phase 29 — Knowledge Contradiction Resolution.** Branch `eng-brain-phase29-contradiction-resolution` from the Phase-28 tip `c67607a`. A deterministic, offline, READ-ONLY advisory layer that finds, per known engineering domain, where the evidence CONTRADICTS itself (a confirming AND a regressing conclusion for the same domain) and characterises each disagreement: its visible CAUSES and whether it is resolved by context, resolved by stronger independent evidence, superseded, or genuinely OPEN. Resolution STATUS ONLY: authors no setup, schedules no test, and is willing to say the evidence does not tell us which conclusion is right. Reuses the canonical Phase-25 record→domain mapping verbatim (invents NO domains); grounds every contradiction in REAL records (never a flag alone). **Core doctrine (test-guarded):** NEVER resolved by majority or averaging (counting records on each side decides nothing; a larger dependent count never wins); dependent evidence NEVER defeats independent (independence = distinct sessions + high confidence, NOT count); newer evidence does NOT automatically win (supersession requires the later side to be ALSO stronger, checked BEFORE the concurrent independence case so recency alone never decides); a version/context mismatch is always SURFACED as a visible cause; a contradiction MAY stay UNRESOLVED (a genuine same-context comparably-supported disagreement is reported open); restart + shuffled rows + different legal order → byte-identical (no timestamp in fingerprint). **NO migration / NO persistence — `DB_VERSION` stays 26**, `RULE_ENGINE_VERSION` 46.0. ContradictionCause 19 (context 9 + evidence-quality 5 + directional 2 + residual 3); ContradictionStatus 9 (not_a_contradiction/resolved_by_context/resolved_by_independence/resolved_by_supersession/resolved_within_noise/partially_resolved/unresolved/unresolved_insufficient/unknown); ladder context→supersession(later&stronger)→independence→both-weak→insufficient→UNRESOLVED. NEW pure `strategy/contradiction_cause.py` (`context_difference_causes` disjoint-value detection + `CONTEXT_RESOLVING_CAUSES`), `strategy/contradiction_resolution_status.py` (`resolve(signals)` ladder; never majority/recency), `strategy/knowledge_contradiction.py` (`detect_contradiction(domain, positive_records, negative_records)`→`KnowledgeContradiction`; per-side signals = independence NOT count), `strategy/programme_contradiction_report.py` (`build_programme_contradiction_report`; a contradiction exists only where a domain has BOTH a confirming and a regressing record) + `strategy/programme_contradiction_report_render.py` (strings only, zero DB). NEW entry `SessionDB.build_programme_contradiction_report` (READ-ONLY). **The shared `_build_knowledge_chain` gate was RELAXED** from "known_domains non-empty" to "known_domains non-empty OR any recorded evidence" — regressions retire domains OUT of known_domains, so the old gate hid exactly the contradiction evidence this phase analyses; the Phase-25 timeline already keeps negative learning visible, so this is a strict, consistent improvement (Phase 26/27/28 confirmation-based tests remain green; a fully-retired programme now correctly shows its regressed domains instead of nothing); empty programmes (no known domains AND no evidence) still yield no chain. No N+1; renderer zero DB; writes nothing; DB byte-identical; user_version 26. UI: pure `ui/engineering_contradiction_vm.py` + `ui/engineering_contradiction_panel.py` (`EngineeringContradictionPanel`, NO Apply/edit/schedule controls — asserted; `[OPEN]`/`[RESOLVED]` text tags not colour alone; Phase 12/…/28 coexistence asserted) in Development History beneath the Phase-27 panel; build OFF the Qt thread via the reused `MechanismAnnotationWorker(QThread)` with stale-worker protection (`_contradiction_worker`). **Golden `config_id` + frozen allowlist + Apply-gate UNCHANGED; `_setup_constants.py` git-verified byte-identical.** Tests: `tests/test_phase29_{domain[15],golden[10],integration[6],safety[13],ui_construction[12]}.py` (56) — golden = mandated behaviours 1–10; integration proves Phase-22-once + Phase-23/24/25 DB entries never called + constant query count (no N+1) + renderer-no-DB + DB-hash/counts/user_version unchanged. Phase 25/26/27/28 regression (107) green after the shared-chain relaxation. Runtime verified: DB byte-identical, restart-identical fingerprint, user_version 26, no setup-value leak, ASCII-clean. Doc: `docs/ENGINEERING_BRAIN_PHASE29_CONTRADICTION_RESOLUTION.md`. Committed locally; NOT pushed / no PR / not merged. Next: Phase 30 — engineering assumption register.)
>
> Prior update: 2026-07-19 (**Engineering Brain PROGRAM 2, Phase 28 — Engineering Knowledge Readiness Report.** Branch `eng-brain-phase28-knowledge-readiness` from the Phase-27 tip `d0a9ff7`. The EXECUTIVE-SUMMARY CAPSTONE of Program 2: a deterministic, offline, READ-ONLY advisory layer that states, per known engineering domain, WHETHER the evidence supports RELYING on the knowledge for a decision (ready / ready within limits / provisional / not yet ready — and why), and grades the whole programme with a TRANSPARENT, RULE-BASED grade. Readiness STATUS ONLY: "ready" means the evidence supports relying on it, NEVER "apply this setup"; recommends no setup, schedules no test, creates no experiment/campaign, never marks unvalidated knowledge ready. Synthesises the Phase-25 convergence + Phase-26 re-validation + Phase-27 coverage authorities VERBATIM; invents NO domains. **Core doctrine (test-guarded):** "ready" ≠ "apply this setup" (the phrase appears only in denial text); unvalidated knowledge (converging/mixed/insufficient/unknown) is NEVER READY; a recorded conflict or regression BLOCKS readiness (CONFLICTED/REGRESSED) AND prevents a HIGH grade regardless of how many other domains are ready; the grade is RULE-BASED over visible counts (NOT an opaque score/weight) and always exposes the counts + the fired rule; too few assessable domains → INSUFFICIENT_EVIDENCE rather than a false grade; restart + shuffled rows + different legal order → byte-identical (no timestamp in fingerprint). **NO migration / NO persistence — `DB_VERSION` stays 26**, `RULE_ENGINE_VERSION` 46.0. KnowledgeReadinessStatus 11 (SUPERSEDED/CONFLICTED/REGRESSED/NEEDS_REVALIDATION/INSUFFICIENT_EVIDENCE/NEEDS_MORE_EVIDENCE/READY/READY_WITH_LIMITATIONS/CONTEXT_BOUND_ONLY/PROVISIONAL/UNKNOWN; deterministic ladder, top wins; RELYABLE={ready,ready_with_limitations,context_bound_only}, BLOCKING={conflicted,regressed}); ProgrammeReadinessGrade 4 (HIGH/MEDIUM/LOW/INSUFFICIENT_EVIDENCE; thresholds MIN_ASSESSABLE=2, HIGH_RELYABLE_FRACTION=0.75, MEDIUM=0.40; any blocker caps below HIGH). NEW pure `strategy/knowledge_readiness.py` (`classify_readiness(convergence, revalidation_item, coverage_item)`→`KnowledgeReadinessItem` with limiting factors / `usable_as` / what-would-raise), `strategy/readiness_grade.py` (`grade_programme(items)`→rule-based grade + counts + rule + reasons), `strategy/programme_readiness_report.py` (`build_programme_knowledge_readiness_report`; ready/ready-with-limits/blocked/not-yet buckets + executive summary + grade detail) + `strategy/programme_readiness_report_render.py` (strings only, zero DB). NEW entry `SessionDB.build_programme_knowledge_readiness_report` (READ-ONLY; reuses the shared `_build_knowledge_chain` [Phase-22 once + records], computes Phase-26 re-validation + Phase-27 coverage PURELY in memory; never calls the Phase-23/24/25/26/27 DB entries; no N+1; renderer zero DB; writes nothing; DB byte-identical; user_version 26). UI: pure `ui/engineering_readiness_vm.py` + `ui/engineering_readiness_panel.py` (`EngineeringReadinessPanel`, NO Apply/edit/schedule controls — asserted; `[READY]`/`[REVIEW]` text tags not colour alone; Phase 12/…/27 coexistence asserted) placed at the TOP of the Development History page (the executive summary); build OFF the Qt thread via the reused `MechanismAnnotationWorker(QThread)` with stale-worker protection (`_readiness_worker`). **Golden `config_id` + frozen allowlist + Apply-gate UNCHANGED; `_setup_constants.py` git-verified byte-identical.** Tests: `tests/test_phase28_{domain[20],golden[10],integration[7],safety[13],ui_construction[12]}.py` (62) — golden = mandated behaviours 1–10; integration proves Phase-22-once + Phase-23/24/25/26/27 DB entries never called + constant query count (no N+1) + renderer-no-DB + DB-hash/counts/user_version unchanged. Phase 26/27 regression green after reusing the shared chain. Runtime verified: DB byte-identical, restart-identical fingerprint, user_version 26, no setup-value leak, ASCII-clean. Doc: `docs/ENGINEERING_BRAIN_PHASE28_KNOWLEDGE_READINESS.md`. Committed locally; NOT pushed / no PR / not merged. Next: Phase 29 — knowledge contradiction resolution.)
>
> Prior update: 2026-07-19 (**Engineering Brain PROGRAM 2, Phase 27 — Evidence Coverage & Blind-Spot Mapping.** Branch `eng-brain-phase27-evidence-coverage` from the Phase-26 tip `23695ef`. A deterministic, offline, READ-ONLY advisory layer ABOVE the Phase-25 convergence authority: maps, per known engineering domain, WHERE the evidence is well supported and WHERE more evidence would strengthen confidence (a "blind spot"), across 18 visible COVERAGE DIMENSIONS, ranking blind spots by severity. Coverage STATUS ONLY: recommends no setup, schedules no test, creates no experiment/campaign, never treats the absence of evidence as a negative result. Reuses Phase-23 transfer + Phase-25 convergence + Phase-26 re-validation verbatim; invents NO domains; re-implements NO record→domain mapping (reuses the canonical Phase-25 `_record_domains`). **Core doctrine (test-guarded):** MISSING (untested) is a DISTINCT status from REGRESSION_ONLY (a recorded negative) and never conflated — missing means untested, never wrong; a blind spot is NOT a fault (early-stage gaps → INFORMATIONAL, "not a concern"); a large DEPENDENT-evidence count is never strong coverage (DEPENDENT_EVIDENCE_ONLY regardless of magnitude); one distinct track/car/driver/compound/discipline/version is SINGLE_CONTEXT_ONLY, never multi-context; severity = the gap between RELIANCE (Phase-22 maturity/confidence/confirmed-good) and evidence ROBUSTNESS (independent lines/contexts/convergence), so a strong claim on thin/single-context/dependent evidence is the CRITICAL/MATERIAL blind spot and an unresolved conflict in a relied-upon domain is CRITICAL; restart + shuffled rows + different legal order → byte-identical (no timestamp in fingerprint). **NO migration / NO persistence — `DB_VERSION` stays 26**, `RULE_ENGINE_VERSION` 46.0. 18 dimensions = context-breadth (track/layout/car/driver/discipline/gt7_version/tyre_compound/corner_phase/corner_type) + evidence-quality (independent_replication/repeated_confirmation/high_confidence_evidence/regression_check/confirmed_good_verification/conflict_resolution/convergence_achieved/transfer_validation/revalidation_currency); CoverageStatus 9; BlindSpotSeverity 5. NEW pure `strategy/coverage_dimension.py` (3 enums + visible thresholds + GAP_STATUSES + priority maps), `strategy/evidence_coverage.py` (`coverage_signals` + `assess_domain_coverage`→`DomainCoverage`), `strategy/knowledge_blind_spot.py` (`classify_blind_spot`→`KnowledgeBlindSpot`; reliance×robustness severity ladder; every blind spot carries "not a fault … untested, never wrong"), `strategy/programme_coverage_report.py` (`build_programme_evidence_coverage_report`; buckets records per domain via the reused canonical mapping; ranks raised/early-stage/unassessable) + `strategy/programme_coverage_report_render.py` (strings only, zero DB). SHARED helper `SessionDB._build_knowledge_chain` MODIFIED to ADDITIVELY return the evidence records it already read (single bulk read preserved) so Phase 27 derives context breadth with NO second query; NEW entry `SessionDB.build_programme_evidence_coverage_report` (READ-ONLY; computes Phase-26 re-validation purely in memory; never calls the Phase-23/24/25/26 DB entries; no N+1; renderer zero DB; writes nothing; DB byte-identical; user_version 26). UI: pure `ui/engineering_coverage_vm.py` + `ui/engineering_coverage_panel.py` (`EngineeringCoveragePanel`, NO Apply/edit/schedule controls — asserted; `[REVIEW]`/`[COVERED]` text tags not colour alone; Phase 12/…/26 coexistence asserted) in Development History beneath the Phase-26 panel; build OFF the Qt thread via the reused `MechanismAnnotationWorker(QThread)` with stale-worker protection (`_coverage_worker`). **Golden `config_id` + frozen allowlist + Apply-gate UNCHANGED; `_setup_constants.py` git-verified byte-identical.** Tests: `tests/test_phase27_{domain[17],golden[10],integration[7],safety[13],ui_construction[12]}.py` (69) — golden = mandated behaviours 1–10; integration proves Phase-22-once + Phase-23/24/25/26 DB entries never called + constant query count (no N+1) + renderer-no-DB + DB-hash/counts/user_version unchanged + no setup-value leak. Phase 25/26 regression green after the chain change. Runtime verified: DB byte-identical, restart-identical fingerprint, user_version 26, no setup-value leak, ASCII-clean. Doc: `docs/ENGINEERING_BRAIN_PHASE27_EVIDENCE_COVERAGE.md`. Committed locally; NOT pushed / no PR / not merged. Next: Phase 28 — engineering knowledge readiness report.)
>
> Prior update: 2026-07-19 (**Engineering Brain PROGRAM 2, Phase 26 — Knowledge Decay & Re-validation Status.** Branch `eng-brain-phase26-knowledge-revalidation` from the Phase-25 tip `30de0bb`. A deterministic, offline, READ-ONLY advisory layer ABOVE the Phase-25 convergence/timeline authority: reports, per knowledge domain, whether established engineering knowledge remains CURRENT/PROTECTED or may need RE-VALIDATION — because the context or GT7 version changed, or the evidence weakened (conflict / regression / dependence / unknown date-or-context). Reports STATUS ONLY: schedules nothing, creates no reminders or future dates, generates no test plan, authors/applies no setup, never touches the Apply gate. Reuses Phase-25 verbatim (no new diagnosis/transfer/timeline/convergence logic). **Core doctrine (test-guarded):** age ALONE never decays — NO date arithmetic anywhere in the pure layer (`timedelta`/`days_since`/`fromisoformat`/`strptime`/`max_age` all absent + asserted; no fixed expiry / "older than N days"); dates are evidence data never authority; a newer record is never automatically more correct; a version change re-validates ONLY version-sensitive domains (those whose Phase-25 transfer limitations mention the GT7 version), version-insensitive knowledge is NOT invalidated; confirmed-good stays PROTECTED unless explicitly invalidated (an unrelated direction regressing does not un-protect it); conflict weakens without deleting history; regression weakens/retires the tested direction; superseded stays visible-but-inactive; retired stays retired; unknown date→INSUFFICIENT_DATE_EVIDENCE, unknown context→INSUFFICIENT_CONTEXT_EVIDENCE (never auto-invalidate); restart + shuffled rows + different legal order → byte-identical (fingerprint has no timestamp). **NO migration / NO persistence — `DB_VERSION` stays 26**, `RULE_ENGINE_VERSION` 46.0. NEW pure `strategy/revalidation_reason.py` (`RevalidationReason` 20; `context_change_reason`; `reasons_from_signals` — a reason emitted ONLY when its explicit signal present; GT7 reason gated on version_sensitive+version_changed), `strategy/knowledge_decay.py` (`programme_context_changes` — version_changed True when an excluded other-group shares source car+driver but differs in gt7_version; changed_fields read from Phase-22 `excluded_reasons.differing_fields` VERBATIM; `decay_signals`; `MIN_INDEPENDENT_FOR_ROBUST`=2), `strategy/revalidation_status.py` (`KnowledgeFreshnessStatus` 12 + `FRESHNESS_PRIORITY` visible ordering + `classify_revalidation` deterministic ladder superseded→retired→conflict→regression→version→date→context→confirmed-good→strongly_converged→context-bound→dependent-heavy/converging→insufficient→unknown), `strategy/programme_revalidation_report.py` (`build_revalidation_report(timeline, programme)`→`ProgrammeRevalidationReport` buckets/totals/safety/fingerprint) + `strategy/programme_revalidation_report_render.py` (strings only, zero DB, no setup values / scheduling / dates-as-action). NEW shared orchestration helper `SessionDB._build_knowledge_chain` (builds the Phase-22 report EXACTLY ONCE, then derives Phase-23 transfer + Phase-24 playbook + Phase-25 timeline PURELY in memory via ONE bounded `_timeline_evidence_records` bulk read; never calls the Phase-23/24/25 DB entries) consumed by NEW entry `SessionDB.build_programme_revalidation_report` (READ-ONLY; no N+1 [query count constant vs event count]; renderer zero DB; writes nothing; DB byte-identical; user_version 26). UI: pure `ui/engineering_revalidation_vm.py` + `ui/engineering_revalidation_panel.py` (`EngineeringRevalidationPanel`, NO Apply/Freeze/Complete/Execute/edit/schedule controls — asserted; `[PROTECT]`/`[REVIEW]` text tags not colour alone; Phase 12/19/…/25 coexistence asserted) in Development History beneath the Phase-25 timeline panel; build OFF the Qt thread via the reused `MechanismAnnotationWorker(QThread)` with stale-worker-result protection (`_revalidation_worker`). **Golden `config_id` + frozen allowlist + Apply-gate UNCHANGED; `_setup_constants.py` git-verified byte-identical.** Tests: `tests/test_phase26_{domain[20],golden[10],integration[7],safety[13],ui_construction[12]}.py` (70) — golden = mandated behaviours 1–10; integration proves Phase-22-once + Phase-23/24/25 DB entries never called + constant query count (no N+1) + renderer-no-DB + DB-hash/counts/user_version unchanged. Runtime verified: DB byte-identical after repeated runs, restart-identical fingerprint, user_version 26, no setup-value leak, ASCII-clean. Doc: `docs/ENGINEERING_BRAIN_PHASE26_KNOWLEDGE_REVALIDATION.md`. Committed locally; NOT pushed / no PR / not merged. Next: Phase 27 — evidence coverage & blind-spot mapping.)
>
> Prior update: 2026-07-19 (**Engineering Brain PROGRAM 2, Phase 25 — Stable Knowledge Timeline & Convergence.** Branch `eng-brain-phase25-knowledge-timeline` from the Phase-24 tip `8fae013`. A deterministic, offline, read-only TEMPORAL knowledge layer explaining how engineering understanding evolved across compatible events, where evidence GENUINELY converged through INDEPENDENT repeated evidence, where it remains unresolved, and where apparent repetition is only duplicated/dependent. Dates are evidence data only — recency NEVER automatically means correctness. NEVER generates setup values, recommends setup changes, schedules tests, creates experiments/campaigns, optimises, applies, or mutates engineering data. No ML/stats/NLP/AI/scheduling/optimiser. **NO migration / NO persistence — `DB_VERSION` stays 26** (rebuilt from immutable records; restart-identical + shuffled-row-identical fingerprint; no timestamps in fingerprint), `RULE_ENGINE_VERSION` 46.0. NEW pure `strategy/evidence_independence.py` (`EvidenceIndependence` 7: INDEPENDENT/PARTIALLY_INDEPENDENT/SAME_SESSION/SAME_CAMPAIGN/SAME_SOURCE_RECORD/DERIVED_FROM_EXISTING_CONCLUSION/UNKNOWN; repeated same-source evidence NEVER = multiple confirmations; Phase-22/23/24 re-statements are one lineage), `strategy/knowledge_transition.py` (`KnowledgeTransitionType` 18 — conflict/regression/supersession/uncertainty never collapsed; newer-weaker → NO_MATERIAL_CHANGE; stronger-later reopens retired), `strategy/knowledge_timeline.py` (`TimelinePoint` + `build_timeline`; internally sorts evidence → INSERTION-ORDER INDEPENDENT; unknown fields explicit), `strategy/knowledge_convergence.py` (`ConvergenceStatus` 10: STRONGLY_CONVERGED/CONVERGING/STABLE_BUT_CONTEXT_BOUND/STABLE_CONFIRMED_GOOD/MIXED/CONFLICTING/REGRESSED/SUPERSEDED/INSUFFICIENT_EVIDENCE/UNKNOWN; strong needs ≥2 independent lines + established Phase-22 maturity + no unresolved regression/conflict; context-bound never labelled strong; reuses Phase-22 maturity/confidence + Phase-24 confirmed-good), `strategy/programme_timeline_report.py` (`build_programme_timeline` joins Phase-22 graph + Phase-24 playbook + evidence records; maps records→domains via VISIBLE Phase-22 keyword maps; confirmation/regression counts derived from raw records so negative learning stays visible even when a direction is retired) + `strategy/programme_timeline_report_render.py`. Orchestrator `SessionDB.build_programme_knowledge_timeline` (READ-ONLY; composes Phase-22 report EXACTLY ONCE, derives Phase-23 + Phase-24 PURELY [never the Phase-23/24 DB entries], ONE bounded bulk read `_timeline_evidence_records` over compatibility columns [query count constant]; no N+1; writes nothing). UI: pure `ui/engineering_timeline_vm.py` + `ui/engineering_timeline_panel.py` (`EngineeringTimelinePanel`, NO Apply/Create-Experiment/Schedule/Optimise/setup-editor/edit controls — asserted; confirmed-good `[PROTECT]`/conflict `[REVIEW]` text tags not colour alone; Phase 12/19/20/21/22/23/24 coexistence asserted) in Development History beneath the Phase-24 panel; build OFF the Qt thread via the reused `MechanismAnnotationWorker(QThread)` with stale-worker-result protection. **Golden `config_id` + frozen allowlist + Apply-gate UNCHANGED; `_setup_constants.py` + protected runtime files git-verified byte-identical.** Tests: `tests/test_phase25_*` (93) — independence (same-record/session/campaign/independent/unknown), convergence+transition (strong-needs-independent, dependent-no-false-converge, confirmed-good distinct, conflict/regression/retired/superseded/context-bound, newer-weaker-no-override), timeline (chronological+deterministic tie-breaks, unknown/equal dates, insertion-order-independent, conflict introduce+resolve visible, history not overwritten, no-timestamp fingerprint), integration (Phase-22 once + Phase-23/24 DB never called + constant query count no N+1 + renderer-no-DB + DB-hash/counts/user_version unchanged), golden 1/2/3/5/7/8/9/10 + empty, safety (no setup-gen/values/writes, no scheduler/optimiser, no 2nd graph/transfer, session_date-not-recorded_at, _setup_constants unchanged, no-AI scan), UI construction + stale-worker + coexistence. Regression: Phase 17–24 non-UI (84) + session_db/no-AI (34) + Phase 22/23/24 UI (7+7+9) + Apply-gate/config-safety (125+1skip) green. Doc: `docs/ENGINEERING_BRAIN_PHASE25_KNOWLEDGE_TIMELINE.md`. Committed locally; NOT pushed / no PR / not merged (master does not contain Phase 25). Next: Phase 26 — knowledge decay & re-validation prompts.)
>
> Prior update: 2026-07-19 (**Engineering Brain PROGRAM 2, Phase 24 — Cross-Programme Engineering Playbook.** Branch `eng-brain-phase24-engineering-playbook` from the Phase-23 tip `4559bed`. Assembles the reusable engineering knowledge across the driver's car stable into a deterministic read-only INVESTIGATION playbook (NOT a baseline setup). Answers: which mechanisms repeatedly matter, which behaviours are confirmed-good and must be protected, which knowledge is safely reusable / a weak hypothesis / non-transferable, what to investigate first on a new programme, what evidence to recollect, and the explicit knowledge boundaries. NEVER generates/copies setup values, recommends a numerical starting setup, applies a setup, creates/schedules an experiment, creates/updates a campaign, optimises, mutates records, persists, calls AI, uses randomness/wall-clock, infers unknown vehicle attributes, creates a second knowledge graph, or recreates Phase-23 transfer logic (TransferLevel reused verbatim). No ML/stats/NLP/AI/optimisation/scheduling. **NO migration / NO persistence — `DB_VERSION` stays 26** (rebuilt from immutable records; restart-identical fingerprints, no timestamps in fingerprint), `RULE_ENGINE_VERSION` 46.0. NEW pure `strategy/stable_themes.py` (`StableEngineeringTheme` per ESTABLISHED source domain; grounded in Phase-22/23 not keyword-grouping; recurrence=1 source+reusable targets, evidence=confirmations, transfer eligibility reused verbatim with "hypothesis-only never copy setup" meaning, confirmed-good protections + negatives + exclusions + calc inputs), `strategy/investigation_priority.py` (`InvestigationCategory` PROTECT_FIRST/RECOLLECT_EVIDENCE/DO_NOT_REUSE/VALIDATE_EARLY/CONTEXT_SPECIFIC/INVESTIGATE; deterministic ladder + transparent weighted score over 11 VISIBLE dimensions/weights/caps; masking_conflict marks a harmful direction in a confirmed-good domain), `strategy/knowledge_boundary.py` (`BoundaryType` 16: car/manufacturer/drivetrain/category/track/track-layout/discipline/driver/gt7-version/tyre-rule/fuel-rule specific + insufficient/conflicting evidence + unknown_vehicle_attribute [never guessed] + failed_historical_outcome + unverified_transfer_proxy), `strategy/new_programme_brief.py` (`NewProgrammeBrief` per target: established/eligible-cautious-reuse[hypothesis]/protect/needs-early-validation/recollect/must-not-reuse/negatives/uncertainties/limits + explicit no-setup-transferred statement), `strategy/engineering_playbook.py` (`EngineeringPlaybook` top-level + `build_engineering_playbook`; joins Phase-22 graph + Phase-23 candidates into normalised per-domain records, runs the 4 layers, deterministic tie-breakers, fingerprint) + `strategy/engineering_playbook_render.py`. Orchestrator `SessionDB.build_programme_engineering_playbook` (READ-ONLY; composes Phase-22 report EXACTLY ONCE, derives Phase-23 transfer PURELY [no 2nd Phase-22 build, never calls the Phase-23 DB entry], runs Phase-24 assembler; no N+1; writes nothing; honest empty state when no known domains). UI: pure `ui/engineering_playbook_vm.py` + `ui/engineering_playbook_panel.py` (`EngineeringPlaybookPanel`, NO Apply/Create-Experiment/Schedule/Optimise/setup-editor/import/copy-setup/edit controls — asserted; confirmed-good rendered visually distinct; Phase 12/19/20/21/22/23 coexistence asserted) embedded in Development History beneath the Phase-23 panel; build OFF the Qt thread via the reused `MechanismAnnotationWorker(QThread)` with stale-worker-result protection (handler guards on current worker). **Golden `config_id` + frozen allowlist + Apply-gate + engine-wiring-status UNCHANGED; `_setup_constants.py` + protected runtime files git-verified byte-identical.** Tests: `tests/test_phase24_*` (76) — domain (grounded themes, recurrence, confirmed-good extraction, failed-direction preservation, contradiction handling, boundaries, unknown-attrs, fingerprint stability/no-timestamps, stable ordering, empty/single), transfer-integration (levels consumed unchanged, NOT_TRANSFERABLE stays, SUPPORTED no setup values, gearbox car/track, track/fuel isolation, driver same-driver, version cap, unknown conservative, proxy labelled, no setup-field values), query-shape (Phase-22 built once, Phase-23 DB entry never called, constant vs campaign count no N+1, renderer-no-DB), golden 1/2/4/5/6 + real production path [all table counts + DB-file hash unchanged] + restart determinism, safety (no setup-gen/apply/optimiser/scheduler, no 2nd graph, TransferLevel reused, no-write, _setup_constants unchanged, no-AI scan), UI construction + stale-worker + prior-phase coexistence + off-thread. Phase 17–23 non-UI regression (57) + session_db/no-AI (34) + Phase 22/23 UI (14) + config-safety (30) + Apply-gate suites (95+1skip) green. Doc: `docs/ENGINEERING_BRAIN_PHASE24_ENGINEERING_PLAYBOOK.md`. Committed locally; NOT pushed / no PR / not merged. Next: Phase 25 — stable knowledge timeline & convergence.)
>
> Prior update: 2026-07-19 (**Engineering Brain PROGRAM 2, Phase 23 — Knowledge Transfer Eligibility & Cross-Car Engineering Reuse.** Branch `eng-brain-phase23-knowledge-transfer` from the Phase-22 tip `0ce6721`. A deterministic Engineering Knowledge TRANSFER layer — NOT setup transfer; it determines whether established engineering KNOWLEDGE (mechanisms, handling behaviour) is likely reusable in another compatible context (e.g. another Porsche Gr.3). Transfers NO setup values, recommends applying NOTHING, imports NOTHING, decides NOTHING; every transfer level fixed by VISIBLE deterministic rules (no inference beyond them). No ML/stats/NLP/AI/optimisation/scheduling/graph-libraries. **NO migration / NO persistence — `DB_VERSION` stays 26** (all reconstructed from immutable records; restart-identical fingerprints), `RULE_ENGINE_VERSION` 46.0. NEW pure `strategy/transfer_rules.py`: all rules VISIBLE CONSTANTS each with why+authority (same_manufacturer/same_drivetrain/same_layout/same_race_category/same_suspension_architecture[mfr+category proxy]/compatible_gt7_version/same_driver) + `car_attributes` (manufacturer/drivetrain/layout/category derived deterministically from GT7 car name via visible maps CAR_DRIVETRAIN_REGISTRY/DRIVETRAIN_KEYWORDS/DRIVETRAIN_LAYOUT/CATEGORY_KEYWORDS, unknown STAYS unknown never guessed) + `DOMAIN_TRANSFER_CLASS` (17 domains → architecture_dependent/handling_drivetrain/car_track_specific/context_bound/driver_specific with reasons). NEW pure `strategy/knowledge_transfer.py`: `TransferLevel` NOT_TRANSFERABLE/VERY_LOW/LOW/MEDIUM/HIGH/SUPPORTED + `evaluate_transfer` (KnowledgeTransferCandidate; level decided ONLY by rules + domain class; established-source gate; context-bound/gearbox[car-track specific, only if explicitly supported]/driver-specific/version-incompatible handling; every field reason+source+calculation). NEW pure `strategy/engineering_reuse.py`: `summarise_reuse` → reusable ("reusable because...") / needs_more_evidence ("additional evidence still required...") / not_reusable ("not reusable because...") + isolated targets; NEVER recommends applying. NEW pure `strategy/programme_transfer_report.py` (`build_transfer_report` evaluates every established source-domain × target, summarises, attaches visible rule catalogue, deterministic fingerprint) + `strategy/programme_transfer_report_render.py`. Orchestrator `SessionDB.build_programme_transfer_report` (READ-ONLY; composes Phase-22 programme knowledge report ONCE [source domains + other compatibility groups as targets]; no per-campaign query; writes nothing). UI: pure `ui/engineering_transfer_vm.py` + `ui/engineering_transfer_panel.py` (`EngineeringTransferPanel`, NO Apply/Execute/Import/Copy-Setup/edit controls — asserted; Phase 12/19/20/21/22 coexistence asserted) embedded in Development History beneath the Phase-22 panel; build OFF the Qt thread via the reused `MechanismAnnotationWorker(QThread)`. **Golden `config_id` + frozen allowlist + Apply-gate + engine-wiring-status UNCHANGED; protected runtime files git-verified byte-identical.** Tests: `tests/test_phase23_*` (62) — transfer-rules (car-attribute derivation known/unknown/empty, rule eval, visible catalogue, domain classes), eligibility (established gate, architecture SUPPORTED/LOW, context-bound/gearbox/driver-specific/version, handling-on-drivetrain, reuse grouping+isolation), golden A–C + real production path [writes nothing] + restart determinism + empty DB + ASCII render, query-shape (constant vs campaign count no N+1, renderer-no-DB), safety (no forbidden imports/wall-clock, no setup-transfer/apply/import/copy, visible rule constants, unlike-never-transfer, no-write, never-recommends-applying, no-AI scan), UI construction + prior-phase coexistence + off-thread. Phase 20–22 non-UI regression (incl. touched session_db/dashboard/development_history_page) green; no-AI architecture guard green. Doc: `docs/ENGINEERING_BRAIN_PHASE23_KNOWLEDGE_TRANSFER.md`. Committed locally; NOT pushed / no PR / not merged. Next: Phase 24 — cross-programme engineering playbook.)
>
> Prior update: 2026-07-19 (**Engineering Brain PROGRAM 2, Phase 22 — Engineering Knowledge Graph & Multi-Event Knowledge Roll-Up.** Branch `eng-brain-phase22-knowledge-graph` from the Phase-21 tip `30df17e`. The first programme-level Engineering Knowledge Graph: a deterministic, explainable map of what the Brain knows, how well, on what evidence, and what remains unknown — organised by ENGINEERING DOMAIN and rolled up across compatible events. NOT AI/graph-theory/ML/optimisation/scheduling — deterministic aggregation of existing authorities. NEVER decides/optimises/prioritises/schedules/completes/applies/creates/mutates. No ML/stats/NLP/AI/Bayesian/graph-libraries/network. **NO migration / NO persistence — `DB_VERSION` stays 26** (all reconstructed from immutable records; restart-identical fingerprints), `RULE_ENGINE_VERSION` 46.0. NEW pure `strategy/engineering_knowledge_graph.py`: `KnowledgeDomain` 17 (Differential/Suspension/Ride Height/Springs/Anti-roll Bars/Dampers/Alignment/Brake Balance/Aero/Tyres/Fuel/Gearbox/Track Surface/Track Segments/Vehicle Balance/Weight Transfer/Driver Technique) + `build_knowledge_graph`; VISIBLE keyword maps FIELD/FAMILY/MECHANISM_DOMAIN_KEYWORDS (unmapped → no domain, NO inference); each DomainKnowledge = knowledge_state/confidence/maturity/remaining_uncertainty (each reason+source+calculation) + supporting_campaigns/experiments/mechanisms/evidence + known_limitations; enumerates ALL domains, empty ones reported MISSING. NEW pure `strategy/multi_event_rollup.py`: `build_rollup`; compatibility key = (car, discipline, gt7_version, driver) [track/layout MAY differ]; NEVER merges unlike contexts (every exclusion names differing fields); every merge/exclusion explained. NEW pure `strategy/knowledge_maturity.py`: `KnowledgeMaturity` UNKNOWN/EMERGING/DEVELOPING/ESTABLISHED/MATURE/COMPLETE/PLATEAUED + `classify_maturity` (ladder over Phase-19/20/21, no invented weighting). NEW pure `strategy/programme_knowledge_report.py` (`build_programme_knowledge` rolls up + builds graph for primary group, lists other groups + exclusion reasons, deterministic fingerprint) + `strategy/programme_knowledge_report_render.py`. Orchestrator `SessionDB.build_programme_knowledge_report` (READ-ONLY; one SELECT DISTINCT over dev-records → distinct events; builds Phase-21 season report ONCE per COMPATIBLE event + enriches campaigns with Phase-21 knowledge_state [join not recompute]; incompatible events surfaced not merged; writes nothing). UI: pure `ui/engineering_knowledge_graph_vm.py` + `ui/engineering_knowledge_graph_panel.py` (`EngineeringKnowledgeGraphPanel`, NO Apply/Approve/Freeze/Complete/Execute/edit/schedule controls — asserted; Phase 12/19/20/21 coexistence asserted) embedded in Development History beneath the Phase-21 panel; build OFF the Qt thread via the reused `MechanismAnnotationWorker(QThread)`. **Golden `config_id` + frozen allowlist + Apply-gate + engine-wiring-status UNCHANGED; protected runtime files git-verified byte-identical.** Tests: `tests/test_phase22_*` (71) — maturity (all levels + explained + sourced), knowledge-graph (visible non-inferred mapping + aggregation + missing domains + multi-track/conflict limitations + large graph + no graph libs), rollup (compatible merge + incompatible-not-merged+reason + dedup + assembly), golden A–C + real production path [writes nothing] + restart determinism + empty DB, query-shape (constant query count vs campaign count no N+1, renderer-no-DB), safety (no forbidden imports/wall-clock, no scheduling/execution/optimisation, visible domain maps, unlike-never-merged, no-write, completion-stays-Phase-18, no-AI scan), UI construction + prior-phase coexistence + off-thread. Phase 19–21 non-UI regression (incl. touched session_db/dashboard/development_history_page) green; no-AI architecture guard green. Doc: `docs/ENGINEERING_BRAIN_PHASE22_ENGINEERING_KNOWLEDGE_GRAPH.md`. Committed locally; NOT pushed / no PR / not merged. Next: Phase 23 — knowledge transfer & cross-car generalisation.)
>
> Prior update: 2026-07-19 (**Engineering Brain PROGRAM 2, Phase 21 — Season Development Plan & Cross-Campaign Knowledge Map.** Branch `eng-brain-phase21-season-development` from the Phase-20 tip `dd0f1ea`. A deterministic, READ-ONLY season-level planning layer — the Engineering Director's dashboard. Where Phases 1–20 understand individual experiments/campaigns, Phase 21 understands the programme AS A WHOLE: what has been learned, where the largest knowledge gaps are, which vehicle systems are understood, where campaigns overlap/duplicate/support/contradict/depend/isolated. It ONLY EXPLAINS the current state of engineering — NEVER decides/optimises/reprioritises/schedules/completes/applies/creates. No ML/stats/NLP/AI/Bayesian/graph-optimisation/clustering/network/scheduling. **NO migration / NO persistence — `DB_VERSION` stays 26** (all reconstructed from existing records; restart-identical fingerprints), `RULE_ENGINE_VERSION` 46.0. NEW pure `strategy/season_development.py`: `summarize_season` → `SeasonDevelopment`; every metric a `SeasonMetric` with value+**reason+source+calculation** (campaign_count/active/completed/needing_confirmation/plateaued/high+low_confidence/total_engineering_value[Phase 17]/total_remaining_value/estimated_remaining_cost[Phase 19]/knowledge_completion) + engineering_summary. NEW pure `strategy/cross_campaign_map.py`: `CampaignRelationship` (NONE/RELATED/OVERLAPS/SUPPORTS/DEPENDS_ON/DUPLICATES/CONTRADICTS/BLOCKED_BY/ISOLATED) + `build_cross_campaign_map` — deterministic O(n²) pairwise, every edge EVIDENCE-GROUNDED (reason+supporting_evidence+authority; never inferred): DUPLICATES(same family+region+identical fields), CONTRADICTS(same family, confident-vs-contradictory), DEPENDS_ON/SUPPORTS(shared Phase-18 source_mechanism + confidence asymmetry, directional), OVERLAPS(same objective / shared setup field), BLOCKED_BY(same family, related contradiction), RELATED(same family diff region / shared mechanism), ISOLATED(no relation). NOT graph search/clustering/ML/optimisation. NEW pure `strategy/season_knowledge_map.py`: `SeasonKnowledgeState` (ENGINEERING_COMPLETE/WELL_UNDERSTOOD/EMERGING_CONFIDENCE/NEEDS_CONFIRMATION/CONTRADICTORY/LITTLE_EVIDENCE/NO_USEFUL_EXPERIMENTS/KNOWLEDGE_PLATEAU/UNKNOWN) + `classify_campaign_knowledge` (ladder over Phase-18 status + Phase-19 saturation + Phase-20 confidence/opportunity; reason+source+factors). NEW pure `strategy/season_engineering_report.py` (`build_season_report` joins Phase-18 programme + Phase-19 efficiency + Phase-20 quality by campaign_id into one normalised record each, PRESERVES order, runs the 3 layers, deterministic fingerprint) + `strategy/season_engineering_report_render.py`. Orchestrator `SessionDB.build_season_engineering_report` (READ-ONLY; composes Phase-18 programme ONCE, derives Phase-19 efficiency + Phase-20 quality PURELY from it + 1 registry read + 1 calibration read — no N+1, no double programme build; writes nothing). UI: pure `ui/engineering_season_vm.py` + `ui/engineering_season_panel.py` (`EngineeringSeasonPanel`, NO Apply/Approve/Freeze/Complete/Execute/edit/schedule controls — asserted; Phase 12/19/20 panel coexistence asserted) embedded in Development History beneath the Phase-20 panel; build OFF the Qt thread via the reused `MechanismAnnotationWorker(QThread)`. **Golden `config_id` + frozen allowlist + Apply-gate + engine-wiring-status UNCHANGED; protected runtime files git-verified byte-identical.** Tests: `tests/test_phase21_*` (72) — knowledge-map (all states + explained + sourced), relationships (all types + evidence-grounded + isolated + large-count + duplicate-id + no-graph-optimisation), development (metrics with reason/source/calc + report assembly + order preserved), golden A–B + real production path [writes nothing] + restart determinism + empty DB, query-shape (single build, constant query count no N+1, renderer-no-DB), safety (no forbidden imports/wall-clock, no scheduling/execution, no optimiser/graph-search, evidence-grounded edges, no-write, completion-stays-Phase-18, no-AI scan), UI construction + prior-phase coexistence + off-thread. Phase 18–20 non-UI regression (incl. touched session_db/dashboard/development_history_page) green; no-AI architecture guard green. Doc: `docs/ENGINEERING_BRAIN_PHASE21_SEASON_DEVELOPMENT.md`. Committed locally; NOT pushed / no PR / not merged. Next: Phase 22 — multi-event season roll-up & engineering narrative.)
>
> Prior update: 2026-07-19 (**Engineering Brain PROGRAM 2, Phase 20 — Confidence-Weighted Evidence & Development ROI.** Branch `eng-brain-phase20-confidence-and-roi` from the Phase-19 tip `f95345e`. Three deterministic, ADVISORY-ONLY analysis layers built ABOVE Phase 19 that MEASURE and decide nothing: **knowledge confidence** (how trustworthy are the conclusions?), **development ROI** (how much engineering knowledge could a further session add, at what cost/risk?), and **campaign opportunity** (is more work worthwhile?). NEVER completes/freezes/abandons a campaign, applies/approves a setup, creates experiments, alters outcomes, re-ranks, recomputes Phase-17 value or Phase-19 cost, or auto-prioritises. No ML/stats/NLP/AI/optimiser/Bayesian. **NO migration / NO persistence — `DB_VERSION` stays 26** (all reconstructed from existing records; restart-identical fingerprints), `RULE_ENGINE_VERSION` 46.0. NEW pure `strategy/knowledge_confidence.py`: `ConfidenceLevel` (UNKNOWN/VERY_LOW/LOW/MEDIUM/HIGH/VERY_HIGH) + `assess_campaign_confidence` — 7 components each with reason/source/**calculation** (confirmation_strength/repeatability/contradiction_level/mechanism_support/outcome_consistency/prediction_accuracy[context-level, excluded when uncalibrated]/remaining_uncertainty[informational]); overall = equal-weighted mean of INCLUDED components (weight 1.0 stated) banded by named constants, then VISIBLE caps (no evidence→UNKNOWN; conflicting→LOW; regression+0 confirmations→VERY_LOW; <MIN_REPEATABILITY confirmations→MEDIUM, mirroring Phase-18 VALIDATION_REQUIRED); every threshold a named constant. NEW pure `strategy/development_roi.py`: `estimate_campaign_roi` — engineering knowledge NOT lap time; expected_information_gain (Phase-19, not recomputed) + expected_confidence_gain (gap a legal test could close: full for discriminating, half for validation, 0 if untestable) + knowledge_gap (1−confidence) + estimated_session_value + cost_to_close_gap (Phase-19 cost verbatim) + remaining_risk + engineering_priority_reason (an EXPLANATION, not a ranking); NOT an optimiser — sorts/ranks/prioritises NOTHING. NEW pure `strategy/campaign_opportunity.py`: `classify_campaign_opportunity` — deterministic ladder → COMPLETE/NEARLY_COMPLETE/WORTH_ANOTHER_CONFIRMATION/WORTH_CONTRADICTION_TESTING/WORTH_MECHANISM_ISOLATION/NOT_WORTH_FURTHER_WORK/EVIDENCE_EXHAUSTED/KNOWLEDGE_PLATEAU/UNKNOWN; reads Phase-18 completion, NEVER overrides it. NEW pure `strategy/knowledge_quality.py` (`build_knowledge_quality` composes the three per campaign, PRESERVES Phase-19 order, no re-sort; totals + deterministic fingerprint) + `strategy/engineering_knowledge_quality_render.py`. Orchestrator `SessionDB.build_engineering_knowledge_quality` (READ-ONLY; reuses Phase-19 `build_engineering_efficiency` ONCE [read-only, no registry write] + one calibration read; no N+1; writes nothing). UI: pure `ui/engineering_confidence_vm.py` + `ui/engineering_confidence_panel.py` (`EngineeringConfidencePanel` — NOT `EngineeringKnowledgePanel`, which Phase 12 owns; NO Apply/Approve/Freeze/Complete/Execute/edit controls — asserted; Phase-12 panel coexistence asserted) embedded in Development History beneath the Phase-19 panel; build OFF the Qt thread via the reused `MechanismAnnotationWorker(QThread)`. **Golden `config_id` + frozen allowlist + Apply-gate + engine-wiring-status UNCHANGED; protected runtime files git-verified byte-identical.** Tests: `tests/test_phase20_*` (74) — confidence (levels/caps/components/calibration-inclusion/equal-weighted-mean/thresholds/determinism/garbage), roi (info+cost reused-not-recomputed, no-testable→0, gap=1−conf, risk levels, not-lap-time, disclaims-ranking), opportunity (all outcomes + never-overrides-completion + factors), golden A–C + real production path [writes nothing] + restart determinism + empty DB, query-shape (single build, constant query count no N+1, renderer-no-DB), safety (no forbidden imports/wall-clock, no completion/execution, not-an-optimiser, value/cost-not-recomputed, no-write, completion-stays-Phase-18, no-AI scan), UI construction + Phase-12 coexistence + off-thread. Phase 12–19 non-UI regression (incl. touched session_db/dashboard/development_history_page) green; no-AI architecture guard green. Doc: `docs/ENGINEERING_BRAIN_PHASE20_CONFIDENCE_AND_ROI.md`. Committed locally; NOT pushed / no PR / not merged. Next: Phase 21 — season development plan & cross-campaign knowledge map.)
>
> Prior update: 2026-07-19 (**Engineering Brain PROGRAM 2, Phase 19 — Campaign Persistence, Evidence Saturation & Cost of Knowledge.** Branch `eng-brain-phase19-campaign-persistence` from the Phase-18 tip. Three deterministic, ADVISORY-ONLY layers built ABOVE Phase 18: they MEASURE a campaign's age, evidence saturation ("is more testing still worth it?") and cost of knowledge (laps/tyres/fuel/time of remaining testing vs a session budget). They decide NOTHING — saturation is INDEPENDENT of campaign status; **completion stays governed by Phase 18**; the frozen Apply gate stays the sole route to the car. NEVER completes/freezes/abandons a campaign, applies/approves a setup, creates/updates experiments, alters outcomes, re-ranks, or recomputes Phase-17 value. No ML/stats/NLP/AI. **ADDITIVE, IDEMPOTENT migration — `DB_VERSION` 25 → 26** (new standalone metadata-only `engineering_campaign_registry` table; NO existing table/column/query altered), `RULE_ENGINE_VERSION` 46.0. NEW pure `strategy/evidence_saturation.py`: `EvidenceSaturation` (NOT_STARTED/EARLY/BUILDING/STRONG/SATURATED/OVERTESTED) + `assess_saturation` — every count exposed in `signals`, every threshold a named visible constant (`CONFIRMATIONS_FOR_STRONG`/`_SATURATED`/`OVERTESTED_REPEATS`/`EXECUTED_FOR_BUILDING`), every status carries `reasons`, `information_gain_remaining` high/moderate/low/none; status NEVER reads campaign status (proven identical across ACTIVE vs READY_TO_FREEZE). NEW pure `strategy/engineering_cost_model.py`: `estimate_experiment_cost` (A/B/A `laps=warmup+baseline+test(+coupled)+revert`, time/fuel/tyres, `value_per_lap`/`value_per_minute`/`info_gain_per_tyre_set`; **engineering_value reused VERBATIM from Phase 17, never recomputed**; all cost constants exposed) + `EngineeringBudget`/`plan_budget` (deterministic GREEDY fit in Phase-17 rank order — explicitly NOT an optimiser/scheduler; `budget_known=False` defers all when no budget). NEW pure `strategy/campaign_persistence.py`: `CampaignRegistryEntry` (metadata only: stable campaign_id + creation_session/first_seen/last_seen/notes/manual_archive_flag/completion_state/links; no setup/experiment/outcome) + `campaign_age_days` (dates are DATA, no wall-clock) + `build_engineering_efficiency` (composes campaigns+age+saturation+cost+budget, deterministic fingerprint). NEW pure `strategy/engineering_efficiency_render.py` (per-campaign age/saturation-reasons/cost/budget; no Apply/freeze/complete wording). The ONLY new write: `SessionDB.record_engineering_campaigns` (idempotent — INSERT OR IGNORE preserves first_seen/creation_session, UPDATE refreshes last_seen/last_updated/completion_state/linked_experiments; user notes/archive NEVER clobbered) + `get_campaign_registry`/`set_campaign_note`; `SessionDB.build_engineering_efficiency` (READ-ONLY by default — writes nothing; reuses `build_engineering_campaign_programme` ONCE + one registry read, no N+1; the registry capture is OPT-IN only when `register_session_id` is passed, best-effort, touches ONLY the registry). UI: pure `ui/engineering_efficiency_vm.py` + `ui/engineering_efficiency_panel.py` (`EngineeringEfficiencyPanel`, NO Apply/Approve/Freeze/Complete/Execute/edit controls — asserted) embedded in Development History beneath the Phase-18 panel; build OFF the Qt thread via the reused `MechanismAnnotationWorker(QThread)` (dashboard supplies session id + today's date → the off-thread build performs the additive registry capture). **Golden `config_id` + frozen allowlist + Apply-gate + engine-wiring-status UNCHANGED; protected runtime files byte-identical.** Tests: `tests/test_phase19_*` (66) — saturation (every status reachable+explained, thresholds/signals visible, independent-of-status, no hidden numbers), cost/budget (A/B/A laps, coupled, value-reused-not-recomputed, greedy fit, no optimiser, only-testable planned), persistence (registry round-trip, age, efficiency assembly, idempotent provenance, notes/archive preserved, restart, migration idempotency, opt-in capture), golden A–C + real production path (writes only registry) + restart determinism + empty DB, query-shape (single build, constant query count no N+1, renderer-no-DB), safety (no forbidden imports/wall-clock, no completion/execution, value-not-recomputed, versions, read-only build, capture-touches-only-registry, no-AI scan), UI construction + off-thread + no controls. Phase 8–18 version-affected suites (260) re-green after the DB_VERSION 25→26 bump (assertions updated); no-AI architecture + config-safety guards green. Doc: `docs/ENGINEERING_BRAIN_PHASE19_CAMPAIGN_PERSISTENCE.md`. Committed locally; NOT pushed / no PR / not merged. Next: Phase 20 — confidence-weighted evidence & development ROI.)
>
> Prior update: 2026-07-19 (**Engineering Brain PROGRAM 2, Phase 18 — Engineering Campaigns & Multi-Session Development Planning.** Branch `eng-brain-phase18-engineering-campaigns` from `60a7e48` (Phase-17 tip). A deterministic READ-ONLY layer that groups a Phase-17 experiment portfolio into coherent, multi-session vehicle-development CAMPAIGNS (what objective, what learned, what uncertain, which experiments, how close to complete). NOT a diagnosis/synthesis/ranking/lifecycle/Apply authority — it ORCHESTRATES existing ones; ranking stays owned by Phase 17, lifecycle by Phase 16, outcomes/reconciliation/calibration by Program 1. NEVER applies/approves/freezes / creates-updates experiments / alters outcomes / writes engineering records / performs hidden weighting / re-ranks independently of Phase 17 / marks a successful-but-unvalidated objective complete. No ML/stats/NLP/AI. **NO migration — `DB_VERSION` stays 25** (reconstructed from existing records; restart-identical fingerprints), `RULE_ENGINE_VERSION` 46.0. NEW pure `strategy/engineering_campaign.py`: `CampaignIdentity` (scoped to exact context, incompatible never merged) + `CampaignObjective` (bounded/evidence-derived/traceable; no "improve the setup") + `CampaignStatus` (NOT_STARTED/ACTIVE/BLOCKED/VALIDATION_REQUIRED/READY_TO_FREEZE/COMPLETED/ABANDONED/STALE) + `CampaignStageType` (DEFINE/DISCRIMINATE/INTERVENE/REVIEW/VALIDATE/FREEZE/RACE_READY) + `CampaignRole` (PRIMARY_DISCRIMINATOR/PRIMARY_INTERVENTION/SECONDARY_INDEPENDENT_TEST/VALIDATION_TEST/PROTECTION_CHECK/CONTINGENCY/RETIRED; references Phase-17 candidates, never copies ranking) + `CompletionCriterion` (explicit, campaign-specific) + `CampaignProgress` (transparent: progress_pct=criteria_satisfied/total, factors visible, no magic numbers) + `EngineeringCampaign`/`EngineeringCampaignProgramme`; deterministic grouping by objective key (issue-family, region); multi-session outcome projection from immutable development records (not-tested/tested/confirmed-across-sessions/regressed; incompatible excluded); stale detection from active applied-setup identity; recommended focus (ordered by status + Phase-17 value; ranking owned by Phase 17) + programme roadmap; `build_campaign_programme`. NEW pure `strategy/engineering_campaign_render.py` (programme summary + campaign list + detail + roadmap; no Apply/freeze wording; "applies nothing"). Orchestrator `SessionDB.build_engineering_campaign_programme` (READ-ONLY; reuses Phase-17 `build_experiment_portfolio` ONCE [feeding it the projected outcome history so Phase-17 retirement sees prior confirmed/regressed] + one development-record read + one calibration read; no N+1; writes nothing) + `_campaign_outcome_history` (projects dev records). UI: pure `ui/engineering_campaign_vm.py` + `ui/engineering_campaign_panel.py` (`EngineeringCampaignPanel`, NO Apply/Approve/Revert/freeze/edit/create controls — asserted) embedded in Development History; build OFF the Qt thread via the reused `MechanismAnnotationWorker(QThread)`; frozen Apply gate + existing lifecycle remain the only execution routes. **Golden `config_id` + frozen allowlist + Apply-gate + engine-wiring-status UNCHANGED.** Tests: `tests/test_phase18_*` (41) — domain (identity/scoping/grouping/objective/stages/status/progress/criteria/validation/freeze/completed/blocked/stale/retirement/incompatible-context/determinism/rendering), golden UAT A–D + real production path + restart, safety (no mutation/apply/freeze/execution/AI, no duplicate ranking/lifecycle, never-complete-unvalidated, versions), query-shape (single portfolio build, constant query count no N+1, cheap empty, renderer-no-DB), UI construction + off-thread + no controls. Phase 12–17 non-UI (451) + setup-experiment/outcome/preflight/postflight/reconciliation (207) + broad non-UI strategy regression (2409) green; every UI construction module passes per-file (12→18). Doc: `docs/ENGINEERING_BRAIN_PHASE18_ENGINEERING_CAMPAIGNS.md`. Committed locally; NOT pushed / no PR / not merged. Next: Phase 19 — campaign persistence, evidence-saturation & cost-of-knowledge.)
>
> Prior update: 2026-07-19 (**Engineering Brain PROGRAM 2, Phase 17 — Experiment Portfolio Optimisation & Information-Gain Selection.** Branch `eng-brain-phase17-experiment-portfolio-optimisation` from `3cc36e8` (Phase-16 tip). The deterministic engineering PLANNER (sits immediately before Phase 15) that decides "which experiment next?" — optimising for ENGINEERING VALUE (information gain FIRST), NOT lap time. CONSUMES existing authorities (Phase-15 bounded experiments + embedded Phase-14 hypotheses + outcome history + prediction calibration + working-window protection + confirmed-good), replaces none. NEVER mutates setup/experiment/outcome/reconciliation/calibration, applies nothing, writes nothing, duplicates no lifecycle/scoring. No ML/stats/NLP/AI. **NO migration — `DB_VERSION` stays 25** (regenerable; restart-identical fingerprints), `RULE_ENGINE_VERSION` 46.0. NEW pure `strategy/experiment_portfolio.py`: 13 INDIVIDUALLY-VISIBLE value dimensions (no black box — `DIMENSION_WEIGHTS` exposed, information_gain weight 3.0 = highest; mechanism_discrimination/attribution_quality/reversibility/protection_of_confirmed_good/low_masking_risk/low_interaction_complexity/low_driver_workload/session_suitability/remaining_uncertainty/proven_history_usefulness/prediction_calibration_benefit/future_engineering_value) → `ValueDimension` + `ExperimentValuation` (engineering_value = transparent weighted mean) + `EngineeringPortfolio`; `PortfolioRole` (HIGHEST_VALUE/ALTERNATIVE/DEFERRED/BLOCKED/REDUNDANT/OBSOLETE); `DependencyKind` (SUPERSEDES/MUTUALLY_EXCLUSIVE/UNNECESSARY_IF_FAILS/DEPENDS_ON) deterministic dependency graph; retirement (already-confirmed/already-rejected/superseded → OBSOLETE); `SessionSuitability` (unknown context lowers confidence, never invents); deterministic advisory roadmap (experiment→review→validate→freeze→race); `build_portfolio`. NEW pure `strategy/experiment_portfolio_render.py` (visible dimensions + roadmap; "information gain first, not lap time"; no Apply wording). Orchestrator `SessionDB.build_experiment_portfolio` (READ-ONLY; reuses Phase-15 `build_bounded_setup_experiments` ONCE + calibration; no per-candidate/N+1 query; writes nothing). UI: pure `ui/engineering_plan_vm.py` + `ui/engineering_plan_panel.py` (`EngineeringPlanPanel`, NO Apply/Approve/Revert/edit controls — asserted) embedded in Development History; build OFF the Qt thread via the reused `MechanismAnnotationWorker(QThread)`; frozen Apply gate remains the sole route to the car. **Golden `config_id` + frozen allowlist + Apply-gate + engine-wiring-status UNCHANGED.** Tests: `tests/test_phase17_*` (37) — portfolio generation/ranking/info-gain/visible-dimensions/retirement/redundancy/dependencies/roadmap/ties/session-awareness/determinism/rendering, golden real-DB production path + restart, safety (no mutation/apply/writes/AI, no hidden optimisation, no duplicate scoring/lifecycle, learning-not-lap-time), query-shape (single-aggregate reuse, no N+1), UI construction + off-thread + no controls. Phase 12–16 non-UI (352) + frozen/no-AI/config/fan-out/session_db + broad non-UI strategy regression (2384) green; every UI construction module passes per-file (12→17). Doc: `docs/ENGINEERING_BRAIN_PHASE17_EXPERIMENT_PORTFOLIO_OPTIMISATION.md`. Committed locally; NOT pushed / no PR / not merged. Next: Phase 18 — cross-session development campaign & convergence detection.)
>
> Prior update: 2026-07-19 (**Engineering Brain PROGRAM 2, Phase 16 — Guarded Experiment Lifecycle & Postflight Loop Closure.** Branch `eng-brain-phase16-guarded-experiment-lifecycle` from `1f90367` (Phase-15 tip). The deterministic READ-ONLY orchestration that CLOSES the Engineering-Brain loop by CONNECTING existing authorities — it creates NO new experiment system / Apply path / outcome recorder / reconciler. Converts a READY Phase-15 bounded experiment into a canonical `SetupExperiment` request (via the existing `build_experiment_from_recommendation`), routes it through the existing Phase-10 preflight, and — for an executed experiment — assembles a read-only closed-loop summary from the existing Phase-3 outcome + Phase-11 reconciliation + prediction calibration. NEVER applies / bypasses the frozen Apply gate / persists-duplicates an experiment / creates an outcome or reconciliation / invents feedback / simulates results / mutates diagnosis-mechanism-hypothesis-setup-history-active-setup-calibration. No ML/stats/NLP/AI. **NO migration — `DB_VERSION` stays 25** (summary is a pure function of existing records; restart-identical fingerprints), `RULE_ENGINE_VERSION` 46.0. NEW pure `strategy/experiment_lifecycle.py`: `ExperimentLifecycleState` (16: NOT_ACTIONABLE/EXPERIMENT_BUILT/PREFLIGHT_FAILED/READY_FOR_MANUAL_APPLY/AWAITING_APPLY/APPLIED/TEST_IN_PROGRESS/READY_FOR_REVIEW/OUTCOME_RECORDED/RECONCILED/CALIBRATED/COMPLETED/REJECTED/REVERTED/BLOCKED/UNKNOWN) + `LifecycleTrace` (unbroken provenance chain diagnosis→mechanism→hypothesis→synthesis→experiment→outcome→reconciliation→prediction, `is_unbroken_to`) + `ExperimentExecutionRequest`/`ExperimentExecutionResult`/`ExperimentLifecycleSummary`; `build_execution_request` (maps READY Phase-15 candidate → canonical SetupExperiment status=draft via build_experiment_from_recommendation, recommendation_status="approved"), `assemble_execution_result` (preflight ok→READY_FOR_MANUAL_APPLY, fail→PREFLIGHT_FAILED), `assemble_lifecycle_summary` (from existing experiment+outcome+reconciliation+calibration). NEW pure `strategy/experiment_lifecycle_render.py` (ordered loop-stage rows; no Apply-control wording). Orchestrator `SessionDB.build_experiment_execution` (one candidate → EXISTING Phase-10 preflight; read-only, writes nothing) + `SessionDB.build_engineering_lifecycle` (aggregate; reuses Phase-15 `build_bounded_setup_experiments` ONCE + calibration + reconciliation records — no per-diagnosis/N+1 scan). UI: pure `ui/engineering_lifecycle_vm.py` + `ui/engineering_lifecycle_panel.py` (`EngineeringLifecyclePanel`, NO Apply/Approve/Revert/edit controls — asserted) embedded in Development History; build OFF the Qt thread via the reused `MechanismAnnotationWorker(QThread)`; the frozen Apply gate remains the sole mutation route. **Golden `config_id` + frozen allowlist + Apply-gate + engine-wiring-status UNCHANGED.** Tests: `tests/test_phase16_*` (35) — lifecycle creation, traceability, preflight/apply/outcome/reconciliation/prediction routing, golden real-DB production path (aggregate + single-candidate execution through the real Phase-10 preflight) + restart, safety (no duplicate lifecycle/apply/outcome/reconciliation/calibration, connects-existing-only, no shadow model, read-only, versions), query-shape (single-aggregate reuse, no N+1), UI construction + off-thread + no controls. Phase 12–15 (341) + frozen/no-AI/config/fan-out/session_db (80) + experiment/outcome/preflight/postflight/reconciliation (285) + broad non-UI strategy regression (2000) green. Doc: `docs/ENGINEERING_BRAIN_PHASE16_GUARDED_EXPERIMENT_LIFECYCLE.md`. Committed locally; NOT pushed / no PR / not merged. **The Engineering Brain loop is now closed end-to-end: evidence→diagnosis→mechanism→hypothesis→bounded experiment→canonical experiment→preflight→manual Apply→outcome→reconciliation→calibration.** Next: Phase 17 — autonomous development cadence & regression guard (deterministic one-at-a-time next-experiment queue across diagnoses, still through every gate + manual Apply).)
>
> Prior update: 2026-07-19 (**Engineering Brain PROGRAM 2, Phase 15 — Minimum-Effective Experiment Synthesis Handoff.** Branch `eng-brain-phase15-minimum-effective-experiment-synthesis` from `8e48beb` (Phase-14 tip). The deterministic READ-ONLY handoff from a valid, testable Phase-14 intervention hypothesis into the existing setup-synthesis/setup-experiment authorities: the output is a BOUNDED setup-experiment candidate — the SMALLEST legal, reversible numeric setup step that tests the hypothesis without disturbing confirmed-good behaviour. Answers "what is the smallest legal reversible numeric experiment that tests this?", never "what is the final ideal setup?". NEVER auto-applies / bypasses the Apply gate / invents limits / builds a second synthesiser / optimises the whole car / silently changes coupled fields / mutates diagnosis-mechanism-outcome-calibration-setup-history-active-setup / persists an experiment. No ML/stats/NLP/AI/network. **NO migration — `DB_VERSION` stays 25** (regenerable; restart-identical fingerprints), `RULE_ENGINE_VERSION` 46.0. CONSUMES canonical authorities: baseline = `setup_state_authority.evaluate_analysis_gate` + `ActiveSetup` + `applied_checkpoint.compute_setup_hash`; step/quantiser = `experiment_selection.legal_step` + `setup_synthesis._round`; bounds = `setup_ranges.resolve_ranges`; final-drive invariant = `gearbox_evidence` (lower ratio = LONGER); lockouts = `working_window.locked_directions()`; direction = the Phase-14 hypothesis (never the field name). NEW pure `strategy/experiment_synthesis.py`: `ExperimentSynthesisStatus` (12: READY_FOR_PREFLIGHT/CONDITIONAL/NO_ELIGIBLE_HYPOTHESIS/INSUFFICIENT_EVIDENCE/BLOCKED_BY_{WORKING_WINDOW,PRIOR_REGRESSION,LEGALITY,INTERACTION_RISK,BASELINE_STATE}/REQUIRES_COUPLED_EXPERIMENT/NOT_EVALUABLE/OUT_OF_SCOPE) + `BaselineSetupReference`/`ParameterExperimentDelta`/`BoundedSetupExperiment`/`ExperimentSynthesisResult` (source hypothesis set retained UNCHANGED + content_fingerprint); `build_baseline_reference`, `synthesize_bounded_experiments`, `synthesize_from_report`; minimum-effective one-step (`_round(field, baseline ± legal_step)`, range-checked, no clamp), justified larger step bounded to 2 from a fixed allowed set; single-field default, coupled only when Phase-14 marked paired (≤2 fields, roles, REQUIRES_COUPLED_EXPERIMENT). NEW pure `strategy/experiment_synthesis_render.py` (numeric baseline vs candidate WITH provenance; no editable value / Apply / "optimal" / "final tune"). DOCTRINE: baseline defaults to the canonical applied setup, blocks on missing/incomplete/mismatch/stale/drift (no fallback); wheelspin never auto-LSD; failed direction blocked; prior single-field regression → BLOCKED_BY_PRIOR_REGRESSION (physics kept); competing → CONDITIONAL discriminating test only (never ready/auto-winner); unknown/conflicting gearbox → no gearing; count-only bottoming → no platform; aero low-speed → never ready; ballast only from eligible ballast hypothesis; ties stay ties (manual choice). Orchestrator `SessionDB.build_bounded_setup_experiments` (READ-ONLY; reuses `build_intervention_hypotheses` ONCE + `resolve_ranges(car)` once — zero per-hypothesis/N+1 queries; applied setup supplied by the caller's `ActiveSetupAuthority`; writes nothing). UI: pure `ui/experiment_synthesis_vm.py` + `ui/experiment_synthesis_panel.py` (`ExperimentSynthesisPanel`, numeric values shown, NO editable/Apply/Approve/Revert controls — asserted) embedded in Development History; build OFF the Qt thread via the reused `MechanismAnnotationWorker(QThread)`; the frozen Apply gate remains the only mutation route. **Golden `config_id` + frozen allowlist + Apply-gate + engine-wiring-status UNCHANGED.** Tests: `tests/test_phase15_*` (103) — domain, golden UAT A–R (incl. real SessionDB production path + restart), 64 property/metamorphic invariants, safety (no-AI/no-Qt-in-domain/no-DB-in-domain/no-shadow-authority/read-only), query-shape (single-aggregate reuse, no N+1, cheap empty, renderer-no-DB), UI construction + off-thread. Phase 12/13/14 (238) + frozen/no-AI/config/fan-out/session_db (80) + setup-state/selection/ranges (143) + broad non-UI strategy regression (1531) green. Doc: `docs/ENGINEERING_BRAIN_PHASE15_MINIMUM_EFFECTIVE_EXPERIMENT_SYNTHESIS.md`. Committed locally; NOT pushed / no PR / not merged. Next: Phase 16 — guarded experiment lifecycle & postflight loop closure (convert a READY candidate into the canonical SetupExperiment via the existing workflow → Phase-10 preflight → frozen Apply gate → Phase-3 outcome → Phase-11 reconciliation).)
>
> Prior update: 2026-07-19 (**Engineering Brain PROGRAM 2, Phase 14 — Mechanism-Constrained Intervention Hypotheses.** Branch `eng-brain-phase14-mechanism-constrained-interventions` from `f3d4e90` (Phase-13 tip). A deterministic, READ-ONLY reasoning layer that converts a valid Phase-13 `MechanismAnnotatedDiagnosis` into structured intervention HYPOTHESES — scientifically-defensible controlled-test DIRECTIONS constrained by the supported physical mechanism. A hypothesis is NOT a setup recommendation and NOT an authored setup change; answers "what controlled directions are defensible to test next?", never "set this value to X". NEVER authors a numeric setup value / applies / approves / persists; mutates no diagnosis / outcome / working-window / calibration / setup-history / active-setup; duplicates neither Phase-12 knowledge, the Phase-13 mechanism model, nor the Program-1 sign graph (consumes all three). No ML/stats/NLP/black-box/AI/network. **NO migration / NO persistence — `DB_VERSION` stays 25** (regenerable from the Phase-13 annotation aggregate; restart-identical fingerprints), `RULE_ENGINE_VERSION` 46.0. NEW pure `strategy/intervention_hypothesis.py`: `InterventionHypothesisStatus` (TESTABLE/CONDITIONAL/INSUFFICIENT_EVIDENCE/COMPETING_MECHANISMS/CONTRADICTED_BY_OUTCOME/BLOCKED_BY_WORKING_WINDOW/BLOCKED_BY_SAFETY_OR_VALIDITY/NOT_EVALUABLE/OUT_OF_SCOPE) + `InterventionDirection` (qualitative: stiffen/soften/raise/lower/move_forward/rearward/shorten/lengthen/increase_locking/…/no_defensible_direction) + `InterventionTestKind` (single_field/paired_coupled/multi_field/preserve_and_observe/evidence_collection) + `InterventionTarget`/`ExpectedResponse`/`ControlledTestDesign`/`InterventionHypothesis`/`InterventionHypothesisSet` (source annotation retained UNCHANGED + content_fingerprint); direction resolved from the CANONICAL sign authority (`vehicle_dynamics.explain_component(...).axis_effects` vs a per-issue goal axis) — NEVER from the parameter name — with gearing via `gearbox_evidence` state + final-drive invariant (lower ratio = LONGER); `build_intervention_hypotheses`/`hypotheses_from_report`. NEW pure `strategy/intervention_hypothesis_render.py` (12 sections; no numeric values / Apply / approval / false certainty). DOCTRINE: wheelspin NEVER auto-increases LSD locking (LSD stays competing/blocked); failed direction → BLOCKED_BY_WORKING_WINDOW (mechanism retained); prior single-field regression → CONTRADICTED_BY_OUTCOME (physics kept); confirmed outcome never proves a mechanism; coupled tests only from a prior coupled-improvement, capped at 2 fields crediting the SET; aero needs speed context; count-only bottoming → insufficient; unknown/conflicting gearbox → no direction; driver preference never overrides evidence/lockout; minimum-effective single-field ordering. Orchestrator `SessionDB.build_intervention_hypotheses` (READ-ONLY; reuses `build_mechanism_annotations` ONCE — zero per-hypothesis/N+1 queries; writes nothing). UI: pure `ui/intervention_hypothesis_vm.py` + `ui/intervention_hypothesis_panel.py` (`InterventionHypothesisPanel`, structured cards, NO Apply/Approve/Revert — asserted) embedded in Development History page; build OFF the Qt thread via the reused `MechanismAnnotationWorker(QThread)`. **Golden `config_id` + frozen allowlist + Apply-gate + engine-wiring-status UNCHANGED.** Tests: `tests/test_phase14_*` (84) — domain, golden UAT A–N (incl. real SessionDB production path + restart), 40 property/metamorphic invariants, safety (no-AI/no-Qt-in-domain/no-DB-in-domain/no-shadow-authority/read-only), query-shape (single-aggregate reuse, no N+1, cheap empty, renderer-no-DB), UI construction + off-thread. Phase 12/13 (154) + frozen/no-AI/config/fan-out/session_db (80) + setup-synthesis/Program-1 (254) + broad non-UI strategy regression (1410) green. Doc: `docs/ENGINEERING_BRAIN_PHASE14_MECHANISM_CONSTRAINED_INTERVENTIONS.md`. Committed locally; NOT pushed / no PR / not merged. Next: Phase 15 — minimum-effective experiment synthesis handoff (hand the top defensible hypothesis to the existing setup-synthesis authority to propose the smallest legal numeric step, still evidence-gated + manual Apply).)
>
> Prior update: 2026-07-19 (**Engineering Brain PROGRAM 2, Phase 13 — Mechanism-Annotated Diagnosis.** Branch `eng-brain-phase13-mechanism-annotated-diagnosis` from `6010d5b` (Phase-12 tip). The strict bridge between Program 1 ("what happened?") and Program 2 / Phase 12 ("why could it have happened?"): for an already-decided canonical Program-1 diagnosis it produces an auditable, evidence-linked explanation of the vehicle-dynamics MECHANISMS behind it, by querying the Phase-12 knowledge authority, and preserves the canonical diagnosis UNCHANGED. NEVER decides whether an observation occurred / a lap is valid / an issue recurs / an experiment improved the car / a change is safe / which experiment is selected; authors no setup value/delta/Apply/Revert; mutates no outcome/working-window/lockout/prediction-calibration; duplicates neither the Phase-12 knowledge nor the Program-1 sign graph (consumes both). No ML/statistics/NLP/black-box/AI. **NO migration / NO persistence — `DB_VERSION` stays 25** (annotations regenerate deterministically from immutable Phase-8 records + Phase-11 reconciliation records + static Phase-12 knowledge; restart-identical `content_fingerprint`), `RULE_ENGINE_VERSION` unchanged (46.0). NEW pure `strategy/mechanism_map.py`: fixed structural `MechanismTemplate` table keyed by canonical `issue_type`, pointing only at Phase-12 Component/HandlingPhase/TransferMode/interaction pairs (+role_hint/intervention_field/requires_speed_context/is_driver_technique); `resolve_handling_phase` maps Program-1 phase strings → Phase-12 `HandlingPhase`. NEW pure `strategy/mechanism_annotation.py`: `MechanismStatus` (SUPPORTED/SUPPORTED_WITH_LIMITATIONS/PLAUSIBLE/COMPETING/CONTRADICTED/INSUFFICIENT_EVIDENCE/NOT_EVALUABLE/OUT_OF_SCOPE/INVALID_SOURCE_DIAGNOSIS), `ConclusionKind`/`EvidenceGrade`/`EvidenceRelation`, `MechanismEvidenceLink`, `CausalMechanismCandidate`, `MechanismComparison`, immutable `MechanismAnnotatedDiagnosis` (source diagnosis retained unchanged + content_fingerprint + knowledge_versions incl. sign-graph fingerprint); `annotate_diagnosis` (eligibility gate → phase resolve → Phase-12 query → evidence support/contradiction → primary/secondary/competing/contradicted ranking → outcome + prediction reconcile), `annotations_from_memory` (folds cross-session `IssueMemory` → annotations). NEW pure `strategy/mechanism_annotation_render.py`: sectioned driver-readable renderer (observation vs interpretation kept separate; no setup values/Apply wording). DOCTRINE: wheelspin primary = driven-wheel traction demand (TRANSMISSION), NOT auto-LSD; prior failed LSD-accel direction flags the LSD candidate's intervention `contradicted` (kept as possible mechanism, never a cure); aero stays PLAUSIBLE without speed evidence; confirmed outcome never proves a mechanism; regression disproves the intervention direction not the physics; GT7-unavailable channels (tyre load / diff lock state / damper velocity / suspension travel / engine torque) declared, never fabricated/observed. Orchestrator `SessionDB.build_mechanism_annotations(**ctx)` (READ-ONLY; composes `build_cross_session_memory` + `build_prediction_calibration`; writes nothing; DB stays v25). UI: pure `ui/mechanism_annotation_vm.py` + `ui/mechanism_annotation_panel.py` (`MechanismAnnotationPanel`, structured section cards, NO Apply/Revert — asserted) EMBEDDED in the Development History page; heavy build runs OFF the Qt thread via `ui/mechanism_annotation_worker.py` (`MechanismAnnotationWorker(QThread)`, mirrors TrackModelBuildWorker) → immutable dict rendered on the UI thread. **Golden `config_id` + frozen allowlist + Apply-gate + engine-wiring-status UNCHANGED.** Tests: `tests/test_phase13_*` (103 cases / 118 w/ parametrisation) — mechanism map, domain (eligibility/support/contradiction/interactions/load-transfer/experiment+prediction/rendering), golden UAT A–L (incl. real SessionDB production path + restart determinism), 25 property/metamorphic invariants, safety (no-AI/no-Qt-in-domain/no-DB-in-domain/no-sign-dup/no-invented-channel), UI construction + off-thread. Phase 8–12 (370) + broad strategy regression (734) green. Doc: `docs/ENGINEERING_BRAIN_PHASE13_MECHANISM_ANNOTATED_DIAGNOSIS.md`. Committed locally; NOT pushed / no PR / not merged. Next: Phase 14 — mechanism-constrained intervention hypotheses (constrain which subsystems are physically eligible for a controlled experiment, still through all Program-1 gates + manual Apply).)
>
> Prior update: 2026-07-19 (**Engineering Brain PROGRAM 2, Phase 12 — Deterministic Vehicle Dynamics Knowledge Engine.** Branch `eng-brain-phase12-vehicle-dynamics` from `master` @ Phase 11 `0923f5c`. Begins Program 2 — a NEW read-only EXPLANATORY authority (Vehicle Dynamics Knowledge) that explains the PHYSICAL MECHANISM behind each setup element ("what physical mechanism is creating this behaviour?"), ADDITIONAL to Program 1 (Phases 1-11), not a replacement. NEVER creates experiments / ranks candidates / overrides evidence / modifies outcomes-memory-working-windows / authors setup. No ML, no statistics, no NLP, no black-box scoring — deterministic curated knowledge. **NO migration / NO persistence — `DB_VERSION` stays 25**, `RULE_ENGINE_VERSION` 46.0 (static code, restart-identical fingerprints). CONSUMES Program-1 `setup_synthesis.PARAMETER_INTERACTIONS` as the SINGLE directional-sign source (never duplicates/contradicts) + layers mechanism + GT7 knowledge. NEW pure `strategy/vehicle_dynamics.py`: `Component` (25 tunable elements) + `ComponentGroup` (suspension/differential/aero/tyres/brakes/transmission/weight_transfer/alignment) + `EngineeringExplanation` (primary_mechanism/secondary_interactions/gt7_limitations/raise_effect/lower_effect/axis_effects-from-graph); `explain_component`, `explain_change` (direction flips axis signs), `build_knowledge_report`, `build_engineering_knowledge` (combined). NEW pure `strategy/load_transfer.py`: `TransferMode` (longitudinal/lateral/combined/pitch/roll/yaw/platform) + `LoadTransferRelation` (mechanism/increased-by/decreased-by/balance-effect/gt7-note). NEW pure `strategy/handling_balance.py`: `HandlingPhase` (corner_entry/trail_braking/initial_rotation/mid_corner/exit_traction/power_on_rotation/straight_line_stability/high_speed_stability) + `PhaseExplanation` (dominant mechanism + key components + load-transfer modes + understeer/oversteer-if); composes the component + load-transfer knowledge. NEW pure `strategy/setup_interactions.py`: `ComponentInteraction` (spring↔damper, damper↔ARB, ride-height↔aero, camber↔tyre, toe↔stability, diff↔suspension) + `InteractionType` (reinforcing/opposing/enabling/limiting) + detailed LSD model (initial/accel/decel) + aero model (front/rear balance, ride-height sensitivity, platform dependence, high-speed behaviour). GT7-specific behaviour modelled separately per component/mode/phase/interaction (bottoming, LSD response, tyre wear, aero ride-height gating, platform sensitivity). Every explanation identifies primary mechanism + secondary interactions + known GT7 limitations. UI: pure `ui/engineering_knowledge_vm.py` + `ui/engineering_knowledge_panel.py` (`EngineeringKnowledgePanel` grouped by Suspension/Differential/Aero/Tyres/Brakes/Transmission/Weight-transfer, 13 tables, NO Apply controls — asserted); EMBEDDED in the existing Development History page (static reference, no DB call, no new tab). **Golden `config_id` + frozen allowlist + Apply-gate + engine-wiring-status UNCHANGED.** Tests: `tests/test_phase12_*` (36 non-UI + 3 UI individual) incl. property/metamorphic/consistency-with-Program-1/determinism/golden/safety. Doc: `docs/ENGINEERING_BRAIN_PHASE12_VEHICLE_DYNAMICS.md`. Next: Phase 13 — mechanism-annotated diagnosis (attach the vehicle-dynamics mechanism to a Program-1 residual issue / proposed change).)
>
> Prior update: 2026-07-19 (**Engineering Brain Phase 11 — Post-Flight Engineering Reconciliation & Prediction Calibration.** Branch `eng-brain-phase11-postflight-reconciliation` from `master` @ Phase 10 `fa9d1f4`. A READ-ONLY OBSERVER ABOVE Phases 1-10 — after a completed experiment, deterministically compares what the Brain PREDICTED (Phase-10 pre-flight) vs what ACTUALLY occurred (Phase-3 outcome + Phase-6 residuals): "how accurate was our engineering expectation?". NEVER changes experiments / outcomes / memory / working windows / setup values — only compares expectation with reality. No AI, no prediction, no learning, no statistics. **SCHEMA: `user_version` 24 → 25** (`DB_VERSION = 25`; `_migrate_v25` + `_DDL_V25` add ONE additive, append-only, IMMUTABLE table `engineering_reconciliation_records` — CREATE IF NOT EXISTS, touches no existing table); `RULE_ENGINE_VERSION` unchanged (46.0). Migration justified (unlike Phases 9/10's regenerable no-migration): the prediction is a point-in-time input made BEFORE the experiment and is NOT reliably regenerable after the outcome changes memory, so the immutable calibration log persists (INSERT OR IGNORE, never UPDATE/DELETE, idempotent record_key). Reuses (not duplicates): Phase 10 pre-flight review, Phase 3 outcome, Phase 6 `residual_issues_from_outcome`, Phase 8 `MemoryContextKey`. NEW pure `strategy/postflight_reconciliation.py`: `reconcile_consequences` → CONFIRMED/PARTIALLY_CONFIRMED/NOT_OBSERVED/CONTRADICTED/INSUFFICIENT_EVIDENCE/UNKNOWN per predicted consequence (primary ← target-issue residual state; side-effect ← matching regression family via keyword map; historical ← history repeated; working-window ← window held; interaction ← coupled family observed); `ReconciliationRecord` + `build_reconciliation_record` (idempotent record_key, time-independent content_fingerprint). NEW pure `strategy/preflight_validation.py`: `validate_checklist` evaluates every checklist item (did the risk appear, did protected stay protected, did the interaction occur, did the regression happen, was confidence appropriate) → MATERIALISED/DID_NOT_MATERIALISE/INSUFFICIENT/N-A + `useful`. NEW pure `strategy/prediction_accuracy.py`: `compute_accuracy` → primary-consequence / side-effect / risk / constraint / historical-transfer / checklist accuracies + overall + confirmed/contradicted counts (plain deterministic ratios). Orchestrator `SessionDB.record_experiment_reconciliation` (append-only, idempotent, writes only its own log) + `get_reconciliation_records` + `build_prediction_calibration` (deterministic regenerable calibration summary — mean accuracies + confirmed/contradicted + elevated-risk regressions). UI: pure `ui/postflight_review_vm.py` + `ui/postflight_review_panel.py` (`PostFlightReviewPanel` — prediction vs observed, confirmed expectations, unexpected behaviour, engineering accuracy, checklist validation, lessons observed, calibration summary; NO Apply controls — asserted); EMBEDDED in the existing Development History page (aggregate calibration; NO new tab). **Golden `config_id` + frozen allowlist + Apply-gate + engine-wiring-status UNCHANGED.** Tests: `tests/test_phase11_*` (36 non-UI + 3 UI individual) incl. golden UAT through the real review_and_learn loop then reconcile. Doc: `docs/ENGINEERING_BRAIN_PHASE11_POSTFLIGHT_RECONCILIATION.md`. Next: Phase 12 — calibration-informed confidence (fold accumulated calibration into the Phase-10 pre-flight confidence labelling).)
>
> Prior update: 2026-07-19 (**Engineering Brain Phase 10 — Engineering Experiment Pre-Flight Review.** Branch `eng-brain-phase10-preflight-review` from `master` @ Phase 9 `b979be0`. A READ-ONLY OBSERVER ABOVE Phases 1-9 — before the selected experiment is shown to the driver, performs a deterministic engineering pre-flight review of the EXACT Phase-5 selection ("what engineering consequences should the driver know before trying this?"). NEVER creates experiments / changes priorities-ranking / changes setup values / blocks recommendations / changes working windows / mutates evidence-memory-outcomes. No AI, no prediction, no statistical inference. **NO migration — `DB_VERSION` stays 24**, `RULE_ENGINE_VERSION` 46.0 (the review is a deterministic regenerable function of the Phase-5 candidate + Phase-9 context + Phase-8 memory + the canonical interaction graph; restart reproduces identical fingerprints). Reuses (not duplicates): Phase 5 `CandidateExperiment` (echoed verbatim; its `expected_positive_effect`/`expected_negative_effects` consumed as-is — no re-derived physics), Phase 9 `build_engineering_context` (transfers/constraints/regression-risks for the proposed change), Phase 8 `build_cross_session_memory` (outstanding issues, familiarity, state), `setup_synthesis.PARAMETER_INTERACTIONS` (coupled fields). NEW pure `strategy/change_consequences.py`: `derive_consequences` → PRIMARY_EFFECT / SIDE_EFFECT / HISTORICAL (Phase-9 transfers w/ sessions + confirmed/provisional) / WORKING_WINDOW / INTERACTION; `coupled_fields` from shared handling axes. NEW pure `strategy/engineering_checklist.py`: `build_checklist` → ✓/⚠/? items (inside learned window, protected-behaviour conflict, similar succeeded/failed, only-one-supporting-session, regression risks, coupled interaction, outstanding residuals) each w/ why+supporting sessions+confidence+context; `RiskLevel` LOW/MODERATE/HIGH/UNKNOWN (descriptive only, NEVER changes the recommendation). NEW pure `strategy/preflight_review.py`: `build_preflight_review` echoes the EXACT selection verbatim + 12 fixed sections (evidence quality, working-window confidence, protected behaviour impact, historical success, historical failure, regression risk, known constraints, interaction risks, coupled fields, driver familiarity, outstanding residual issues, current engineering state) + consequences + checklist + risk + time-independent fingerprint (a section is emitted only when it has content). Orchestrator `SessionDB.build_experiment_preflight` (read-only, NO persistence, writes nothing, NEVER blocks). UI: pure `ui/preflight_review_vm.py` + `ui/preflight_review_panel.py` (`PreFlightReviewPanel` — risk banner, proposed experiment, engineering rationale, checklist, expected consequences, known risks, historical outcomes, protected behaviours, constraint summary; NO Apply, NO approval controls — asserted); a compact pre-flight summary surfaced beside the proposed experiment in the Setup Builder outcome flow. NO new tab/migration. **Golden `config_id` + frozen allowlist + Apply-gate + engine-wiring-status UNCHANGED.** Tests: `tests/test_phase10_*` (40 non-UI + 3 UI individual) incl. golden UAT through the real review_and_learn loop. Doc: `docs/ENGINEERING_BRAIN_PHASE10_PREFLIGHT_REVIEW.md`. Next: Phase 11 — post-flight reconciliation (compare pre-flight prediction vs Phase-3 actual outcome, fold into memory as calibration).)
>
> Prior update: 2026-07-19 (**Engineering Brain Phase 9 — Cross-Context Engineering Transfer & Regression Risk Intelligence.** Branch `eng-brain-phase9-context-transfer` from `master` @ Phase 8 `da53569`. A READ-ONLY OBSERVER ABOVE Phases 1-8 — before an experiment is proposed it surfaces every relevant lesson from COMPATIBLE historical contexts ("what already happened in similar situations?"). REPORTS ONLY: evaluates no evidence, creates/chooses no experiment, modifies no working window, mutates nothing, and NEVER BLOCKS (authority stays with Phases 3/5/6). No AI, no prediction, no probability, no natural-language reasoning. **NO migration — `DB_VERSION` stays 24**, `RULE_ENGINE_VERSION` 46.0 (transfers/constraints/risks are deterministic regenerable FOLDS over the immutable Phase-8 `engineering_development_records`; restart reproduces identical per-artifact fingerprints). Reuses (not duplicates): Phase 8 records + `build_history`/`build_engineering_memory`, Phase 8 `ConstraintKind` + per-record protected-knowledge, existing `cars.category` for the RELATED tier. NEW pure `strategy/context_transfer.py`: `classify_context_match` (FIXED 5-tier hierarchy DIRECT [same driver/car/track/layout/discipline/gt7] / STRONG [same driver+car, diff track] / RELATED [same driver+track, similar car class] / WEAK [same vehicle, diff discipline] / UNKNOWN [general]; incompatible contexts EXCLUDED not mixed; every match states WHY + which sessions/experiments produced it; RELATED never fires without real class data); `EngineeringTransfer` (SUCCESSFUL_EXPERIMENT/FAILED_EXPERIMENT/STABLE_WINDOW/PROTECTED_BEHAVIOUR/KNOWN_UNSTABLE/INEFFECTIVE_DIRECTION, ranked strongest-match-first then confirmed-first, confirmed=high-conf+≥2 sessions); folds per-context memory via the Phase-8 authorities. NEW pure `strategy/engineering_constraints.py`: `derive_constraints` folds per-record protected-knowledge + protected-behaviours from all compatible contexts → `EngineeringConstraint` (kind/field/direction/value/evidence-source/supporting sessions+experiments/times-reinforced/confidence/confirmed; confirmed = high-conf + ≥2 sessions + ≥STRONG match). NEW pure `strategy/regression_risk.py`: `assess_regression_risk` (NEVER blocks) → KNOWN_FAILED_DIRECTION/PREVIOUSLY_UNSTABLE_RANGE/PROTECTED_FIELD_CONFLICT/WORKING_WINDOW_EDGE/REPEATED_REGRESSION/CONFIDENCE_WEAKNESS w/ HIGH/MED/LOW/INFO severity; works with or without a proposed change. Orchestrator `SessionDB.build_engineering_context` (read-only, NO persistence, writes nothing; candidate pool = car OR track OR driver; car classes from `cars.category`) + `get_development_records_for_context_search` + `_car_class_map`. UI: pure `ui/engineering_context_vm.py` + `ui/engineering_context_panel.py` (`EngineeringContextPanel` — relevant past contexts, regression risks, engineering constraints, known successful fixes, known failures, stable working windows, protected behaviours; NO Apply, NO decision controls — asserted); EMBEDDED in the existing Development History page (NO new tab, NO registry change). **Golden `config_id` + frozen allowlist + Apply-gate + engine-wiring-status UNCHANGED.** Tests: `tests/test_phase9_*` (36 non-UI + 3 UI individual) incl. golden UAT through the real review_and_learn loop (Porsche RSR @ Fuji). Doc: `docs/ENGINEERING_BRAIN_PHASE9_CONTEXT_TRANSFER.md`. Next: Phase 10 — proactive experiment pre-flight (attach the Phase-9 advisory to a specific Phase-5 proposed candidate inline).)
>
> Prior update: 2026-07-19 (**Engineering Brain Phase 8 — Cross-Session Engineering Development Memory & Driver Progress Intelligence.** Branch `eng-brain-phase8-development-memory` from `master` @ Phase 7 `dfc70a9`. The permanent engineering memory ABOVE Phases 1-7 — answers "what have we learned over every previous session?" NOT "what happened today?". DECIDES NOTHING: no experiment selection, no setup authoring, no lap evaluation, no evidence mutation, no history rewriting. NO AI, no network, no prediction, no text interpretation. **SCHEMA: `user_version` 23 → 24** (`DB_VERSION = 24`; `_migrate_v24` + `_DDL_V24` add ONE additive, append-only, IMMUTABLE table `engineering_development_records` — CREATE IF NOT EXISTS, touches no existing table); `RULE_ENGINE_VERSION` unchanged (46.0). Migration justified (unlike Phases 4/6/7's regenerable no-migration): the memory context key needs driver/gt7_version/tyre_compound, only fully known at review time, so the immutable record is captured WITH its full context; long-term memory/history/metrics/scorecard/comparison are deterministic FOLDS over the stored record_json (regenerable → a restart reproduces identical fingerprints; immutability enforced via INSERT OR IGNORE, never UPDATE/DELETE, idempotent record_key). Reuses (not duplicates): Phase 3 `setup_experiment_outcomes` (re-projected, never re-evaluated), Phase 6 `residual_issues_from_outcome`/`EngineeringIssueIdentity`, Phase 5 `list_working_windows`, Phase 3 failed-directions, Phase 1 scope. NEW pure `strategy/development_history.py`: `MemoryContextKey` (driver/car/track/layout/discipline/gt7/compound — a known value and unknown value are DIFFERENT scopes, incompatible contexts never merge); `DevelopmentRecord` (idempotent `record_key`, time-independent `content_fingerprint`; captures changes/residual-states/confirmed-improvements/new-regressions/protected-behaviours/working-window-snapshot + derived protected-knowledge `ConstraintKind` NEVER_MOVE_DIRECTION/NEVER_BELOW/NEVER_ABOVE/PREFERRED_RANGE/KNOWN_UNSTABLE/PROTECTED_BEHAVIOUR); `build_history` (chronological, dedup by record_key), `build_timeline` (session→experiment→improvement/regression/resolution/protected). NEW pure `strategy/engineering_memory.py`: `IssueMemory` (recurrence, sessions/dates, times resolved/regressed, current resolution, successful/failed fix experiments), `WorkingWindowEvolution` (per-field snapshots + convergence), `ProtectedKnowledgeItem` (reinforced constraints), `EngineeringMemory` fold. NEW pure `strategy/progress_metrics.py`: `numeric_trend` (min-points + window + hysteresis → a single session can NEVER flip a long-term trend), `ProgressMetrics` (experiment success rate, issue resolution rate, recurring-issues-reduced, working-window convergence, brake/corner-entry/exit-traction/driver-consistency/engineering-confidence trends, development velocity, experiment efficiency), `EngineeringScorecard`+`ScorecardBand`, `SessionComparison`+`compare_latest_sessions`. Orchestrators `SessionDB.record_engineering_development` (append-only, idempotent) wired best-effort into `review_and_learn` so every completed review is captured automatically; `build_cross_session_memory`/`build_development_history`/`get_development_records` (read-only folds; observer writes nothing beyond its own log). UI: pure `ui/development_history_vm.py` + `ui/development_history_page.py` (`DevelopmentHistoryPage` — scorecard banner, long-term metrics, engineering timeline, resolved/remaining issues, protected behaviours + protected knowledge, experiment history, working-window evolution, session comparison; NO Apply/Save/Revert — asserted); NEW **"Development History" tab** (index 12) wired via `dashboard.py`+`tab_registry.py`(`TAB_DEVELOPMENT_HISTORY`)+`product_flow.py`(ROLE_WORKFLOW), 13 tabs. **Golden `config_id` + frozen allowlist + Apply-gate + engine-wiring-status UNCHANGED.** Tests: `tests/test_phase8_*` (50 non-UI + 3 UI individual) incl. golden UAT through the real review_and_learn loop (Porsche RSR @ Fuji, multi-session). Doc: `docs/ENGINEERING_BRAIN_PHASE8_DEVELOPMENT_MEMORY.md`. Next: Phase 9 — cross-context transfer & regression-risk foresight (surface permanent-memory constraints when a new experiment is proposed).)
>
> Prior update: 2026-07-19 (**Engineering Brain Phase 7 — Live Engineering State Monitor & Session Development Ledger.** Branch `eng-brain-phase7-live-state-monitor` from `master` @ Phase 6 `abfa14b`. A READ-ONLY OBSERVER over the Phase 1-6 spine answering "what is the car doing right now?" — NOT "what experiment next?". NO generative AI, no network, no auto-apply/revert, no setup authoring, no working-window mutation, no candidate reordering, no whole-app redesign, no new authority. **NO migration — `DB_VERSION` stays 23**, `RULE_ENGINE_VERSION` stays 46.0 (proof: the live state + development ledger are a deterministic regenerable function of persisted `corner_issue_occurrences` + the Phase-4 lap-validity authority; restart rebuild yields byte-identical fingerprints — no state persisted). Reuses (not duplicates): Phase 4 `corner_evidence.CornerObservationRecord`/`from_issue_occurrence_row`/`classify_recurrence` (the SINGLE recurrence authority via `RecurrenceThresholds`), Phase 4 `engineering_lap_validity.evaluate_session_laps` (comparable-lap window — the observer trusts it, never re-judges laps), Phase 6 `engineering_issue.EngineeringIssueIdentity` (display-text-free). NEW pure `strategy/state_transitions.py`: Trend (IMPROVING/UNCHANGED/WORSENING/FLUCTUATING/INSUFFICIENT_EVIDENCE) + IssueStatus (UNKNOWN/NEW/ACTIVE/RECOVERING/STABLE/RESOLVED/PROTECTED/DAMAGED); `detect_trend` window-fraction over VALID laps only with min-lap + jitter gates + a ≥2-lap support rule so ONE exceptional lap can NEVER flip a trend (IMPROVING needs ≥2 recent clear laps, WORSENING ≥2 recent affected); `next_status` documented recovery/regression/protected paths. NEW pure `strategy/live_engineering_state.py`: `LiveIssueState` (identity/status/trend/recurrence/confidence/present-now/first+last observed lap/last corner/affected-laps/consistency/severity), `ConsistencyMeasures` (engineering repeatability numbers — recurrence ratio + lap-to-lap repeatability, explicitly NOT driver ratings), `SessionHealth`/`SessionHealthBand` (NOMINAL/SETTLING/DEVELOPING/DEGRADING/UNKNOWN), `LiveEngineeringState` (time-independent fingerprint); `update_live_state` pure ORDER-INDEPENDENT fold — excluded (kerb/airborne/noise) events + non-comparable laps never count. NEW pure `strategy/session_development.py`: append-only immutable `SessionDevelopmentLedger` (positional `sequence_no`, never a timestamp; events never rewritten/deleted; `append_snapshot` returns a NEW ledger); `build_session_ledger` folds `(lap, state)` snapshots and is BYTE-EQUAL to incremental append (the append-only + determinism contract). Orchestrator `SessionDB.build_live_engineering_state(session_id, …)` — read-only, NO persistence, WRITES NOTHING (asserted), regenerates state + per-lap-prefix development timeline. UI: pure `ui/live_engineering_vm.py` (health rows, active/resolved/protected issue tables, per-lap `▇/·` trend sparkline, development timeline) + `ui/live_engineering_monitor.py` (`LiveEngineeringMonitor` widget — health banner + Session Health + Active/Resolved/Protected + append-only timeline; NO Apply/Save/Revert controls, asserted); `dashboard.py` untouched. **Golden `config_id` + frozen allowlist + Apply-gate + engine-wiring-status UNCHANGED.** Tests: `tests/test_phase7_{state_transitions,live_state,ledger,orchestrator,view_model}.py` (55) + `tests/test_phase7_ui_construction.py` (3, run individually) incl. single-lap-no-flip (both directions), append==rebuild, restart-determinism, order-invariance metamorphic, pit/out-lap exclusion, golden resolution timeline, observer-writes-nothing, no-Apply-control. Doc: `docs/ENGINEERING_BRAIN_PHASE7_LIVE_STATE_MONITOR.md`. Next: Phase 8 — Live in-session wiring (off-thread per-lap tick) + cross-session development history + protected-key derivation.)
>
> Prior update: 2026-07-18 (**Engineering Brain Phase 6 — Live Residual-Issue Detection & Multi-Symptom Experiment Planning.** Branch `eng-brain-phase6-residual-experiment-planning` from `master` @ Phase 5 `535aed9`. Residual detection + development-programme planning over the Phase 1-5 spine — NO generative AI, no auto-apply/revert, no live-coaching, no UI redesign. **NO migration — `DB_VERSION` stays 23**, `RULE_ENGINE_VERSION` stays 46.0 (proof: the plan is a deterministic function of the immutable Phase-3 outcome + Phase-5 working windows + scope; regenerates byte-identical across restart — no evidence/audit lost). Reuses (not duplicates): Phase 3 `SetupExperimentOutcome` corner/protected rows (re-classified, never re-evaluated), Phase 4 assembly/lap-validity/`resolve_setup_decision`, Phase 5 `select_next_experiment`/`generate_candidates` (one selector), `setup_synthesis.PARAMETER_INTERACTIONS`. NEW pure `strategy/engineering_issue.py`: `EngineeringIssueIdentity` (stable key excluding display text) + `ResidualState` (RESOLVED/IMPROVED_BUT_PRESENT/UNCHANGED/WORSENED/NEW/CONFIRMED_GOOD/GOOD_BEHAVIOUR_DAMAGED/INSUFFICIENT_EVIDENCE/INVALID_COMPARISON/AMBIGUOUS/NOT_OBSERVED/OUT_OF_SCOPE); RESOLVED needs improved verdict + test below authorable threshold + ≥3 samples + non-low confidence (missing window is never resolution); NEW needs weak baseline + authorable test recurrence. NEW pure `strategy/engineering_state.py`: `EngineeringStateSnapshot` (residual issues grouped + confirmed/damaged-good + decision state + time-independent fingerprint). NEW pure `strategy/experiment_planning.py`: `prioritise_issues` (hard exclusion → documented precedence new-regression>damaged-good>high-recurrence>persistent>drive-out/gearing>tyre-fuel>consistency>weak>evidence; stable tie-break), `detect_conflicts` (same-field-opposite/strong-interaction/protected-good), `cluster_issues` (rule-based, isolation-required), `build_development_plan` (AT MOST ONE immediate experiment + queued hypotheses w/ dependencies+blockers+promotion/cancellation, invalidation triggers, fingerprint). Non-setup issues (gearing/drive-out/tyre-fuel/technique) routed to their own review task, never a suspension/LSD experiment. Orchestrator `SessionDB.build_engineering_plan` (regenerable, no persistence); `review_and_learn` returns `engineering_plan={snapshot,plan}`; off-thread; UI renders engineering state + development plan (advisory). **Golden `config_id` + frozen allowlist + Apply-gate + engine-wiring-status UNCHANGED.** Tests: `tests/test_phase6_*` (70) incl. golden UAT A/B/F/G/J/L through the production loop. Doc: `docs/ENGINEERING_BRAIN_PHASE6_RESIDUAL_EXPERIMENT_PLANNING.md`. Next: Phase 7 — Standalone Live Engineering-State Monitoring & Session Development Ledger.)
>
> Prior update: 2026-07-18 (**Engineering Brain Phase 5 — Working-Window Learning, Successful-Direction Reinforcement & Minimum-Effective Experiment Selection.** Branch `eng-brain-phase5-working-window-learning` from `master` @ Phase 4 `52628af`. Learning + selection over the Phase 1-4 spine — NO generative AI, no opaque scoring, no physics, no auto-apply/revert, no multi-field shotgun. **SCHEMA: `user_version` 22 → 23** (`DB_VERSION = 23`); `RULE_ENGINE_VERSION` unchanged (46.0). Reuses (not duplicates): Phase 3 `SetupExperimentOutcome`/`OutcomeStatus` + `setup_experiment_failed_directions`; Phase 4 lap-validity/corner-evidence/decision authorities; `setup_ranges` bounds; `setup_synthesis.PARAMETER_INTERACTIONS`; `practice_pattern_analysis.RecurrenceThresholds`. NEW pure `strategy/working_window.py`: `LearnedWorkingWindow` = deterministic function of an append-only evidence ledger (idempotent, order-independent); `outcome_to_window_evidence` (confirmed→successful, regression→unsuccessful+direction-lockout, no_change→ineffective, confounded/insufficient→no learning); confidence never over-claims (1 exp→provisional, contradiction caps low). NEW pure `strategy/experiment_selection.py`: physics-informed candidate generation (symptom→handling axis→one legal step of one field via the interaction graph — never a generic value table), HARD dead-end gates (failed/ineffective direction, illegal/at-current value, disproved value, protected-field), 5-stage deterministic selection (subordinate to `resolve_setup_decision`; stable tie-break; honest no-selection), `build_test_protocol`. EXTENDED `strategy/corner_evidence.py`: `from_corner_slip_aggregate` + `unify_corner_observations` (dedup by stable identity; slip records lap-attribution-free so can't inflate recurrence; unlinked slip ineligible; audit). MIGRATED `practice_capture.resolve_clean_lap` → compatibility adapter over the ONE `engineering_lap_validity` authority (Practice + Perfect-Lap paths; behaviour preserved). Additive DB v23 (`_migrate_v23`): `setup_working_window_evidence` (append-only, UNIQUE(context,experiment,outcome)→idempotent) + `setup_working_windows` (materialised cache). Orchestrators `learn_from_experiment_outcome`/`select_next_experiment`/`review_and_learn`; off-thread 'Review Test Outcome' worker rewired to `review_and_learn` + renders learned window + selected next experiment + blocked alternatives + no-selection state. **Golden `config_id` + frozen allowlist + Apply-gate + engine-wiring-status UNCHANGED.** Tests: `tests/test_phase5_*` (95) incl. golden UAT A-J through the production loop. Doc: `docs/ENGINEERING_BRAIN_PHASE5_WORKING_WINDOW_LEARNING.md`. Next: Phase 6 — Live Residual-Issue Detection & Multi-Symptom Experiment Planning.)
>
> Prior update: 2026-07-18 (**Engineering Brain Phase 4 — Canonical Evidence Authorities, Unified Clean-Lap Semantics & Live Per-Corner Outcome Assembly.** Branch `eng-brain-phase4-canonical-evidence` from `master` @ Phase 3 `6314c05`. Evidence authorities + live wiring only — NO new physics/rules/outcome-evaluator, no auto-apply/rollback, no UI redesign. **NO migration — `DB_VERSION` stays 22**, `RULE_ENGINE_VERSION` stays 46.0 (proof: `corner_issue_occurrences` already carries session_id+setup_checkpoint_id+lap_number+segment_id+corner_phase+issue_type+axle+exclusion_reason, live-populated by the Practice path; `sessions.date_utc`+`applied_setup_checkpoints.confirmed_at` give timing — sufficient durable linkage, no duplicate telemetry table). FOUR pure authorities: (1) `strategy/engineering_lap_validity.py` — `evaluate_engineering_lap`/`evaluate_session_laps` with states VALID/VALID_WITH_LIMITATIONS/INVALID/UNRESOLVED + purpose policy (SETUP_ENGINEERING/OUTCOME_COMPARISON strictest, PRACTICE_PATTERN, PERFECT_LAP_REFERENCE, RACE_STRATEGY); unifies aggregate_lap_window flags + resolve_clean_lap pace-outlier + cross_lap_persistence reason vocabulary; all reasons retained. (2) `strategy/corner_evidence.py` — `CornerObservationRecord` + `from_issue_occurrence_row` + recurrence classifier REUSING `practice_pattern_analysis.RecurrenceThresholds` (isolated/emerging/recurring/strongly_recurring; distinct affected VALID laps, never raw count; excluded events don't count; no invented GT7 channels); `to_phase3_observations` → exact Phase-3 shape. (3) `strategy/setup_evidence_assembly.py` (pure selectors RESOLVED/PARTIAL/AMBIGUOUS/MISSING/INCOMPATIBLE — never picks newest) + SessionDB `assemble_setup_experiment_evidence`/`review_experiment_outcome` (production path: assemble per-corner baseline/test from persisted stores → Phase-3 evaluator; no test-only manual objects). (4) `strategy/setup_decision_status.py` — `resolve_setup_decision` (13 driver-facing states; outcome>lifecycle>recommendation precedence; contradictions→INVALID; deterministic allowed/blocked actions). Dormant `arbitrate_setup_decision` formally DEPRECATED (banner kept, points to Phase-4 authority; unwired per test_engine_wiring_status). Wired the off-thread 'Review Test Outcome' worker to `review_experiment_outcome` (closes the Phase-3 live gap: real per-corner evidence now assembled) + renders canonical decision status + evidence readiness. **Golden `config_id` + frozen fan-out allowlist + Apply-gate predicate UNCHANGED.** Tests: `tests/test_phase4_{lap_validity,corner_evidence,evidence_assembly,setup_decision,golden_uat}.py` (86) incl. 3 Fuji RSR golden cases through the production assembly path. Doc: `docs/ENGINEERING_BRAIN_PHASE4_CANONICAL_EVIDENCE.md`. Next: Phase 5 — Working-Window Learning, Successful-Direction Reinforcement & Experiment Selection.)
>
> Prior update: 2026-07-18 (**Engineering Brain Phase 3 — Closed-Loop Outcome Evaluation, Regression Detection & Failed-Direction Learning.** Branch `eng-brain-phase3-outcome-evaluation` from `master` @ Phase 2 `b6f6dd4`. Deterministic outcome engine only — NO new physics/rules, no UI redesign, no auto-apply, no auto-rollback. **SCHEMA: `user_version` 21 → 22** (`DB_VERSION = 22`); `RULE_ENGINE_VERSION` unchanged (`46.0`). NEW pure `strategy/setup_experiment_outcome.py` (Qt/DB/UI/network/AI-free; obtains the Phase 1 `scope_fingerprint`, never recomputes it; COMPOSES existing authorities — `recommendation_scoring.aggregate_lap_window` clean-lap windows, `practice_pattern_analysis.RecurrenceThresholds` repeatability, deterministic driver-feedback fields): outcome states CONFIRMED_IMPROVEMENT/PARTIAL_IMPROVEMENT/NO_MEANINGFUL_CHANGE/REGRESSION/CONFOUNDED/INSUFFICIENT_EVIDENCE; `resolve_experiment_evidence_association` (resolved/ambiguous/mismatch/unresolved — never picks newest silently); `evaluate_lap_validity`; median-based whole-lap + per-corner recurrence comparison (fastest lap alone can't prove success); primary-target (diagnosed symptom) + supporting criteria; protected-behaviour enforcement (material protected regression blocks CONFIRMED); driver/telemetry arbitration (preserves disagreement, no generative text); explicit `decide_outcome` table; `build_failed_direction_learning` (strong single-field→LOCKOUT, compound/weak→CAUTION, never global/invalid/confounded); `build_next_action`; deterministic `evaluate_outcome`. Additive DB v22 (`_migrate_v22`, idempotent): five standalone tables `setup_experiment_outcomes` (immutable; only audit cols superseded_by/invalidated_reason ever UPDATEd) + criteria/protected/corners child tables + `setup_experiment_failed_directions` (scoped). Atomic create w/ full ROLLBACK; idempotent by key. `_experiment_gate_state.has_outcome_record` now real → COMPLETED honestly gated. Orchestrator `SessionDB.evaluate_setup_experiment(...)` drives APPLIED→TEST_IN_PROGRESS→READY_FOR_REVIEW→COMPLETED/REJECTED (CONFOUNDED/INSUFFICIENT stay reviewable), attaches TEST/DRIVER_REVIEW/OUTCOME evidence, and feeds the EXISTING consumers (LOCKOUT → `learning_outcomes` 'worsened' + lineage 'worsened'; consumed by `blocked_rules_from_outcomes`/`rollback_from_lineage`). Driver-triggered off-thread UI: Setup Builder "Review Test Outcome" button → worker thread → `_display_outcome_result` (read-only; never applies/reverts). **Golden `config_id` + frozen fan-out allowlist + Apply-gate predicate UNCHANGED.** Tests: `tests/test_setup_outcome_{domain,persistence,integration,golden_uat}.py` (79) incl. 2 Fuji RSR golden scenarios. Doc: `docs/ENGINEERING_BRAIN_PHASE3_OUTCOME_EVALUATION.md`. Next: Phase 4 — Canonical Evidence Authorities & Unified Clean-Lap/Setup-Decision Semantics.)
>
> Prior update: 2026-07-18 (**Engineering Brain Phase 2 — Persisted Setup Experiments & Recommendation Evidence Ledger.** Branch `eng-brain-phase2-setup-experiments` from `master` @ `3d7c6af` (Phase 1 fast-forwarded onto master first). Durable evidence ledger only — NO before/after outcome scoring, no auto-judgement, no physics, no new rules, no strategy maths, no auto-apply/pit, no UI redesign (Phase 3+). **SCHEMA: `user_version` 20 → 21** (`DB_VERSION = 21`); `RULE_ENGINE_VERSION` unchanged (`46.0`). NEW pure domain `strategy/setup_experiment.py` (Qt/DB/UI/network/AI-free; imports ONLY the pure Phase 1 identity module to OBTAIN, never recompute, the context fingerprints): `SetupExperiment` + enums (`ExperimentStatus` DRAFT→…→COMPLETED/REJECTED/REVERTED/CANCELLED/INVALID, `ChangeRole`, `EvidencePhase`, `AppliedMatchState`, `HandlingPhase`), frozen models (`ExperimentChange`/`ProtectedBehaviour`/`TestProtocol`/`ExperimentEvidence`/`ExperimentHypothesis`), deterministic `validate_transition` with honesty gates (APPLIED needs a checkpoint; READY_FOR_REVIEW needs test evidence; COMPLETED needs a Phase-3 outcome — unavailable now), `compare_proposed_vs_applied` (MATCH/PARTIAL_MATCH/MISMATCH/UNVERIFIABLE), timestamp-free `compute_idempotency_key` (stable under change reorder), and `build_experiment_from_recommendation` (parsed advisor `_data` dict → DRAFT experiment, or None when not actionable). Additive DB v21 (`_migrate_v21`, idempotent): six standalone tables `setup_experiments` (+ changes/protected_behaviours/test_protocol/evidence/state_history); atomic create (full ROLLBACK on any child failure), idempotent by UNIQUE `idempotency_key`, append-only evidence + state history, NULL unknowns, first-class indexed join columns. Repository APIs + orchestration seams `record_recommendation_experiment` / `link_apply_to_experiment`. Wired at the Setup Builder Analyse boundary (`_display_setup_result`, `entry_type=='analyse_setup'` only — baseline Build is a setup ARTEFACT, explicitly NOT an experiment) and the Apply-in-GT7 boundary (`_on_changes_applied_in_game` → link checkpoint + comparison; no auto-apply, recommendation never altered). Every experiment references the Phase 1 `scope_fingerprint`. **Golden `config_id` vector + frozen fan-out allowlist + Apply-gate predicate UNCHANGED.** Tests: `tests/test_setup_experiment_{domain,persistence,integration}.py` (80). Doc: `docs/ENGINEERING_BRAIN_PHASE2_SETUP_EXPERIMENTS.md`. Next: Phase 3 — Closed-Loop Outcome Evaluation, Regression Detection & Failed-Direction Learning.)
>
> Prior update: 2026-07-18 (**Engineering Brain Phase 1 — Canonical Engineering Context & Identity Bridge.** Branch `eng-brain-phase1-canonical-context` from `master` @ `c611d79`. Architecture/data-foundation only — no physics, no new setup rules, no UI redesign, no auto-apply, no auto-pit, no experiment/outcome loop. **SCHEMA: `user_version` 19 → 20** (`DB_VERSION = 20`); `RULE_ENGINE_VERSION` unchanged (`46.0`). NEW pure identity spine `data/engineering_context_key.py` — `EngineeringContextKey` (13 `Optional[str]` components; `None` = genuinely unknown, never guessed), a versioned FULL `fingerprint()` (`eck_v1:<hex16>`) and a STABLE `scope_fingerprint()` over (driver, car, track_location, layout, gt7_version) for before/after joins; structured `EngineeringContextResolution` (status COMPLETE/PARTIAL/AMBIGUOUS/UNRESOLVED/INVALID + per-field provenance + unresolved/ambiguous/warnings); resolvers for session/checkpoint/lineage/feedback; honest ambiguity (free-text track never invents a layout); enrichment without contradictory duplicates. Pure — no PyQt/DB/network/AI; never raises. Additive DB v20 (`_migrate_v20`, `CREATE IF NOT EXISTS`, idempotent): standalone `engineering_context` (keyed by UNIQUE fingerprint) + `engineering_context_links` (bridge `(source_kind, source_id)`→fingerprint, `UNIQUE`, so historical rows resolve WITHOUT destructive migration). Wired best-effort (outside the write lock) at `open_session` / `save_applied_checkpoint` / `record_lineage` / `write_feedback` (feedback INHERITS the session context). PROOF: a new session + applied checkpoint + lineage node + feedback record on the same car/track/layout share ONE `scope_fingerprint` without free-text coincidence. **Golden `config_id` vector + frozen fan-out allowlist UNCHANGED.** Tests: `tests/test_engineering_context_key.py` + `tests/test_engineering_context_bridge.py` (62). Runtime files git-verified untouched. Ownership + compatibility + limitations documented in `docs/ENGINEERING_BRAIN_PHASE1_CANONICAL_CONTEXT.md`. Next: Phase 2 — Persisted Setup Experiments & Recommendation Evidence Ledger (references this context via `scope_fingerprint`).)
>
> Prior update: 2026-07-07 (**Race Strategy Brain Phase 8: Live Pit & Tyre-Age State Tracking — Group 54.** Branch `group54-live-pit-tyre-age-tracking`, from clean `master` (`2081f88`, Group 53 merged). **Added a read-only live pit-stop counter + laps-since-pit / tyre-age tracker so the live replan snapshot can judge tyre age and pit count honestly** — resolving the Group 53 LOW-confidence cap. Evidence-quality only — no auto pit calls, no voice, no driver commands, no setup changes. 1 NEW pure module + 7 NEW test files + additive tracker/adapter wiring. DISCOVERY: `RaceStateTracker` ALREADY detects pit entry/exit (fuel-refuel + conservative sustained-stop heuristic → PIT_ENTRY/PIT_EXIT events); GT7 has NO pit flag (packet flags_raw = on_track/paused/loading only); the app just never counted stops or aged the stint — Group 54 adds that on top of the EXISTING detection (no fabricated signal). (1) NEW `telemetry/pit_state.py` (PURE, no files/AI/actions, never raises): `PitStintState` (frozen: pit_stops_completed/laps_since_pit/current_stint_index/current_stint_start_lap/last_pit_lap/last_pit_event/pit_detection_confidence/pit_detection_source/tracking_active; `.tyre_age_laps`=laps_since_pit while tracking else None); `PitEvent` (NONE/ENTER/EXIT/MANUAL) + `PitDetectionConfidence` (HIGH/MEDIUM/LOW/UNKNOWN); pure updaters `start_stint_tracking`/`apply_lap_completed`/`apply_pit_event` (dedups same-lap, ignores negative laps, NONE never counts)/`apply_manual_pit` + `classify_pit_confidence(fuel_added, threshold)` (MEDIUM refuel/LOW speed-only); KEY HONESTY: before any pit, pit=0 is CERTAIN (HIGH) + tyre_age == laps since start. (2) RaceStateTracker integration (additive +50 lines, runtime-only, read-only): holds a PitStintState; start_stint_tracking at RACING transition; apply_lap_completed in `_check_lap` after a lap records; apply_pit_event in `_exit_pit` (confidence from classify on fuel added); read-only getters `pit_stops_completed`/`laps_since_pit`/`tyre_age_laps`/`pit_state_confidence`/`pit_stint_state`; no persistence, no changed events, no crash on partial packets (119 telemetry tests green). (3) `strategy/race_strategy_live_state.py` maps tracker.tyre_age_laps→RaceReplanState.tyre_age_laps + tracker.pit_stops_completed→pit_stops_completed ONLY at HIGH/MEDIUM confidence (so they can lift readiness); LOW → NOT populated but low-confidence estimate surfaced (SRC_LIVE_LOW "live_telemetry (low confidence — not used)") + warning; UNKNOWN → missing; `LiveReplanStateResult` gained pit_state_confidence. (4) CONFIDENCE IMPACT: pre-pit or post-refuel-pit → MEDIUM (was capped LOW in Group 53); unknown/low-conf tyre → LOW; missing fuel/distance → INSUFFICIENT; still capped MEDIUM (never forced HIGH). (5) UI: no dashboard.py change — enhanced pure `render_live_replan_text` (Group 53 Refresh already calls it) lists pit/stint under Found ("laps since pit: 12 (live)", "pit stops completed: 0 (live)") + Missing when unknown; read-only, manual refresh, no pit-command button. (6) `run_fuji_live_replan(kind)` gained pre_pit_healthy/just_pitted/missing_pit fixtures. Constants: NONE (`DB_VERSION` 13, `RULE_ENGINE_VERSION` 46.0). Schema/migrations: NONE. Tests: 7 new `tests/test_group54_{pit_state_model, pit_detection, tracker_pit_state, live_adapter_pit_state, live_replan_confidence, porsche_fuji_pit_state, strategy_safety_regression}.py` — **63 pure/offline tests (pure model + REAL tracker via MagicMock packets + SQLite :memory:), all pass ~2 s.** tracker_pit_state exercises real wiring (_check_lap ages stint; _exit_pit counts refuel-pit MEDIUM / speed-only LOW). Safety: modules no Qt import + no setup-authoring + no api_key + no Apply/approve + no setup-history write (content-hash); missing pit never safe; Apply-gate + disabled AI-build intact; SessionDB read-only. Regression Group 53/52/51/50/49/48 + telemetry (119) + 47/46 subsets green; dashboard constructs (13 test_ui_structure_smoke). Runtime files untouched. Deferred (Group 55+): automatic pit-call prompts, pit command recommendations, pit-wall voice, auto-refresh loops, weather radar, rival modelling, steering metrics, ML, AI-authored numbers, AI-Build re-enable, track-mapping/track-model mutation, session editing/deleting, large session browser, poster generation; tyre-age is an APPROXIMATION (detected pit assumed to include a tyre change — GT7 doesn't report tyre changes); track-specific pit-lane mapping is a Group 55+ dependency. Known caveat: run UI test files individually on Win/Py3.14 (PyQt cross-file segfault); Group 54's suites are Qt-free. See `docs/UAT_RACE_STRATEGY.md`.)
> Last updated: 2026-07-07 (**Race Strategy Brain Phase 7: Live Current-State Replan Input — Group 53.** Branch `group53-live-replan-current-state`, from clean `master` (`bbf2198`, Group 52 merged). **Wired the Group 52 replan foundation to the app's EXISTING read-only live race-state source.** The Strategy Builder can read live state, compare it to the pre-race plan, and show an advisory-only snapshot with honest missing-state. No automatic pit calls, no voice, no driver commands, no setup changes. 2 NEW pure modules + 6 NEW test files + extended UAT helper + upgraded read-only UI surface. LIVE-STATE DISCOVERY (honest): from `telemetry.state.RaceStateTracker` + last `GT7Packet` the app HAS current lap (laps_recorded), remaining time (computed_remaining_ms, timed) / remaining laps (laps_remaining, lap race), fuel % (packet.fuel_level/fuel_capacity), live burn (avg_fuel_per_lap), strategy/UI-tagged compound (_current_compound — GT7 doesn't broadcast compound); the app does NOT track live tyre age / pit-stop count / required-compounds-used / weather-damage-safety-car → recorded missing; consequence: live snapshots are typically LOW_CONFIDENCE (tyre unknown) or INSUFFICIENT_EVIDENCE and say so. (1) NEW `strategy/race_strategy_live_state.py` (PURE, no Qt/DB/IO/AI, never raises): `build_replan_state_from_tracker`/`build_replan_state_from_live_packet`/`build_replan_state_from_dashboard_context`/`extract_live_replan_state`/`summarise_live_state_sources` → `LiveReplanStateResult` (RaceReplanState + per-field state_sources {live_telemetry/strategy-UI tag/missing} + warnings + missing_state + live_fuel_per_lap); drops impossible values (fuel>capacity ignored not clamped, negative laps→None); unknown → missing (never invented, never safe). (2) NEW `strategy/race_strategy_live_replan.py` (PURE): `build_live_replan_snapshot(*, pre_race_result, live_source|live_state, event_settings, latest_fuel_samples, generated_at)` → `LiveReplanResult` (state/state_sources/readiness/snapshot/driver_message/missing_state/warnings/safety_notes/generated_at; .status/.confidence props); feeds adapter live burn into Group 52 snapshot fuel maths; generated_at caller-supplied (no clock); `render_live_replan_text` + Fuji fixtures (fuji_live_state_healthy/fuel_short/missing). (3) UI (`ui/dashboard.py`, additive): Group 52 placeholder → small read-only "Live Replan Readiness (read-only, advisory only)" group + "Refresh Live Replan Snapshot" button → `_refresh_live_replan_snapshot()` (reads self._tracker+_last_packet read-only, compares vs self._last_race_plan_result stored at Build time, renders advisory snapshot); no plan → "Build a Race Plan first"; no live source → honest "not connected yet"; NO auto-loop/timer/voice/pit-call/Apply/API-key. (4) `ui/race_strategy_uat.py::run_fuji_live_replan(kind)` pairs pre-race one-stop with healthy/fuel_short/missing fixtures (offline). Constants: NONE (`DB_VERSION` 13, `RULE_ENGINE_VERSION` 46.0). Schema/migrations: NONE. Tests: 6 new `tests/test_group53_{live_state_adapter, live_replan_snapshot, live_replan_ui_surface, live_replan_safety, porsche_fuji_live_replan, strategy_regression}.py` — **70 pure/offline tests (duck-typed mocks + SQLite :memory:; UI guarantees source-verified), all pass <1.5 s.** Safety tests: live modules no Qt import + no setup-authoring imports + no I/O + no api_key + no Apply/approve capability + no setup-history write (content-hash); unknown tyre never high confidence; missing fuel→INSUFFICIENT; Apply-gate predicate + disabled AI-build intact; Group 48/49 scoring deterministic; dashboard constructs (13 test_ui_structure_smoke). Regression Group 52/51/50/49/48 + 47/46 subsets green. Runtime files untouched. Deferred (Group 54+): full live replan loop, automatic pit-call prompts, pit-wall voice, telemetry subscription loops, weather radar, rival modelling, steering metrics, ML, AI-authored numbers, AI-Build re-enable, track-mapping, session editing/deleting, large session browser, poster generation; live tyre-age + pit-stop tracking DON'T EXIST — adding a read-only laps-since-pit / pit-stop counter to the tracker is the natural next step to raise live snapshot confidence. Known caveat: run UI test files individually on Win/Py3.14 (PyQt cross-file segfault); Group 53's own suites are Qt-free. See `docs/UAT_RACE_STRATEGY.md`.)
> Last updated: 2026-07-07 (**Race Strategy Brain Phase 6: Manual UAT Remediation & Live Replan Readiness Foundation — Group 52.** Branch `group52-race-strategy-uat-replan-readiness`, from clean `master` (`a32c694`, Group 51 merged). **Verified the Group 48–51 Race Plan surface under Porsche RSR/Fuji UAT (NO defects found) + added a pure, read-only, advisory-only foundation for future live replan.** No new strategy maths, no live telemetry, no automatic pit calls. 1 NEW pure module + 6 NEW test files + extended UAT helper + read-only UI placeholder. (1) `ui/race_strategy_uat.py` extended with `run_fuji_race_plan_uat_check(n_laps, fuel)` → structured `FujiUatCheckResult` (event/session validation, readiness_level, clean_lap_count, fuel/tyre flags, candidate_count, recommended_strategy, one_stop/two_stop_total_time, push_plan_rejected_or_not_recommended, missing_evidence, warnings, safety_checks dict, passed, failure_reasons); deterministic offline. UAT OUTCOME: no defects — full (12-lap) + incomplete (4-lap/no-fuel) scenarios both correct (no crash, missing evidence visible, legal-only candidates, push not recommended, no false certainty); pinned by test_group52_race_plan_uat_remediation.py. (2) NEW `strategy/race_strategy_replan.py` (PURE, no Qt/DB/IO, never raises): `RaceReplanState` (all fields default unknown None; unknown tyre NEVER assumed safe); `validate_replan_state(...)` → RaceReplanStateValidation; `assess_replan_readiness(...)` → RaceReplanReadiness with ReplanReadinessLevel (READY/PARTIAL/LOW_CONFIDENCE/INSUFFICIENT_EVIDENCE; no fuel/compound/distance→INSUFFICIENT, tyre unknown→LOW_CONFIDENCE); `build_replan_snapshot(*, pre_race_result, state, event_settings, latest_fuel_samples, latest_pace_samples)` → RaceReplanSnapshot (original_plan_status/current_plan_still_viable/RaceReplanReason/remaining_strategy_options/confidence/missing_state/driver_message/safety_notes) — compares reported fuel to pre-race burn over laps-to-next-stop; advisory options = pre-race Group 48 scored candidates labelled "pre-race estimate"; confidence capped MEDIUM (LOW when tyre unknown); INSUFFICIENT_EVIDENCE when critical state/plan missing; every snapshot carries "Advisory only — no pit call, setup change, or driver command is applied."; `render_replan_snapshot_text(...)`. (3) Read-only UI placeholder: `replan_placeholder_message()` + a small "Live Replan Readiness: not connected yet …" label at the bottom of the Race Plan group (no button/loop/wiring). Constants: NONE (`DB_VERSION` 13, `RULE_ENGINE_VERSION` 46.0). Schema/migrations: NONE. Tests: 6 new `tests/test_group52_{race_plan_uat_harness, race_plan_uat_remediation, replan_state, replan_snapshot, replan_safety, strategy_regression}.py` — **64 pure/offline tests (SQLite :memory:; UI guarantees source-verified), all pass <1.5 s.** Safety tests: replan module no Qt import + no setup-authoring imports + no I/O + no Apply/approve capability + no setup-history write (content-hash) + honest placeholder; Apply-gate predicate + disabled AI-build intact; Group 48/49 scoring deterministic; dashboard still constructs (13 test_ui_structure_smoke). Regression Group 51/50/49/48 + 47/46 subsets green. Runtime files untouched. Deferred (Group 53+): full live replan, telemetry subscription/loop, automatic pit-call prompts, pit-wall voice, weather radar, rival modelling, steering metrics, ML, AI-authored numbers, AI-Build re-enable, track-mapping, session editing/deleting, large session browser, poster generation. Known caveat: run UI test files individually on Win/Py3.14 (PyQt cross-file segfault); Group 52's own suites are Qt-free. See `docs/UAT_RACE_STRATEGY.md`.)
> Last updated: 2026-07-06 (**Race Strategy Brain Phase 5: Race Plan UAT Hardening & Session Selection Polish — Group 51.** Branch `group51-race-plan-uat-hardening`, from clean `master` (`6938218`, Group 50 merged). **The Group 50 Race Plan surface is now reliable, understandable, and ready for real manual UAT.** Hardening + usability only — NO strategy-maths changes. 2 NEW pure Qt-free modules + 6 NEW test files + ADDITIVE UI wiring in `ui/dashboard.py`. (1) `ui/race_strategy_readiness_vm.py` (PURE, no Qt): `ReadinessLevel` enum (READY/PARTIAL/LOW_CONFIDENCE/INSUFFICIENT_EVIDENCE) + `CheckStatus` enum (OK/MISSING/DEGRADED/MISMATCH/MANUAL/DEFAULT/NA); `build_race_plan_readiness(*, samples, event_settings)` → `RacePlanReadiness` (per-field statuses + overall + readiness_message + next_best_action + honest found/missing); grading mirrors Group 48 gate (no clean laps<3 OR no fuel OR no race length → INSUFFICIENT; missing pit loss OR refuel → LOW_CONFIDENCE; missing tyre proxy OR compound → PARTIAL; else READY); `build_session_diagnostics(...)` → `SessionDiagnostics` (session label, car/track/layout, match status+note, clean-lap count, fuel/tyre/compound availability, message); `validate_event_settings(...)` → `EventSettingsValidation` (honest warnings + field_status + can_run); `empty_state_messages(...)` (short actionable deduped lines for every empty/missing case) + `strategy_result_message(...)` (surfaces Group 48 no-recommendation reason); `list_recent_matching_sessions(db, car_id, track, limit)` (read-only, uses only get_practice_sessions); `render_readiness_html(...)`; never raises, invents nothing. (2) `ui/race_strategy_uat.py` (offline UAT helper, ui→strategy): `FUJI_UAT_EVENT_SETTINGS` + `build_fuji_uat_db`/`build_fuji_uat_context`/`run_fuji_uat` reproduce the RSR/Fuji scenario deterministically in-memory (READY readiness, one-stop beats two-stop, push flagged rear-fragile; `build_fuji_uat_context(n_laps=4, fuel=0.0)` simulates an incomplete session). (3) UI (`ui/dashboard.py`, additive): a read-only session selector `_rp_session_combo` ("Active session (auto)" + recent sessions) + `_btn_rp_refresh_sessions`, a `_rp_session_status` line, a `_rp_readiness_status` line; `_selected_race_plan_session_id()`/`_populate_race_plan_sessions()`/`_refresh_race_plan_diagnostics()` (all read-only); `_assemble_race_plan_inputs()` now uses the selected session; `_run_race_plan()` prepends a readiness+diagnostics banner + "Before you rely on this" guidance above the plan; selector repopulates on Strategy-tab show. NO API key, NO Apply/approve controls, NO setup writes. Constants: NONE changed (`DB_VERSION` 13, `RULE_ENGINE_VERSION` 46.0). Schema/migrations: NONE. Tests: 6 new `tests/test_group51_{race_plan_readiness, session_selection_vm, event_settings_validation, race_plan_empty_states, strategy_surface_hardening, porsche_fuji_uat_path}.py` — **81 pure/offline tests (mock/read-only DB + SQLite :memory:; Qt guarantees source-verified), all pass <1 s.** 1 Group 50 test updated (`test_group50_strategy_surface.py`) for the legitimate new read-only Refresh button (no-Apply/approve intent preserved). Safety tests assert Apply-gate predicate + disabled AI-build line intact, readiness module + Group 50 VM have no Qt import + import no setup-authoring, SessionDB read path read-only, no setup-history write (content-hash), Group 48/49 scoring deterministic; dashboard still constructs (13 `test_ui_structure_smoke` pass). Regression: Group 50/49/48 strategy suites + Group 47/46 subsets green. Runtime files untouched. Deferred: all Group 48–50 deferrals stand; session selector is a small read-only dropdown (not a browser); tyre degradation stays disclosed lap-drift proxy + pit loss manual/event-supplied; no new strategy maths. Known caveat: run UI test files individually on Win/Py3.14 (PyQt cross-file segfault); Group 51's own suites are Qt-free. See `docs/UAT_RACE_STRATEGY.md`.)
> Last updated: 2026-07-06 (**Race Strategy Brain Phase 4: Driver-Facing Race Plan Surface — Group 50.** Branch `group50-race-strategy-surface`, from clean `master` (`7b65fbd`, Group 49 merged). **The Group 48/49 strategy engine is now surfaced to the driver in the Strategy Builder as a clean, read-only Race Plan.** Presentation/integration only — NO strategy-maths changes. 1 NEW pure Qt-free view-model module + 6 NEW test files + a purely ADDITIVE UI block in `ui/dashboard.py`. (1) `ui/race_strategy_vm.py` (PURE, no Qt import): `RacePlanViewModel` frozen dataclass (recommended_strategy_title, confidence_label/reason, estimated_total_time, gap_to_alternatives, stint_plan_rows, candidate_comparison_rows, evidence_source_rows, missing_evidence_rows, risk_flags, safety_notes, driver_explanation, warnings, source_note) + `build_race_plan_view_model(SessionStrategyResult)` + section formatters + `format_race_time` (mm:ss.s) / `compound_name` (RM→Racing Medium) / `fuel_map_label`; evidence rows carry a `category` ∈ {measured, derived, event, manual, default, missing}; risk surfacing = recommended plan's own flags + a cross-plan "Rear traction fragile: push strategy not recommended." note; never raises, deterministic. (2) Pure renderers `render_race_plan_html(vm)` (read-only narrative, never advertises Apply, never claims certainty) + `candidate_table_rows(vm)`/`CANDIDATE_TABLE_COLUMNS` (Strategy/Pit Stops/Compounds/Total Time/Gap to Best/Pit + Refuel/Deg Cost/Fuel Save/Risk/Confidence/Status) + `run_race_plan_from_session(db, **kwargs)` / `run_race_plan_from_event_context(...)` (pipeline + VM, testable with mock/:memory: db). (3) UI: additive `_build_race_plan_group()` in the Strategy Builder tab — intro, two small manual inputs (Pit loss s, Starting fuel %), a "Build Race Strategy" button → `_run_race_plan()`, read-only `_race_plan_text` (HTML) + `_race_plan_table`; `_assemble_race_plan_inputs()` reads canonical EventContext + resolved session id + car id; `_run_race_plan()` derives rear-fragility from the structured DriverProfile and renders; NO API key, NO Apply/approve controls, NO setup writes. (4) Session-backed action with honest fallback: no session → INSUFFICIENT_EVIDENCE + visible missing evidence + "No session data selected" note; manual pit-loss/starting-fuel override event values, surfaced under event settings. (5) Porsche RSR/Fuji surface (via Group 49 benchmark): one-stop 51:52.0 beats two-stop 52:28.0 (+36.0s), SessionDB-measured pace/fuel + derived tyre proxy, push flagged rear-fragile + never recommended, no Apply action. Constants: NONE changed (`DB_VERSION` 13, `RULE_ENGINE_VERSION` 46.0). Schema/migrations: NONE. Tests: 6 new `tests/test_group50_{race_strategy_vm, strategy_surface, strategy_candidate_table, strategy_evidence_display, strategy_safety_regression, porsche_fuji_strategy_surface}.py` — **70 pure/offline tests (mock DB + SQLite :memory:; Qt guarantees source-verified, not via QApplication), all pass <1 s.** Safety tests assert Apply-gate predicate + disabled AI-build line intact, Race Plan group/method has no Apply/approve capability, reads no API key, writes no setup history (content-hash), VM imports no setup-authoring module + no Qt import, SessionDB adapter read-only, Group 48/49 scoring deterministic; dashboard still constructs (13 `test_ui_structure_smoke` pass, run individually). Regression: Group 49 + 48 strategy suites + Group 47/46 subsets green. Runtime files untouched. Deferred: all Group 48/49 deferrals stand; no session browser (uses active/resolved session); tyre degradation stays disclosed lap-drift proxy + pit loss manual/event-supplied. Known caveat: run UI test files individually on Win/Py3.14 (PyQt cross-file segfault) — Group 50's own suite is Qt-free. See `docs/SMART_RACE_ENGINEER_ROADMAP.md` § Group 50.)
> Last updated: 2026-07-06 (**Race Strategy Brain Phase 3: SessionDB Evidence Integration — Group 49.** Branch `group49-strategy-sessiondb-integration`, from clean `master` (`df78535`, Group 48 merged). **The Group 48 strategy brain can now build evidence from real stored SessionDB practice/race telemetry instead of only caller-supplied samples.** 5 NEW pure `strategy/` modules + 6 NEW test files + two tiny ADDITIVE edits: a read-only `SessionDB.get_session_meta(...)` method (no schema change) and an optional `evidence_sources` field on the Group 48 `StrategyExplanation` (Group 48 behaviour byte-identical when unset). (1) `strategy/race_strategy_session_adapter.py` — READ-ONLY (`get_session_meta` + `get_session_laps` only; writes nothing; never raises); `SessionStrategySamples` frozen dataclass + `extract_session_strategy_samples(db, session_id, *, expected_car_id, expected_track, layout_id, derive_tyre_wear)`; clean laps (pit/out excluded); fuel from `fuel_used` else `fuel_start-fuel_end`; per-compound pace session-scoped; TYRE-WEAR HONESTY: SessionDB has no tyre-wear column so degradation is a proxy DERIVED from measured within-stint lap-time drift (positive same-compound increases, ≥3 consecutive laps), labelled "derived" + warning, too-little-data → missing; safe on no-DB/no-session/no-laps/car-or-track mismatch (mismatch → NO samples, flagged). (2) `strategy/race_strategy_from_session.py` — `build_strategy_evidence_from_session(...)` → `SessionEvidenceResult` (evidence, samples, missing_evidence, warnings, source_summary): feeds adapter samples + event settings into Group 48 `build_strategy_evidence(...)`; fabricates nothing; missing data → lower confidence / INSUFFICIENT_EVIDENCE; `build_strategy_evidence_from_event_context(db, session_id, event_context, ...)` sources canonical event settings from `EventContext` (pit loss + starting fuel stay caller-supplied, not in EventContext); `source_summary["fields"]` classifies each input SessionDB-measured / event-setting / default / missing. (3) `strategy/race_strategy_pipeline.py` — `recommend_strategy_from_session(...)` + `recommend_strategy_from_event_context(...)` run evidence → generate_candidates → score_candidates → recommend_strategy → build_session_explanation → frozen `SessionStrategyResult` (…candidates, scored_candidates, recommendation, explanation, confidence, missing_evidence, warnings, safety_notes, source_summary); illegal candidates excluded; Group 48 safety-aware tie-break carries through; NO learning parameter. (4) `strategy/race_strategy_session_explain.py::build_session_explanation(recommendation, evidence, source_summary)` reuses the Group 48 builder unchanged + attaches per-input provenance lines ("Race pace: SessionDB measured (7 clean laps)", "Tyre degradation: missing, confidence reduced", "Refuel rate: event setting"); Group 48 caller-sample path keeps `evidence_sources == []`. (5) `strategy/race_strategy_session_benchmark.py` seeds an in-memory `SessionDB(":memory:")` with 12 RSR/Fuji practice laps (rising +0.08 s/lap drift, ~4.0 L/lap) and runs the full pipeline: samples read from SessionDB, one-stop beats two-stop by ~36 s on total race time (pit loss + refuel included), confidence HIGH, explanation says what came from SessionDB, push plan flagged rear-fragile + never recommended, offline (no AI). Constants: NONE changed (`DB_VERSION` 13, `RULE_ENGINE_VERSION` 46.0). Schema/migrations: NONE (only the additive read-only `get_session_meta`). Tests: 6 new `tests/test_group49_{strategy_session_adapter, strategy_from_session, strategy_pipeline, strategy_session_explainability, porsche_fuji_session_strategy, strategy_safety_regression}.py` — **73 pure/offline tests (mock DB + SQLite `:memory:`), all pass <1 s.** Safety tests assert Apply-gate predicate + disabled AI-build line intact, pipeline leaks no setup-field tokens, no apply/approve capability, imports no setup-authoring module, writes nothing to `data/setup_history.json` (content-hash before/after), no learning parameter, driver memory can't flip legality or change total-time maths, Group 48 scoring deterministic. Regression: Group 48 (95) + Group 47 (73) + Group 46 subset (91/1 skip) + Groups 41–45 non-UI + `test_session_db` (326/1 skip) all green. Runtime files untouched. Deferred (honest): all Group 48 deferrals stand; no new Qt surface (wiring `recommend_strategy_from_session` into the Strategy tab is next); SessionDB has no explicit tyre-wear/pit-loss column so tyre degradation is a disclosed lap-drift proxy + pit loss stays event-supplied. See `docs/SMART_RACE_ENGINEER_ROADMAP.md` § Group 49.)
> Last updated: 2026-07-06 (**Race Strategy Brain Phase 2: Telemetry-Based Strategy Intelligence — Group 48.** Branch `group48-race-strategy-intelligence`, from clean `master` (`1c5890e`, Group 47 merged). **Purely additive — 5 NEW pure `strategy/` modules + 6 NEW test files; no existing file modified.** The Pit Crew can now rank race strategies by estimated TOTAL race time (not fastest lap) from real evidence, with honest confidence and a category-separated explanation. (1) `strategy/race_strategy_evidence.py` — `StrategyConfidence` enum (HIGH/MEDIUM/LOW/INSUFFICIENT_EVIDENCE, `.rank`/`.worst()`) + frozen `RaceStrategyEvidence` (car/track/layout, race duration+laps, fuel & tyre multipliers, refuel rate, pit loss, starting fuel, available/required compounds, mandatory stops, weather, MEASURED lap/fuel/tyre/compound samples, driver_consistency, evidence_confidence, missing_evidence) + `build_strategy_evidence(...)` / `evidence_from_race_params(...)`; records missing evidence via stable codes, NEVER fabricates; race pace = MEDIAN clean lap; confidence gating: no lap OR no fuel → INSUFFICIENT_EVIDENCE, weak pit maths → LOW, soft gaps step HIGH→MEDIUM→LOW. (2) `strategy/race_strategy_candidates.py` — `generate_candidates(evidence)` enumerates no/one/two/three-stop + `1stop_fuelsave`/`2stop_push`/`1stop_compound_switch` with stable IDs; each `StrategyCandidate` carries the plan + PACE-FREE fuel/refuel/pit maths (fuel from measured use only; refuel=ceil(fuel/rate); pit=stops×loss+refuel); LEGALITY is a hard gate (mandatory stops, per-stint fuel ≤ 100 L tank, required compounds all fit) — illegal candidates returned-but-flagged, excluded from `legal_candidates()`; fuel-map consumption factors are documented model constants, not invented telemetry. (3) `strategy/race_strategy_scorer.py` — `score_candidates(...)` ranks legal candidates by `total = green_base + degradation_cost + pit_time + fuel_saving_cost + compound_cost`, all itemised; degradation from measured tyre-wear samples only (0+note otherwise), fuel-save cost only on a lean-map candidate, compound cost only where per-compound pace evidence exists; `recommend_strategy(...)` adds a safety-aware tie-break (`SAFETY_TIE_TOLERANCE_S=5.0` → lower-risk plan wins a near-tie) so a fragile push loses to a stable plan; `fuel_save_worth_it()`; driver memory (`rear_traction_fragile`) touches confidence/risk/tie-break ONLY, never legality or the time maths. (4) `strategy/race_strategy_explain.py` — `build_explanation(...)` → `StrategyExplanation.to_text()` keeps KNOWN/CALCULATED/ASSUMPTION/MISSING/RISK visibly separate, shows recommended plan + confidence, no "perfect strategy"/"guaranteed" language. (5) `strategy/race_strategy_benchmark.py` — Porsche 911 RSR '17 / Fuji Full Course / ~50 min / 8× tyre / 3× fuel / 1 L/s refuel; reads rear-fragility from the structured Group 42 `DriverProfile` (never free text); proves a one-stop beats a two-stop by ~36 s on total race time, the push plan is flagged for rear fragility and never recommended. Constants: NONE changed (`DB_VERSION` stays 13; `RULE_ENGINE_VERSION` stays 46.0). Schema/migrations: NONE. Tests: 6 new `tests/test_group48_{strategy_evidence, strategy_candidates, strategy_scorer, strategy_confidence, strategy_ui_explainability, porsche_fuji_strategy_benchmark}.py` — **95 pure/offline tests, all pass <1 s.** Safety tests assert the strategy surface leaks no setup-field tokens, has no apply/approve capability, imports no setup-authoring module, keeps the Apply-gate predicate string + the disabled AI-build line intact, and that driver memory cannot flip legality or change the total-time maths. Regression: Group 47 (73) + Group 46 (151/1 skip) + Groups 41–45 non-UI (351/1 skip) green; no existing file modified. Runtime files untouched. Deferred (honest): rival/steering/track-mapping/weather-radar/ML/AI-authored-numbers/AI-Build-reenable/pit-wall-voice/UI-redesign all stand; wiring the evidence builder to pull live `SessionDB` samples is a scoped follow-up; no new Qt surface (explanation is a pure text builder, per the PyQt-segfault test convention). See `docs/SMART_RACE_ENGINEER_ROADMAP.md` § Group 48.)
> Last updated: 2026-07-06 (**Setup Brain Outcome Verification & Learning Loop 2 — Group 47.** Branch `group47-setup-brain-outcome-learning`, on top of Group 46 (merged to master @ `249410e`). NEW pure module `strategy/setup_outcome_verification.py` classifies whether an approved, applied change actually helped — IMPROVED/UNCHANGED/WORSE/MIXED/INSUFFICIENT_EVIDENCE — from before/after telemetry + safely-folded driver feedback, feeding a one-step, validator-gated confidence/ranking nudge and an honest explanation. `_migrate_v13` adds 5 additive `learning_outcomes` columns (SQLite-only; never `setup_history.json`); `DB_VERSION`→13; `RULE_ENGINE_VERSION` stays 46.0. Rule-first, AI-audit-only; all Group 43–46 guarantees intact (Analyse/Baseline AI-disabled; AI audit explanation-only; old AI Build disabled; Apply-gate UNCHANGED; only approved changes actionable). Learning affects confidence/ranking/explanation only. 4 new test files (73 tests); Group 38–46 regression green incl. AC37. See `docs/RULE_FIRST_SETUP_BRAIN.md` §16.)
> Last updated: 2026-07-06 (**Setup Brain Learning & Race Context Intelligence — Group 46.** Backend + UI + DB; branch `ofr2-quali-race-disciplines` (on top of Group 45). **The rule-first Setup Brain now LEARNS across sessions (a real SQLite-backed rule-outcome feed) and its ANALYSE recommendations are shaped by fuel load and by fuller per-gear telemetry; session type now numerically biases the from-scratch baseline; the Porsche pack inherits the new fuel/tyre/learning confidence layers.** Architecture preserved: telemetry + feedback + setup + car/track/session context + learning history → deterministic diagnosis → deterministic rule recommendation → validation → AI audit-only → approved-only display/apply; the AI still cannot author setup values / add approved fields / un-block / un-reject / author per-gear values; Baseline and Analyse both work AI-disabled; the old AI Build path stays disabled; the Apply-gate predicate is UNCHANGED. (1) CROSS-SESSION LEARNING PERSISTENCE + FEED: NEW SQLite table `learning_outcomes` (`data/session_db.py::_migrate_v12`; PRAGMA user_version 11→12; additive `CREATE TABLE IF NOT EXISTS`, idempotent; columns id/ts/car_id/track/layout_id/session_id/session_type/rule_id/source_path/verdict/confidence/driver_profile_version/rule_engine_version + index on (car_id,track,layout_id)) — **DELIBERATELY NOT persisted to `data/setup_history.json`** (that file is a user-local artifact owned by `setup_history.py`; learning lives entirely in the gitignored DB). `record_learning_outcome(...)` (INSERT, never raises) written from the OFR-1 scoring pass in `ui/dashboard.py::_trigger_scoring_pass` after `persist_score`, per approved rule_id, skipping insufficient_data; `get_learning_outcomes(car_id,track,layout_id)` returns `[]` on any error. FEED: `build_combined_setup_response` loads scoped rows into a real `RuleOutcomeStore` before `run_rule_engine` (improved→fire+success; worsened/neutral→fire; insufficient_data→skip); `_process_rule` threads car/track/profile_version with a key-aware lookup + empty-key fallback; UPGRADE (`>=MIN_OUTCOME_SAMPLES=3` samples AND success_rate `>=HIGH_SUCCESS_RATE=0.60` → +1) and DOWNGRADE (`<LOW_SUCCESS_RATE=0.40` → −1), between = no change, both capped at one step, both validator-gated, both structurally unable to un-block/un-reject/author values; `learning_influence` set ONLY when a step actually happened; `_learning_note` reflects real loaded history. (2) FUEL-MULTIPLIER INFLUENCE (Analyse): `diagnosis["fuel_multiplier"]` (value) + `diagnosis["fuel_high"]` (`>=HIGH_FUEL_MULTIPLIER_THRESHOLD=5.0`; unknown→False) injected (previously only `fuel_known`); `_process_rule` UPGRADES confidence of traction/stability fields (`_FUEL_TRACTION_STABILITY_FIELDS` = lsd_accel/lsd_initial/arb_rear/aero_rear/ride_height_rear with delta>0), NOTE-ONLY for rotation/aero-cut (`_FUEL_ROTATION_FIELDS` delta<0, no downgrade); NO new deltas (ranking/confidence only); `fuel_influence` set only when the effect occurred + appended to the change's `evidence` list so it renders in the existing UI; fuel=1.0/absent → no bias, no claim. (3) SESSION-SPECIFIC NUMERICAL BASELINE TUNING: `setup_baseline._SESSION_BIAS_TABLE` keyed qualifying/sprint/endurance/practice/unknown → {field:delta}, accumulated into the SAME bias dict as the driver-profile table so existing clamp/round/validator apply unchanged; `_normalise_session_for_bias(session_type, duration_mins)`: qualifying / sprint (race & duration<60 or unknown) / endurance (race & duration_mins>=60) / practice / unknown, duration<=0 NOT endurance; `build_baseline_setup` + `build_baseline_setup_response` gained a `duration_mins` param (threaded from `_ai_snap.duration_mins` / `EventContext.race_duration_minutes`); a per-field `session_changed` flag compares clamped/rounded output with vs without the session bias so `session_influence` claims a bias ONLY for fields that actually moved (known-session-but-unchanged → "session noted — no numerical change for this field"; unknown → ""). (4) FULLER PER-GEAR INTELLIGENCE (REAL telemetry detection): `setup_diagnosis` genuinely detects `wheelspin_by_gear` by bucketing per-frame wheelspin (throttle>0.7, speed>2 m/s, rear-wheel-speed>1.3×vehicle speed) by the gear active at each frame, normalized PER-LAP (mirroring `rev_limiter_by_gear`); `bog_by_gear` honestly deferred = None (GT7 10 Hz lacks a reliable longitudinal-accel signal), `lockups_by_gear` also None; `setup_rule_engine._emit_per_gear_changes` proposes `gear_N` ONLY when a REAL indexed signal exists for gear N — rev-limiter-in-gear (`per_gear_limiter_evidence[N]>0` with `gearing_diagnosis_category=="gear_too_short"`) OR per-gear wheelspin (`wheelspin_by_gear[N] >= _PER_GEAR_WHEELSPIN_THRESHOLD=2.0` frames/lap); conservative ±0.03 delta (smaller than final_drive's ±0.05); gated on `gearbox_flag=="may_change"`; SAME clamp + strict-`>` monotonic + validator machinery; rule_id `"PG_{N}"`, `source_label` "per-gear rule"; `final_drive` (B5/B5b) UNTOUCHED as the broad lever; `diagnosis["per_gear_explanation"]` records proposed(+evidence)/not-proposed(+reason) for EVERY gear; "top speed low" alone with no indexed evidence → NO gear change + explanation why. (5) PORSCHE 911 RSR '17 EXTENSION: existing Pack P (P1 traction-first lsd_accel, rr+gr3 via `CAR_DRIVETRAIN_OVERRIDES`) auto-benefits from the fuel/tyre/learning confidence layers (no new authored rule); rear-downforce protection under instability still via existing Pack A A2; benchmark AC37 (RSR/Fuji/50min/high tyre+fuel/rear-loose+mid-push+floaty-front/snap-throttle-wheelspin+top-speed-low+entry-stable+possible-bottoming) verified traction-first before/instead of aero-cut, no rear-downforce reduction, no rearward brake bias, no generic ride-height raise without bottoming confidence, no top-speed gear-lengthening as the primary wheelspin fix, no AI-authored values, passes the Apply gate; `source_label` distinguishes Porsche-specific vs generic. Constants: `RULE_ENGINE_VERSION` "45.0"→**"46.0"**; DB user_version 11→12 (`DB_VERSION=12`); `HIGH_FUEL_MULTIPLIER_THRESHOLD=5.0`; `HIGH_SUCCESS_RATE=0.60`. Tests: 6 new `tests/test_group46_{learning_persistence, fuel_influence, baseline_session_modifiers, per_gear, porsche_pack, ui_explainability}.py` (122 tests, incl. the AC37 RSR/Fuji integrated regression, a fuel-renders-into-evidence test, and an AC16 single-winner-per-field learning-safety assertion); reconciled version/schema tests — `RULE_ENGINE_VERSION`→46.0 (`test_group42_rule_first_engine`), DB version→12 (`test_group42_legacy_storage`, `test_group18b_rec_persistence`, `test_session_db`, `test_group18e_setup_history`); run the suite IN HALVES on Win/Py3.14; the ~7–20 pre-existing frozen-allowlist / OFR failures are KNOWN, unrelated, and untouched. `docs/RULE_FIRST_SETUP_BRAIN.md` (§ 15 "Setup Brain Learning & Race Context (Group 46)"), `docs/SETUP_BRAIN_UPGRADE.md` (§ Group 46), `docs/UAT_SETUP_BRAIN.md` (§ Group 46), `MASTER_TESTING_REGISTER.md` (Setup Brain Learning & Race Context (Group 46)). Deferred (honest): the Baseline path does NOT consume rule-confidence learning (baseline change dicts carry an honest EMPTY `learning_influence`; learning shapes the ANALYSE path only); `source_path` is effectively always "Analyse" today (baseline recording of a scored `setup_recommendations` row is DEFERRED, so `source_path="Baseline"` is schema-supported but unwritten); `session_type` is stored as `""` on `learning_outcomes` (setup_recommendations has no session_type column; scope enforced by car_id+track+layout_id; a JOIN/column deferred); `bog_by_gear` + `lockups_by_gear` per-gear detection deferred (no genuine telemetry signal); a fuel-specific *delta* rule (fuel is confidence/ranking-only). Prior: **Setup Brain Intelligence Expansion — Group 45.** Backend + UI; branch `ofr2-quali-race-disciplines` (on top of Group 44). **The rule-first Setup Brain became context-aware: session type, tyre-wear, drivetrain, and car-class now genuinely shape which rules fire and how confident/ranked they are — WITHOUT inventing precision (delta magnitudes unchanged; context affects filtering, confidence, ranking, contraindication, and explanation only).** Architecture preserved: Pit Crew owns the decision, AI stays audit-only (`parse_audit_response` strips canonical params; `map_audit_to_finaliser` never un-blocks; voice narration-only); both Analyse and Baseline run through the one validator → finalisation funnel → renderer → Apply gate; the old AI build path stays disabled; everything works with AI disabled. (1) ENGINE SCOPE FILTER (`strategy/setup_rule_engine.py::_scope_matches`): rules already carried `applies_session`/`applies_drivetrain`/`applies_car_class` but the engine ignored them — now enforced at runtime; `any`/`None` = wildcard-permissive (unknown never filters); **Pack A safety rules EXEMPT**; all-rules-filtered-out → valid empty `SetupPlan` (no raise). (2) CONTEXT RESOLUTION (`strategy/driving_advisor.py`): Analyse reads `_event_ctx` (tyre_wear→tyre_wear_multiplier, fuel_multiplier, duration_mins for endurance) + new params `purpose`→`SessionType`, `car_specs.category`→`CarClass`, `drivetrain`; drivetrain precedence = explicit UI combo > `CAR_DRIVETRAIN_OVERRIDES` (`{"Porsche 911 RSR (991) '17":"rr"}`) > empty DB → `None` (generic); Baseline receives SCALAR params only (session_type, tyre_wear_multiplier, car_class) — NO EventContext injected; both UI analyse handlers (`_setup_analyse_ai`/`_setup_analyse_ai_for_form`) + both baseline callers thread these. (3) DRIVER-PROFILE ACTIVE WEIGHTING (`setup_rule_engine.py`): bounded {−1,0,+1} rank bonus as a conflict-resolution tiebreaker when confidence is equal (all `rule.driver_style_tags ⊆ profile.style_tags`→+1; `dislikes_snap_exit`+lsd_accel increase→hard block+−1); magnitudes/deltas UNCHANGED (no fake precision); baseline `_PROFILE_BIAS_TABLE` gained `trail_braker`→brake_bias −0.5 + `rotation_without_snap`→lsd_decel −2. (4) SESSION/TYRE/FUEL: session type biases confidence (quali upgrades front-bite/trail-braker rules; race upgrades safety-phase/consistency rules; endurance = race + duration_mins>=60); `HIGH_TYRE_WEAR_THRESHOLD=5.0` → tyre_wear_multiplier>=5.0 sets `diagnosis["tyre_wear_high"]` which CONTRAINDICATES (suppresses) 4 genuinely tyre-abusing rules — B3 (lsd_accel decrease), C1_entry_lsd_decel (lsd_decel decrease), C3_mid_arb_rear (rear ARB soften), C7_kerb_arb_rear (rear ARB soften); rules that INCREASE lsd lock or rear downforce are deliberately NOT suppressed (they stabilise worn tyres); missing tyre/fuel context → honest "tyre/fuel context not available — conservative default applied", NO tyre/fuel claim; fuel multiplier READ (`fuel_known`) but only informational. (5) DRIVETRAIN/CAR-CLASS MODIFIERS: applies_drivetrain (fr/ff/mr/rr/awd) + applies_car_class (gr1..gr4/road/race) filters enforced; car class from `car_specs.json.category` (579 cars); drivetrain NOT reliably in per-car data — from the manual combo or overrides; unknown → generic logic + honest note. (6) PORSCHE 911 RSR '17 PACK (Pack P via `register_pack("P",...)`): rule P1 = cautious lsd_accel increase (traction-first), scoped applies_drivetrain=rr + applies_car_class=gr3, precondition snap-throttle wheelspin, contraindicated when `snap_oversteer_exit` diagnosed; rear-downforce protection under rear instability already provided by existing Pack A A2 (unconditional, all cars) so a separate P2 was intentionally OMITTED; ride-height raise gated by existing A3/A4; top-speed deficit under wheelspin handled traction-first (P1), not aero-cut-first (A2 blocks the cut); every change labelled `source_label` "Porsche-specific rule"/"generic rule"; pack asserts RR via `CAR_DRIVETRAIN_OVERRIDES` (NOT the empty DB column), the manual combo overrides it. (7) GEARBOX: B5 (gear_too_short→final_drive_down) + new B5b (gear_too_long→final_drive_up); limiter_limited stays preserve; the sprint's "limiter_before_braking" is NOT a real category — maps to existing gear_too_short (documented, not faked); diagnosis exposes `per_gear_limiter_evidence` (alias of `rev_limiter_by_gear`), full per-gear rules DEFERRED (final-drive-only broad logic today); monotonic ordering enforced NON-INCREASING — equal adjacent ratios ALLOWED, only a strict inversion rejected "monotonic ordering violation" (engine AND `gearbox_ratio_inversion` validator both strict-`>` now). (8) LEARNING SEAM: production now constructs a live-but-EMPTY `RuleOutcomeStore` (was None) — the confidence-downgrade hook is wired but never fires without samples, so behaviour unchanged; response carries `_learning_note` "no cross-session learning history available"; cross-session persistence + a success-recording feed (e.g. from OFR-1 `recommendation_scoring` verdicts) DEFERRED; learning can only lower a confidence label/affect ranking — it CANNOT un-block a safety rule, un-reject a change, bypass validation, or make AI actionable. (9) EXPLAINABILITY: each approved + each rejected change now carries `source_label`, `session_influence`, `car_drivetrain_influence`, `pack` (plus the pre-existing symptom/evidence/rule_id/rationale/risk_level/confidence_level/driver_style_alignment); populated HONESTLY — a positive session/tyre/car claim appears only when that context was received AND used, missing context → explicit neutral/"not available" string; baseline changes carry `_LABEL_NEUTRAL`/`_LABEL_BIASED`/`_LABEL_MIDPOINT`/`_LABEL_CONSERV` and never claim telemetry evidence; renderer shows a small `source_label` row; baseline `session_influence` text honest — records a session was noted but the baseline is NOT session-tuned (session-specific baseline bias deferred; baseline bias driver-profile-driven only). Constants: `RULE_ENGINE_VERSION` now **"45.0"** (was "42.0"), `HIGH_TYRE_WEAR_THRESHOLD=5.0`. Tests: NEW `tests/test_group45_engine_scope.py`, `test_group45_gear_monotonic.py`, `test_group45_context_signals.py`, `test_group45_porsche_pack.py`, `test_group45_explainability.py`, `test_group45_learning.py`, `test_group45_baseline_context.py`, `test_group45_ui_context.py`; 3 existing tests reconciled (`RULE_ENGINE_VERSION` "42.0"→"45.0"; baseline lsd_decel bias now nets differently with `rotation_without_snap`; gearbox inversion validator now strict-`>` allowing equal ratios); all Group 45 tests pass, the ~18 pre-existing frozen-allowlist/schema failures are KNOWN, unrelated, and untouched; run the suite IN HALVES on Win/Py3.14 (flaky PyQt teardown segfault). `docs/RULE_FIRST_SETUP_BRAIN.md` (§ Group 45 + the dedicated "Setup Brain Intelligence Expansion" section §14), `docs/SETUP_BRAIN_UPGRADE.md` (§ Group 45), `docs/UAT_SETUP_BRAIN.md` (§ Group 45), `MASTER_TESTING_REGISTER.md` (Setup Brain Intelligence Expansion (Group 45)). Deferred (honest): cross-session `RuleOutcomeStore` persistence + a success-recording feed (seam in place, empty in production); full per-gear individual ratio proposal rules (`per_gear_limiter_evidence` key exists for future use); session-specific NUMERICAL baseline tuning (session context recorded on baseline changes but does not yet change baseline values); fuel multiplier read but only informational (no fuel-specific rule); the two opposing lsd_decel baseline bias entries (race_values_consistency +2 vs rotation_without_snap −2) net to zero on a profile with both flags. Prior: **Rule-First From-Scratch Setup Baseline Generator — Group 44.** Backend + UI; branch `ofr2-quali-race-disciplines` (on top of Group 43); delivered via the feature-factory chain. **Restores the capability lost when Group 43 disabled "Build Setup with AI" — the app can now author a COMPLETE safe starting setup for a car with NO telemetry, deterministically, with the AI NEVER called.** A new **"Build Baseline Setup"** button (`_btn_baseline`) sits separate from the still-disabled `_btn_build_setup` (Group 43 guards untouched). Why a new module (not `run_rule_engine`): the rule engine emits DELTAS off a telemetry diagnosis, so with no telemetry almost no rules fire — a separate ABSOLUTE-VALUE author was required. NEW `strategy/setup_baseline.py`: `NEUTRAL_SEEDS` (single source of truth for neutral physics defaults; matches the form seeds in `ui/setup_form_widget.py` — lsd_front_initial/accel/decel take the FORM values 10/15/5, differing from the `ai_planner` parser fallbacks 0/0/0); `build_baseline_setup(car, ranges, drivetrain, num_gears, profile, allowed_tuning, tuning_locked) -> raw_data dict` (plan_to_raw_data shape) authors ALL 33 actionable `_CANONICAL_SETUP_PARAMS` (34 minus display-only transmission_max_speed_kmh) as ABSOLUTE values via neutral seed → driver-profile bias (`_PROFILE_BIAS_TABLE`: prefers_rear_stability→arb_rear−1/toe_rear+.05; dislikes_snap_exit→lsd_accel−2; prefers_front_bite→arb_front+1/toe_front−.02; dislikes_floaty_front→aero_front+50; protects_downforce→aero_rear+50; race_values_consistency→lsd_decel+2) → clamp to `resolve_ranges(car)`. GEARBOX (`_build_gearbox_changes`): final_drive = midpoint of `_FINAL_DRIVE_RANGE (2.5,6.0)`; gear_1..gear_num_gears = a strictly-DECREASING geometric sequence inside `_GEAR_RATIO_RANGE (0.5,4.0)` (monotonic by construction → `gearbox_ratio_inversion` can never fire), sized to the car's gear count (>6 capped, ≤1 → single gear@2.0, 0 → none); gearbox ranges function-local-imported from `setup_diagnosis` (source of truth, try/except fallback to local constants). LOCKED categories (via `_derive_locked_fields`) excluded from actionable output + named by human category (e.g. "Suspension, Aero") in the analysis text; tuning_locked=True → empty changes. Every change carries a source label ("neutral default"/"range midpoint"/"driver-profile biased"/"conservative default, not diagnosed" — the last is honest: camber/toe/dampers/springs/lsd_initial/lsd_front_initial have NO engineering authority; the baseline is a safe STARTING POINT, not an optimum). ORCHESTRATOR `DrivingAdvisor.build_baseline_setup_response(car_name, ranges, drivetrain, num_gears, allowed_tuning, tuning_locked, session_type="Race") -> JSON str`: `build_driver_profile()` → `build_baseline_setup` → `validate_setup_engineering_structured` (neutral baseline passed as BOTH the `setup` arg AND the proposed setup_fields so increment/comparison rules see zero delta) → `_filter_baseline_artifact_warnings` (drops ONLY WARNING-severity failures whose message contains "is a no-op" or "too many changes" — definitional artifacts of a full-field from==to baseline; ALL blocking failures pass through unfiltered — the severity guard `if vf.severity == "warning"` is the OUTER condition, proven unable to suppress a blocking failure) → `_finalise_recommendation` → JSON identical in shape to `build_combined_setup_response`. NO api_key read, NO call_api, NO audit; a clean neutral baseline returns status "approved" with validation_warnings == []. FRONTEND: new `_btn_baseline` "Build Baseline Setup" (enabled+visible; added to `_RACE_ALIASES`) in `ui/setup_form_widget.py` + `ui/setup_builder_ui.py` handlers `_generate_baseline_setup`/`_generate_baseline_setup_for_form` (daemon thread → `_baseline_result_queue` in `ui/dashboard.py`, polled) → `_display_baseline_result` re-enables the baseline button then DELEGATES to the shared `_display_setup_result` renderer + Apply gate (no duplication); advisor accessor `self._driving_advisor`; Group 43 `_btn_build_setup`/`_run_build_setup*` guards untouched. Tests: `tests/test_group44_baseline_generator.py` (86 backend) + `tests/test_group44_baseline_ui.py` (64 UI/integration); **406 green together with group41 + group42 (all) + group43; 0 fail** — the 8 pre-existing frozen-allowlist track-modelling failures are unrelated and untouched; run the suite in halves on Win/Py3.14 to avoid the flaky PyQt teardown segfault. `docs/RULE_FIRST_SETUP_BRAIN.md` (§ Group 44), `MASTER_TESTING_REGISTER.md` (Rule-First Setup Baseline Generator (Group 44)). Deferred (honest): `_btn_baseline` enabled-at-construction with a runtime car/track guard (no proactive disable, consistent with `_btn_analyse_setup`); the shared renderer also re-enables `_btn_analyse_setup` after a baseline (harmless); the "no telemetry baseline" symptom label is generic even on driver-profile-biased fields; the no-authority fields are conservative defaults not engineered; the old `build_car_setup` AI-authoring path remains dead-in-tree behind the Group 43 guards; build candidates — wire `RuleOutcomeStore` live-learning + persistence, per-car gearbox ratio bounds (promote `gearbox_out_of_range` to blocking), track-type biasing of the baseline. Prior: **Rule-First Setup Brain — Group 42.** Backend + UI + DB; branch `ofr2-quali-race-disciplines` (on top of Group 41). **The Setup Brain is inverted from AI-first to RULE-FIRST** — deterministic race-engineering rules are now the PRIMARY source of setup recommendations; the AI is demoted to an AUDIT-ONLY layer (approve/warn/reject/request-more-data) that CANNOT author actionable setup changes. ONE source of truth for actionable changes: the deterministic rule engine. New flow in `build_combined_setup_response`: diagnose → `build_driver_profile()` → `run_rule_engine()` → `SetupPlan` → `plan_to_raw_data` → `_normalise_changes` → `validate_setup_engineering_structured` → if blocking `_build_deterministic_fallback` (NOT AI), else if API key `call_api` for AI AUDIT ONLY → `parse_audit_response` (strips canonical setup field keys) → `map_audit_to_finaliser` → `_finalise_recommendation` (unchanged funnel). NEW backend modules (all `strategy/`, pure Python): `setup_knowledge_base.py` (rule catalogue: `register_pack`/`get_all_rules`/`resolve_delta`; enums RulePhase/RiskLevel/ConfidenceLevel/DrivetrainType/CarClass/SessionType; NamedTuples SetupRule/SetupEvidence; **22 rules** — Pack A A1–A8 safety invariants, Pack B B1–B6 driver-style adaptation, Pack C/D C1_entry_lsd_decel/C2_entry_brake_bias/C3_mid_arb_rear/C4_mid_rear_aero/C5_exit_lsd_accel/C6_exit_rear_aero/C7_kerb_arb_rear/C8_kerb_rh_rear handling-phase starter set, extensible via register_pack; remaining per-setting Pack C deferred; delta resolvers = named-string lookups in `_DELTA_RESOLVERS`, no stored callables); `setup_driver_profile.py` (`DriverProfile` NamedTuple + `DriverStyleAlignment` enum; `build_driver_profile()` derives 8 booleans from the existing `PERSONAL_DRIVER_TUNING_MODEL`/`DRIVER_HARD_CONSTRAINTS`; never raises — driver style is now a DATA STRUCTURE for engine ranking + contraindications, not just prompt text); `setup_rule_engine.py` (`SetupChangeIntent`/`SetupPlan` NamedTuples; `run_rule_engine(...)` — Pack A protects fields, conflict resolution → rejected `conflict:<id>`, no-op exclusion, gear-count gating, confidence-downgrade hook; `RuleOutcomeStore` fire/success counts keyed rule_id/car/track/driver_profile_version, `get_success_rate` None below MIN_OUTCOME_SAMPLES; never raises → empty plan on error); `setup_plan.py` (`plan_to_raw_data` emits the raw_data dict the funnel consumes incl. confidence + validation_targets; `rejected_to_json`); `setup_ai_audit.py` (`AuditStatus` APPROVED/APPROVED_WITH_WARNINGS/REJECTED/NEEDS_MORE_DATA + `AuditResult`; `build_audit_prompt` 8 labelled sections; `parse_audit_response(text, canonical_params)` STRIPS any canonical param key + logs stripped_fields, unknown status → NEEDS_MORE_DATA, never raises; `map_audit_to_finaliser` — REJECTED/NEEDS_MORE_DATA + no blocking → approved_with_warnings advisory, a blocking engineering failure ALWAYS wins). Constants in `_setup_constants.py`: `RULE_ENGINE_VERSION="42.0"`, `MIN_OUTCOME_SAMPLES=3`, `LOW_SUCCESS_RATE=0.40`, `AI_AUDIT_REJECTED_ADVISORY="ai_audit_rejected_advisory"` (NOT in APPROVED_STATUSES); APPROVED_STATUSES unchanged = {approved, approved_with_warnings, fallback_generated}. VOICE PATH (`build_setup_advice_response`) constrained to NARRATION-ONLY via new `_strip_actionable_for_voice(data)` (zeroes changes=[]/setup_fields={} pre-normalisation); full rule-first voice rebuild DEFERRED. DB v11: `data/session_db.py::_migrate_v11` bumps user_version to 11 + adds 8 nullable TEXT cols to `setup_recommendations` (deterministic_plan_json, ai_audit_json, validation_status, approved_changes_json, rejected_changes_json, diagnosis_json, driver_profile_version, rule_engine_version), recommendation_text blob preserved; POPULATED on insert (`_rec_parser.py` + `insert_setup_recommendations`); full migration off the blob deferred. LEGACY SAFETY (closes Group 41's caveat): `data/setup_history.py` adds `is_legacy_unknown`/`normalise_validation_status`/`LEGACY_UNKNOWN` — a rec with absent/None/unrecognised status is now `legacy_unknown` = DISPLAY-ONLY, NO Apply (previously could default to approved — hole closed, enforced in `_display_setup_result` + gated at Apply); `_rejected_` bucket routing preserved (`ai_audit_rejected_advisory` routes there). LEARNING: `RuleOutcomeStore` is FOUNDATION ONLY — the confidence-downgrade hook (samples≥MIN_OUTCOME_SAMPLES + success_rate<LOW_SUCCESS_RATE → downgrade one step) is implemented + unit-tested, but live wiring + cross-session persistence DEFERRED (`build_combined_setup_response` passes `rule_outcome_store=None`); no fake ML, a deterministic weighted counter only. UI (`ui/setup_builder_ui.py::_display_setup_result` + `ui/setup_form_widget.py`): section order diagnosis → "Pit Crew recommendation" (approved changes, each with a collapsed "Why Pit Crew recommended this" details block: symptom/rationale/evidence/rejected_alternatives/risk_level/confidence_level/driver_style_alignment) → "Protected fields (Pit Crew will not change these)" → "Rejected candidate changes (not applied)" → "AI audit" (verdict + concerns) → "Rejected AI output — not for use"; legacy banner "Legacy recommendation — display only, cannot apply"; Apply relabelled "Apply Pit Crew recommendation", hidden unless status ∈ APPROVED_STATUSES AND approved changes present AND not legacy. RESPONSE CONTRACT: per-change explainability keys INSIDE each `changes` item (symptom, evidence list, rule_id, rationale, rejected_alternatives list, risk_level, confidence_level, driver_style_alignment); new top-level keys `ai_audit`, `deterministic_plan` {proposed_count, rejected_candidate_count, protected_fields}, `protected_fields`. Tests: 136 new across `tests/test_group42_rule_first_engine.py`, `test_group42_ai_audit_only.py`, `test_group42_driver_style.py`, `test_group42_legacy_storage.py`, `test_group42_handling_phases.py`, `test_group42_voice_path_safety.py`, `test_group42_ui_gate.py` + 17 rewritten (test_group38 TestRegenerateOnceOrchestration, test_group40 TestAC9DeterministicFallback, test_group41 ×2, test_group27 ×1); all green, zero new regressions; the SAME 8 pre-existing frozen-allowlist track-modelling guards remain (unrelated). `docs/RULE_FIRST_SETUP_BRAIN.md` (NEW architecture doc), `docs/SETUP_BRAIN_UPGRADE.md` (§ Group 42), `docs/UAT_SETUP_BRAIN.md` (Rule-First section). Deferred: RuleOutcomeStore live wiring + persistence, remaining Pack C per-setting rules, full DB migration off the blob, full voice-path rule-first rebuild. Prior: Setup Builder Engineering Validation Gate — Group 41. Backend + UI (a display-safety gate in the Setup Builder); branch `ofr2-quali-race-disciplines` (on top of Group 40). Production files: `strategy/setup_diagnosis.py`, `strategy/driving_advisor.py`, `strategy/_setup_constants.py` (NEW), `strategy/_rec_parser.py`, `data/setup_history.py`, `ui/setup_builder_ui.py`. A hard gate between the AI's raw setup output and what the driver can see/apply. NEW RECOMMENDATION LIFECYCLE with explicit statuses (generated, validation_failed, retry_requested, retry_failed, approved, approved_with_warnings, fallback_generated, blocked_no_safe_recommendation; `APPROVED_STATUSES = {approved, approved_with_warnings, fallback_generated}` in `_setup_constants.py`). SINGLE FINALISATION FUNNEL `_finalise_recommendation` (both AI paths `build_setup_advice_response`/`build_combined_setup_response` route through it) → frozen `SetupRecommendationResult` dataclass, embedded into the returned JSON (recommendation_status/changes/setup_fields/rejected_changes/engineering_validation_errors/validation_warnings/fallback_used). DISPLAY SAFETY (`_display_setup_result`): "CHANGES TO MAKE IN CAR SETUP" renders ONLY when status ∈ APPROVED_STATUSES + approved_changes non-empty; Apply button HIDDEN (not disabled) unless approved-ish with non-empty approved_fields, applies approved_fields only via `SetupFormWidget.apply_ai_fields`; rejected output only in a collapsed "Rejected AI output — not for use" section, no apply path. VALIDATOR SEVERITY: `ValidationFailure(code, message, severity)` + `validate_setup_engineering_structured()` (legacy `validate_setup_engineering` still byte-identical); ANY blocking failure (safety-prefix OR structural malformed_schema/invalid_units/locked-field) forces validation_failed (retry_failed if retried) + approved_changes=[]; out-of-range is a WARNING (clamping forces the value back in range). NEW BLOCKING RULES: `snap_throttle_lsd_accel_gate`, `kerb_strike_rh_over_increment`, `gearbox_fake_field`, `gearbox_ratio_inversion`. NEW WARNING `gearbox_out_of_range` (final_drive 2.5–6.0 / gears 0.5–4.0 — invented constants pending per-car data). REAL GEARBOX FIELDS: final_drive + gear_1..gear_6 now actionable (`_CANONICAL_SETUP_PARAMS` + `_CAT_FIELDS["transmission"]`; `_normalise_changes` expands `gear_ratios:[...]`); transmission_max_speed_kmh DEMOTED to display-only (`_DISPLAY_ONLY_FIELDS`) — never actionable; `gearbox_category_mismatch` now also blocks final_drive/gear_1..6 on a preserve category. STRICT RETRY CONTRACT: `_build_retry_prompt` lists blocking codes + max deltas + forbidden fields; retry with any blocking failure → retry_failed; banner reworded "AI recommendation rejected after retry". DETERMINISTIC FALLBACK ENGINE (`_build_deterministic_fallback`) emits 1–3 real conservative validator-passing changes, else blocked_no_safe_recommendation + "run more laps". PERSISTENCE: `data/setup_history.py::save_entry` `validation_status` routes non-approved to a `_rejected_<config_id>` diagnostic bucket; DB `setup_recommendations` row carries the final lifecycle status (`_rec_parser.py`). WORDING/LOGIC: kerb_strike distinct from true floor contact (no forced RH "required"); snap_throttle_induced no longer asserts "inside rear spins" (mixed setup/driver); "top speed below target ⇒ no gearing change" leakage removed. `_ENG_SAFETY_PREFIXES` → shared `ENG_SAFETY_PREFIXES` in `_setup_constants.py`. AMENDMENT B: redundant read-only "Race Conditions (from Event Planner)" box removed from the Setup Builder header (dup of Event Planner + Home Race Setup card, same EventContext); 320px header cap lifted; `_sync_setup_builder_from_event` keeps all functional side effects. AMENDMENT C: Home "Race Setup" card now shows a Damage line (`EventContext.damage`). `tests/test_group41_validation_gate.py` (NEW, AC0–AC14); `docs/SETUP_BRAIN_UPGRADE.md` (§ Group 41), `docs/UAT_SETUP_BRAIN.md` (NEW). **Full suite 5505 pass / 8 fail / 6 skip** — the 8 fails are the SAME pre-existing frozen-allowlist guards (`ui/track_modelling_ui.py::_tm_restore_last_track`, unrelated track-modelling tech debt), zero new regressions. Test-run note: the full suite in one process can hit a flaky native PyQt teardown segfault on Windows/Py3.14; running in two halves completes clean — an environmental artifact, not a defect. Deferred: per-car gearbox ranges (promote `gearbox_out_of_range` to blocking), a human-readable DB rec store, the flaky full-suite segfault. Prior: Setup Brain Upgrade — Professional Race Engineer Diagnosis. Backend-only, no UI surface; branch `ofr2-quali-race-disciplines` (on top of OFR-2). Two production files: `strategy/setup_diagnosis.py` + `strategy/driving_advisor.py`. NEW app-side GEARING DIAGNOSIS `_classify_gearing(...)` → `gearing_diagnosis_category` ∈ {gear_too_short, gear_too_long, top_gear_power_band_limited, traction_limited_acceleration, drag_or_power_limited, limiter_limited, insufficient_data} via a priority decision table, fed by new pure `_derive_top_gear_frame_signals(frames, top_gear)` (accel_fade/peak-power-early over ~10Hz `LapStats.frames`; degrades to insufficient_data; tunable module constants). REMOVED the flawed gearing rule (the `gear_note` "Do NOT lengthen gears" block, old `DRIVER_HARD_CONSTRAINTS` #8 → now 8, and `gearbox_edit_when_preserve`) → replaced by `gearbox_category_mismatch` (blocks only insufficient_data/gear_too_long/limiter_limited or driver-flagged-good; Fuji RSR power-band now ALLOWS a gearbox change). NEW WHEELSPIN SUBTYPE `_classify_wheelspin_subtype(...)` → {both_rear_spin, snap_throttle_induced, kerb_unload_spin, gear_too_short_spin, aero_instability, mixed, insufficient_data} with honest deferrals (`inside_wheel_spin` NEVER emitted — no per-wheel slip; `rear_platform_stiffness` folds into mixed — no damper baseline; kerb_unload_spin is a kerb-count proxy). NEW `compliance_priority` bool via `_detect_compliance_priority` raises natural-freq/damping first-or-second in `_derive_tuning_priority` UNPROMPTED (stiffness/kerb terms + kerb events/lap > 2) + explicit compliance instruction in `format_diagnosis_for_prompt`. DOMINANT re-order: severe/major wheelspin outranks "consider"-band bottoming unless driver cites bottoming (new `bottoming` in `_FEEL_VOCABULARY`). LSD ANTI-OSCILLATION: `validate_setup_engineering` +`rec_history` + rule `lsd_reversal_without_evidence` (fires on an unevidenced LSD-accel reversal; skips when a `worsened` verdict backs it / no prior / no history); rec_history resolved by the CALLER from STRUCTURED `data/setup_history.json` + DB `worsened` verdict — no new config["strategy"] read. FEEDBACK CHRONOLOGY: `_get_driver_feedback_context` splits Latest vs Earlier with per-field trend tags current/improving/worsening/resolved via `DrivingAdvisor._feedback_trend_tag` — latest dominates old. SCHEMA FIX: `not-present` added to allowed `issue_classification` in both prompt builders + `_race_engineer_directives`; invalid example removed. New keys present on the conservative/error path too. `tests/test_group39_setup_brain_upgrade.py` (NEW, ~72; AC1–AC9 + frame-signal units); 4 re-pointed in `tests/test_group38_setup_diagnosis.py` (constraint 9→8, rule rename). `docs/SETUP_BRAIN_UPGRADE.md` (NEW). **Full suite 5356 pass / 6 skip / 8 fail** — the 8 fails are ALL pre-existing frozen-allowlist guards from the already-committed `ui/track_modelling_ui.py::_tm_restore_last_track` config["strategy"] consumer (unrelated track-modelling tech debt, NOT this sprint); ~72 new tests green, zero regressions. Deferred: inside_wheel_spin/rear_platform_stiffness subtypes, kerb spatial-proximity, the fragile DB `recommendation_text` LSD join, a UI readout for the new keys. Prior: OFR-2 — Separate Race vs Qualifying Telemetry Disciplines (Core split), feature-factory run. NEW `strategy/telemetry_disciplines.py` (pure; QUALIFYING peak-metrics block w/ not-measured disclaimer, RACE consistency/efficiency block, **None sentinel for every non-quali/race purpose → generic block byte-for-byte**); ai_planner session_purpose/per_lap_telemetry threading with byte-identical UNKNOWN rendering; practice_orchestrator self-resolves discipline via new `db.get_session_type` (RF1); `get_session_laps` +2 cols + `latest` param (validator caught earliest-laps bug); snapshots gain hash-excluded `discipline` (Setup+Practice); `ui/setup_builder_ui._resolve_recent_laps` wires the recent stint into build-setup (RF2). Validator fix round: practice/test-fallthrough-to-RACE (C1) + lap ordering (I1) + coverage/polish. Strategy prompts + OFR-1 untouched; no config["strategy"] reads; allowlist unchanged. `docs/OFR2_SEPARATE_DISCIPLINES.md` (NEW); 269 new tests. **Full suite 5217 pass / 6 skip / 0 fail**. Prior: OFR-1 — Between-Race Learning Loop, Loop 1 setup self-scoring; built via the /feature-factory chain with story+brief approval gates, 43-test acceptance verification, validator + fix round. NEW `data/recommendation_scoring.py` (pure: honesty-gated verdict+confidence algorithm, §6.4 formatter); session_db migration v9 (score_confidence/-1.0, score_verdict, score_details) + 6 methods incl. write-once persist_score + cross-layout guard; `ui/dashboard.py` `_trigger_scoring_pass` at both session-open sites (after-session via get_previous_session_id — approved correction; never blocks, no config["strategy"] reads) + `learning_saved` journey-step-13 gate now DB-derived; `driving_advisor` scored-block-first prompt injection (replaces free-text, exact fallback). Validator caught + fix round resolved: dead mixed-signal branch, unwired feedback bonus, a forbidden config["strategy"] read (removed; allowlist scan EXTENDED to driving_advisor — 15 pre-existing bridge entries frozen). `docs/OFR1_BETWEEN_RACE_LEARNING.md` (NEW); 171 new tests. **Full suite 4948 pass / 6 skip / 0 fail**. Prior: Fan-Out Rule-Cache Deletion — **the audit's original SSOT violation is deleted**. `_fanout_event_to_strategy` no longer writes the 12 event-RULE fields (wear/fuel_mult/stops/weather/damage/refuel/req+avail tyres/mandatory_compounds/bop/tuning/categories) — every consumer reads rules DB-first (EventContext per-field; `config["events"]` mirror covers no-DB; AI CONTEXTS ignores legacy rule keys; match-key reads core only — all proven by invisibility tests). The helper now writes only the working-config core (track/race format/lengths/event_id). Touch-ups: the redundant writer-internal permission call deleted (sync applies from the just-saved DB event); advisor no-DB fallback hardened via `_active_event()`. Residual edge documented (ancient NULL-column rows; healed by re-save). `docs/FANOUT_RULE_CACHE_DELETION.md` (NEW); `tests/test_fanout_rule_cache_deletion.py` (NEW, 16); 12 legacy pins updated in place (invariant: rules NOT cached, DB-only). Remaining `config["strategy"]`: working-config core writer, plan-state persistence, bridges, cosmetics — all allowlisted. **Full suite 4777 pass / 6 skip / 0 fail**. **Recommended next: product work (OFR-1).** Prior: Working Race Config Read Model — retirement-map item 3, reader half (product decision; writer half deferred with item 4). NEW `data/working_race_config.py` (pure — `WorkingRaceConfig` frozen: track/car/raw race_type/total_laps default 25/race_duration_minutes default 60 (the hash's own defaults)/stored config_id; `from_strategy()` verbatim + documented garbage-coercion hardening; **owns the match-key algorithm** `compute_config_id()` — still frozen by the 6b golden vectors through the dashboard delegation; `length_text()`). Migrated via new `_working_race_config()` bridge builder: `_compute_race_config_id` (delegates), `_update_race_config` label+snapshot reads (write stays), `_sync_strategy_from_event` no-event checks, `_save_session_to_db` tagging. Allowlist net −3 direct readers. Reader side of the entire consolidation COMPLETE. `docs/WORKING_RACE_CONFIG.md` (NEW); `tests/test_working_race_config.py` (NEW, 25); hash-vector suite updated in place (full delegated path). **Full suite 4760 pass / 6 skip / 0 fail**. Prior: Legacy Fan-Out Removal Phase 6b — config_id Hash Byte-Stability Proof. Purely additive tests+docs; no production code changed. Retirement-map item 2 delivered as proof+pins; migration provably BLOCKED by restore-divergence (`_load_session_config` deliberately desyncs the working config from the active event so the id follows the RESTORED session; DB-first EventContext would break it) → folds into items 3/4. `tests/test_race_config_id_hash.py` (NEW, 17 — 5 frozen golden vectors through the REAL `_compute_race_config_id` incl. the field-observed `'||l25'→05e6d2f288`; DEFAULT_CONFIG pin; shape/stability/sensitivity + l25/t60 defaults; source-level algorithm pin; in-sync EventContext equivalence + the restore-divergence proof; allowlist/Home/guardrail invariants). `docs/LEGACY_FANOUT_PHASE_6B.md` (NEW). **Full suite 4738 pass / 6 skip / 0 fail**. Prior: SessionContext Real Connection Signal. `MainWindow(udp_listener=...)` NEW param (duck-typed `.connected`/`.total_received`/`.parse_errors`/`.packet_rate` — real `UDPListener` properties, packet-timeout based; GIL-atomic reads, no locks); `main()` passes the listener. `_build_session_context` sources connected+packet_count from the listener when wired (legacy tracker fallbacks retained verbatim — the 25 existing SessionContext tests pass unchanged) → **Home `live_active`, journey step-12 gate, and telemetry labels are now REAL**. Also fixed a wider latent bug: `_update_telemetry_labels` read FOUR phantom tracker attrs (panel frozen at "Disconnected / 0 / — Hz") — now the listener's real stats. `tests/test_session_connection_signal.py` (NEW, 18); docs in `SESSION_CONTEXT_MIGRATION.md` §5a. **Full suite 4721 pass / 6 skip / 0 fail**. Prior: Legacy Fan-Out Removal Phase 6a — Dispatcher SessionTag Snapshot. Retirement-map item 1: the telemetry pipeline no longer touches `config["strategy"]` at runtime. NEW `data/session_context.SessionTag` (frozen track/car/config_id/event_id; `from_strategy()` verbatim legacy reads; immutable → GIL-atomic swap, no lock between UI/dispatcher threads). `EventDispatcher`: seeds the tag at construction (single remaining main.py bridge read, allowlisted `__init__`×1 replacing `_dispatch`×2), `set_session_tag()` None-safe swap, `_dispatch` reads only the tag (per-lap write_lap event_id + fallback race-session open), `self._config` removed. `MainWindow._push_session_tag()` (EventContext + `_active_config_id()`, byte-identical) pushed at end of `_update_race_config` (Set-as-Active/garage/restore all funnel through), `_on_event_save` re-sync branch (after the fan-out write), end of `__init__`. Dead-code note pinned: the old fallback `strat.get("track","Unknown")` default never fired (DEFAULT_CONFIG materialises track=""). `docs/LEGACY_FANOUT_PHASE_6A.md` (NEW); `tests/test_legacy_fanout_phase_6a.py` (NEW, 21 — incl. the REAL dispatcher exercised thread-less); Phase 5 FROZEN_ALLOWLIST consciously shrunk. **Full suite 4703 pass / 6 skip / 0 fail**. Prior: Legacy Fan-Out Removal Phase 5 — Functional Readers + Frozen Allowlist Guard. Scope (product decision "Functional + guard"; full writer retirement re-audited and BLOCKED — telemetry-path `main.py _dispatch` reads, the `_compute_race_config_id` hash, restore writers, plan-state persistence, bridges — mapped for Phase 6). Migrated byte-identically: live-session open tagging in `_on_live_mode_changed` (EventContext track/car/event_id + StrategyContext config_id), degradation params (EventContext wear + StrategyContext consecutive-laps), BoP checks (`_get_bop_data_for_car` + reload gate → `bop_enabled`/`car`), `_current_setup_dict` event-identity fields (car/track/weather/bop; voice-thread-safe — SessionDB check_same_thread=False+lock), setup-save event_id. **No product decision reads the legacy dict any more.** NEW frozen allowlist (`tests/test_legacy_fanout_phase_5.py::FROZEN_ALLOWLIST`) pins all 41 remaining access sites (annotated writer/bridge/hash/plan/restore/cosmetic/telemetry) — exact-equality scan blocks new consumers AND silent removals. `docs/LEGACY_FANOUT_PHASE_5.md` (NEW, incl. the Phase 6 retirement map); 15 new tests; `test_group4_fixes` BoP pins ×2 updated (source now EventContext; "never a widget" invariant unchanged). **Full suite 4682 pass / 6 skip / 0 fail**. Prior: Legacy Fan-Out Removal Phase 4 — Divergence Elimination + Last Readers. NEW `dashboard._fanout_event_to_strategy(evt_name)` — the Set-as-Active fan-out block extracted verbatim (event-RULE fields only; config-dict only, no tracker/advisor/sync/persist side effects; never touches car/config_id/stops); `_on_event_set_active` behaviour unchanged; **`_on_event_save` re-syncs the fan-out when the saved event IS the active event** → the DB record and `config["strategy"]` can no longer diverge (activation side effects stay Set-as-Active-only). Last readers migrated byte-identically: `_get_mandatory_compounds` (EventContext.required_tyres codes → names via data.tyres.get_by_code — the writer's own mapping), setup-tab refuel/req/avail labels + car rebind — `_sync_setup_builder_from_event` reads NO config["strategy"] (dead `sc` removed). Writer retirement investigated + deferred to Phase 5 (car/config_id/stint plan live only in the fan-out; ~25 readers remain — with re-sync it can't go stale, so retirement is mechanical). `docs/LEGACY_FANOUT_PHASE_4.md` (NEW); `tests/test_legacy_fanout_phase_4.py` (NEW, 18; 11 legacy pins updated in place — group7 ×7, group12a ×3, group4 ×1 + Phase 1/2/3 writer pins). **Full suite 4667 pass / 6 skip / 0 fail**. Prior: Legacy Fan-Out Removal Phase 3 — Functional Gating / Validation Migration. Scope (explicit product sign-off: "flip reads only"): the two remaining FUNCTIONAL `config["strategy"]` consumers now read DB-first EventContext — setup-permission gating (`_sync_setup_builder_from_event` feeds `_on_bop_toggled`/`_apply_setup_permissions` from `ev_ctx.bop_enabled`/`.tuning_allowed`/`.allowed_tuning_categories`; gating logic unchanged) and the DEF-P3-012 strategy-options tuning validation (`_strat_locked`/`_strat_allowed` from EventContext). `_on_event_set_active`'s writer-internal permission call deliberately still reads the just-written `strat` (fresh by construction, pinned). Byte-identical in-sync; in the diverged (edited-not-reactivated) case the signed-off change: editable fields + validation follow DB truth — READER CONSISTENCY COMPLETE (AI inputs, labels, gating, validation all DB-first). Fan-out writers untouched (Phase 4). `docs/LEGACY_FANOUT_PHASE_3.md` (NEW); `tests/test_legacy_fanout_phase_3.py` (NEW, 20; 2 Phase 2 pins updated in place). **Full suite 4649 pass / 6 skip / 0 fail**. Prior: Legacy Fan-Out Removal Phase 2 — Event-Rule Display-Label Migration. Scope (user-chosen): DISPLAY LABELS ONLY — the Strategy/Setup event-context readout labels now reflect DB-first EventContext (consistent with the AI inputs, which already read DB-first); functional paths (setup-permission gating `_apply_setup_permissions`, BoP toggle `_on_bop_toggled`, spinbox rebind) still read `config["strategy"]`, so which fields are editable is unchanged. Byte-identical when the DB event and fan-out are in sync (all event fields are integer QSpinBox values → migrated labels wrap int() so "2×" stays "2×"; race_type normalisation handles the DB "Timed Race" vs config "timed"). Migrated `dashboard._sync_strategy_from_event` (`_lbl_strategy_event_ctx` line + `_lbl_fuel_mult_display`) and `setup_builder._sync_setup_builder_from_event` (`_lbl_setup_event_ctx` + `_lbl_rc_*` race_type/length/fuel/wear/mand_pits/weather/damage/bop/tuning labels) via one `ev_ctx = self._build_event_context()`; left refuel/req/avail labels + all functional reads on the fan-out. Behaviour change (documented+tested): edited-but-not-reactivated event → labels show fresh DB truth; editable fields unchanged. `docs/LEGACY_FANOUT_PHASE_2.md` (NEW); `tests/test_legacy_fanout_phase_2.py` (NEW, 15). **Full suite 4629 pass / 6 skip / 0 fail**. Prior: SessionContext / TelemetryContext. New `data/session_context.py` (pure — telemetry-layer canonical read model, peer of Event/Strategy/Setup/Track contexts: `connected`/`packet_count`/`laps_recorded`/`active_session_id`/`is_recording`/`live_active`/`live_mode`/`telemetry_avg_fuel_per_lap`/`fuel_burn_per_lap`+`fuel_burn_source`/`has_practice_laps`/`has_valid_laps`/`source`; `build_session_context` never raises; `flow_flags` bridge). `ui/dashboard.py` `_build_session_context()` helper (safe tracker getters + config fuel-fallback bridge + live mode); migrated `_computed_fuel_burn_lpl` (→ `SessionContext.fuel_burn_per_lap`, byte-identical 3-tier fallback — the config["strategy"] fuel read now lives only in the builder), `_build_home_dashboard_state` (live_active/has_practice_laps/has_valid_laps via SessionContext), `_refresh_telemetry_context` (connection/packet/recording/telemetry-fuel labels). Additive + byte-identical; no telemetry/PTT/voice/live/setup/strategy/track/AI/tab-order change; config["strategy"] + both fan-out writers preserved. `docs/SESSION_CONTEXT_MIGRATION.md` (NEW); `tests/test_session_context.py` (NEW, 25). **Full suite 4614 pass / 6 skip / 0 fail**. Prior: Legacy Fan-Out Removal Phase 1 — Read-Only Consumer Migration. Migrates low-risk read-only `config["strategy"]` consumers to canonical read models, each byte-identical (no behaviour change; `config["strategy"]` + both fan-out writers preserved). `config_id` → `StrategyContext.config_id` via new `dashboard._active_config_id()` (setup_builder history combo/select + 2 history-save keys; 0 raw config_id reads left in setup_builder_ui.py); `car` → `EventContext.car` in `dashboard._sync_practice_from_event`. DB-first event-rule reads (track/wear/fuel_mult/tuning/bop/length) deferred to Phase 2 (not byte-identical). `docs/LEGACY_FANOUT_PHASE_1.md` (NEW — full classification of every remaining reader). `tests/test_legacy_fanout_phase_1.py` (NEW, 22). **Full suite 4589 pass / 6 skip / 0 fail**. Prior: Config Safety Guardrails. New `config_paths.py` (pure — single owner of config path resolution + IO + a test guardrail; `DEFAULT_CONFIG` moved here from `main.py` and re-exported, now materialises `strategy.degradation_consecutive_laps: 2`; `resolve_config_path` precedence `--config`→`NGR_CONFIG_PATH`→`config.json`; `is_test_environment`/`is_real_config_path`/`real_config_access_blocked`; `load_config` refuses to READ real config under tests→defaults; `save_config` refuses to WRITE real config under tests→`ConfigSafetyError`, serialise-first + `.bak` + atomic `os.replace`; `write_default_config`). `main.py` uses `resolve_config_path`. `ui/dashboard.py` `_persist_config` delegates to guarded atomic `save_config` (catches `ConfigSafetyError`, never crashes; normal runs unchanged). `.gitignore` +`config.json.bak`/`.tmp`. `tests/conftest.py` (NEW — `temp_config_path` fixture + session-autouse `_guard_real_config` SHA-256 net). Safety/test-isolation only — no setup/strategy/track/AI/telemetry/PTT/voice/workflow change; `config["strategy"]`+fan-outs untouched. `docs/CONFIG_SAFETY_GUARDRAILS.md` (NEW); `tests/test_config_safety_guardrails.py` + `tests/test_config_safety_smoke.py` (NEW, 34). **Full suite 4567 pass / 6 skip / 0 fail**. Prior: Home Dashboard Promotion — Move Home to Index 0 and Add Click Navigation. `ui/tab_registry.py` `DEFAULT_TAB_ORDER` now leads with `TAB_HOME` (comments renumbered 0–13; every non-Home tab keeps its relative order, +1 each) — order-only edit, the positional registry re-derives indices. `ui/dashboard.py`: Home `addTab` moved first; `select_tab(TAB_HOME)` at end of `_setup_ui` (open on Home by key); one guarded `_home_refresh()` at end of `__init__` (first render); `_build_home_tab` adds per-card "Open <Tab>" buttons + a next-action button; new `_home_navigate`/`_home_navigate_next_action`/`_home_update_next_action_button`/`_home_nav_button_text`. `ui/home_dashboard_vm.py`: `CARD_TAB_KEYS` + `tab_key_for_card()` (stable keys only, still no PyQt6). Navigation is tab-change only (`select_tab`, `has_tab`-guarded); no new raw index; `select_tab` still the only `setCurrentIndex` site; no setup/strategy/track/AI/telemetry/PTT/voice/persistence/fan-out change. `docs/HOME_DASHBOARD_PROMOTION.md` (NEW); `tests/test_home_dashboard_promotion.py` (NEW) + 4 order-pinning suites updated in place. **Full suite 4533 pass / 6 skip / 0 fail**. Prior: Tab Navigation Refactor — Named Tab Lookup. New `ui/tab_registry.py` (pure — stable keys `TAB_LIVE`…`TAB_HOME`, `DEFAULT_TAB_ORDER` mirrors the addTab order with a source-scan pairing test + runtime count guard, never-raising `TabRegistry`, ⚙-decoration-safe `key_for_title`). `ui/dashboard.py`: `_on_tab_changed` dispatches by stable key (zero `index == N` comparisons, same 8 behaviours); helpers `get_tab_index`/`has_tab`/`current_tab_key`/`select_tab` (only `setCurrentIndex` site); 3 jumps + 2 visibility guards migrated to keys; `_home_tab_index` retired. Tab order byte-identical, Home at 13, no logic change anywhere. **Full suite 4512 pass / 6 skip / 0 fail** (33 new tests; 6 updated in place). Prior: Diagnostic Tab Cleanup — Low-Risk UI Dags Removal. DELETED the 7 hidden never-connected legacy per-segment review buttons + unreachable handlers + dead imports (`ui/track_modelling_ui.py`; pure review functions retained) and the dead never-rendered `_TELEMETRY_REFERENCE_HTML` constant; RENAMED "Race Config ID"→"Session Match Key", Diagnostics "Rem(clk)"/"rem_ms(raw)"/"Ann queue"→"Time left:"/"remaining_time_ms:"/"Voice queue:", window title + Guide h1 → "Next Gear Racing Pit Crew"; Guide fixed (phantom "Dashboard" Step 8 → real Home tab, API-key bullet → Strategy Builder, new ⚙ tool-tabs note, pip-install tooltip removed). No tab order/logic/prompt/fan-out change (pinned by tests). `docs/DIAGNOSTIC_TAB_CLEANUP.md` (NEW). **Full suite 4479 pass / 6 skip / 0 fail** (25 new tests; group24 `_tm_` floor 54→46). Prior: Home Dashboard Build — Race Engineer Command Centre. New `ui/home_dashboard_vm.py` (pure display view model — five cards: Race Setup / Track Intelligence / Setup Brain / Strategy Brain / AI Input Safety, plus the Next Best Action from `build_flow_state_summary()`; never raises; reads only the four canonical contexts + AI snapshot core) rendered by a new **Home tab appended at index 13** in `ui/dashboard.py` (indices 0–12 unchanged; refresh on tab-shown + guarded hooks; no polling/workers; display-only — home methods write nothing, source-scanned). Plain-English stale indicators (setup-vs-event, setup-vs-strategy, strategy-vs-event, track mismatch, AI legacy fallback) now visible in the UI. `docs/HOME_DASHBOARD_BUILD.md` (NEW). **Full suite 4454 pass / 6 skip / 0 fail** (52 new tests). Prior: AI Snapshot Migration — Frozen Context Inputs. New `data/ai_context_snapshot.py` (frozen, owner-documented AI-input snapshots composing the four canonical contexts: `StrategyAISnapshot`/`PracticeAnalysisSnapshot` race-params + `SetupAISnapshot`; byte-identical to the legacy expressions when stores are in sync — proven incl. a byte-identical `_build_race_prompt` text test; fresh DB event values supersede stale fan-out copies as the documented intentional difference; staleness detected at build time) + `docs/AI_SNAPSHOT_MIGRATION.md`. Migrated: `_assemble_strategy_inputs`, `_run_ai_analysis`, `_run_practice_analysis` (DEF-P1-005 default preserved), `_run_build_setup` (+ worker-thread rec metadata frozen), `_setup_analyse_ai`. No prompt wording/intelligence changed; legacy stores retained. **Full suite 4402 pass / 6 skip / 0 fail** (41 new tests; 20 legacy scans updated). Prior: State Consolidation 4 — TrackContext (`data/track_context.py`, 68 tests); 3 — SetupContext; 2 — StrategyContext; 1 — EventContext)
> Read this file first, then MASTER_TESTING_REGISTER.md, before touching any code.
>
> Note: this file's group table below was authored through Group 17U. Groups 17V–38, the
> lettered groups (A/B/C/D/E), Qualifying Mode, and the Setup Brain + Strategy Outcome
> integration are catalogued in `MASTER_TESTING_REGISTER.md` (see its "Groups 26–38 +
> Lettered Groups" and "Integration" sections) and in `docs/CURRENT_CLAUDE_HANDOFF.md`.
> The build-status total below is current (4053).

---

## Current Build Status

| Metric | Value |
|--------|-------|
| Automated tests | Group 46 adds 6 new `tests/test_group46_{learning_persistence, fuel_influence, baseline_session_modifiers, per_gear, porsche_pack, ui_explainability}.py` (122 tests, incl. the AC37 RSR/Fuji integrated regression, a fuel-renders-into-evidence test, and an AC16 single-winner-per-field learning-safety assertion); reconciled version/schema tests — `RULE_ENGINE_VERSION`→46.0 (`test_group42_rule_first_engine`), DB version→12 (`test_group42_legacy_storage`, `test_group18b_rec_persistence`, `test_session_db`, `test_group18e_setup_history`). All Group 46 tests pass; the ~7–20 pre-existing frozen-allowlist / OFR failures are KNOWN, unrelated, and untouched. NOTE: the full suite in one process can hit a flaky native PyQt teardown segfault on Windows/Py3.14; run IN HALVES (or by group) |
| Latest work | Setup Brain Learning & Race Context Intelligence — Group 46 (`data/session_db.py` v12, `strategy/driving_advisor.py`, `strategy/setup_rule_engine.py`, `strategy/setup_diagnosis.py`, `strategy/setup_baseline.py`, `strategy/_setup_constants.py`, `ui/dashboard.py`, `ui/setup_builder_ui.py`; `docs/RULE_FIRST_SETUP_BRAIN.md` § 15 "Setup Brain Learning & Race Context (Group 46)", `docs/SETUP_BRAIN_UPGRADE.md` § Group 46, `docs/UAT_SETUP_BRAIN.md` § Group 46) — cross-session learning persistence + feed (NEW SQLite `learning_outcomes` table via `_migrate_v12`; DELIBERATELY not in `setup_history.json`; feed into a real `RuleOutcomeStore` → confidence UPGRADE `>=0.60`/DOWNGRADE `<0.40`, one step, validator-gated, cannot un-block/un-reject/author) + fuel-multiplier influence (Analyse; confidence/ranking only, no new deltas, renders into `evidence`) + session-specific NUMERICAL baseline tuning (`_SESSION_BIAS_TABLE` + `duration_mins` param; `session_changed` honesty gate) + fuller per-gear intelligence (REAL `wheelspin_by_gear` detection, per-lap; `_emit_per_gear_changes` only on real indexed evidence; `bog_by_gear`/`lockups_by_gear` honestly None) + Porsche RSR extension (Pack P auto-benefits; AC37 benchmark verified). Architecture preserved (rule-first, AI audit-only, one funnel, works with AI disabled, Apply gate unchanged). `RULE_ENGINE_VERSION`="46.0"; DB `user_version`=12; `HIGH_FUEL_MULTIPLIER_THRESHOLD=5.0`; `HIGH_SUCCESS_RATE=0.60` — backend + UI + DB; `tests/test_group46_*.py` (6 files, 122) + reconciled version/schema tests, zero new regressions |
| Prior work | Setup Brain Intelligence Expansion — Group 45 — `tests/test_group45_*.py` (8 files) + 3 reconciled. Rule-First From-Scratch Setup Baseline Generator — Group 44 — `tests/test_group44_*.py` (86 + 64). Rule-First Setup Brain — Group 42 — `tests/test_group42_*.py` (136 new + 17 rewritten). Setup Builder Engineering Validation Gate — Group 41 — `tests/test_group41_validation_gate.py` (AC0–AC14). Setup Brain Upgrade — Professional Race Engineer Diagnosis — ~72 tests (+4 re-pointed). OFR-2 Separate Race/Quali Telemetry Disciplines — 269 tests. OFR-1 Between-Race Learning Loop — 171. Tab Navigation Refactor — 33 (+6 updated). Diagnostic Tab Cleanup — 25 tests. Home Dashboard Build — 52. AI Snapshot Migration — 41 (+20 legacy scans updated). State Consolidation 4 — TrackContext — 68. 3 — SetupContext — 67. 2 — StrategyContext — 53. 1 — EventContext — 38. Product Consolidation — 27 |
| Latest fix | DEF-17U-UAT-007 — Time Trial calibration builds correctly; pit-in detection OFF by default, new PARTIAL_START/STOP lap quality (`data/track_calibration.py`, `ui/track_modelling_vm.py`, `ui/track_modelling_ui.py`, `data/track_segment_detection.py`) |
| Latest group | Group 18A — Track Truth foundation (`data/track_truth.py`, `track_truth_matcher.py`, `track_truth_calibration.py`) — 45 new tests |
| Last test run | 2026-07-03, DEF-17U-UAT-007 — new tests `test_def17u_uat007_calibration_build.py` (~35), `test_def17u_uat007_partial_laps.py` (44) |
| App launches | Yes (no startup crash) |

---

## Architecture Stabilisation — Group Status

| Group | Focus | Status | Key Defects |
|-------|-------|--------|-------------|
| Group 1 | Crash fixes | Complete + UAT Passed | DEF-P1-003, DEF-P1-004, DEF-P1-008 |
| Group 2 | AI prompt accuracy + data persistence | Complete — UAT: Groups 2–4 failed, Groups 1+5 passed | DEF-P1-005 through DEF-P2-016 |
| Group 3 | Session reload accuracy | Complete — Partially fixed: DEF-P2-013, DEF-P2-014 re-opened | DEF-P1-002, DEF-P2-011–DEF-P2-014 |
| Group 4 | BoP and tuning permissions | Complete — Partially fixed: DEF-P1-005 blocked by Root Cause A | DEF-P2-004–DEF-P2-008 |
| Group 5 | Live mode + voice guards | Complete + UAT Passed | DEF-P2-001, DEF-P2-002, DEF-P2-008, DEF-P2-QRF |
| Group 6 | UI placement + data quality | Complete + AWR pending | DEF-P2-010, DEF-P2-015 |
| Group 7 | Event persistence (Root Cause A) | **Complete — Awaiting Retest** | DEF-P1-009 |
| Group 8 | Session reload mapping (Root Cause B) | **Complete — Awaiting Retest** | DEF-P2-013, DEF-P2-014, DEF-P2-009 |
| Group 9 | AI Debug / Log visibility (Root Cause C) | **Complete — Awaiting Retest** | DEF-P1-010 |
| Group 10 | AI Prompt BoP Context (test coverage only) | **Complete — Awaiting Retest** | DEF-P1-005, DEF-P2-007, DEF-P2-016 |
| Group 11 | UI Display Fixes (smoke test regressions) | **Complete — AWR-040/041 pending retest** | DEF-P1-011, DEF-P2-021 |
| Group 12 | BoP tuning fix + History mapping investigation + AI Log display | **Complete — AWR-042/043/044 pending runtime** | DEF-P1-005 ✅, DEF-P2-013/014 ✅, DEF-P2-021 ✅, DEF-P2-022 closed |
| Group 13 | Live session defects: pit detection, save-session, compound propagation, live tyre | **Complete — AWR-045–049 pending runtime** | DEF-P2-023 ✅, DEF-P2-024 ✅, DEF-P2-025 ✅, DEF-P2-019/026 ✅, DEF-P2-020/027 ✅ |
| Group 14 | UAT no-go remediation: BoP prompt, None stats, session dupe, qualifying PIT_EXIT, AI log timer, UTC timestamp, Garage setups | **Complete — AWR-050–057 pending runtime** | DEF-P1-012 ✅, DEF-P2-029 ✅, DEF-P2-030 ✅, DEF-P2-031 ✅, DEF-P2-032 ✅ (regression), DEF-P2-033 ✅, DEF-P2-034 ✅, DEF-P2-035 ✅ |
| Group 15 | AI context remediation: RaceParams bop/avail_tyres, practice worker car_id, PTT car context, practice BoP/feedback/prev-AI, race prompt timed race, build_car_setup race context, data quality note, strategy validation | **Runtime-validated 2026-06-23 — AWR-058–067/069 CLOSED; AWR-063/068 blocked by DEF-P3-013 (fixed Group 15A)** | DEF-P1-013 ✅, DEF-P1-014 ✅, DEF-P2-036 ✅, DEF-P2-037 ✅, DEF-P2-038 ✅, DEF-P2-039 ✅, DEF-P2-040 ✅, DEF-P2-041 ✅ (via 15A), DEF-P3-009 ✅, DEF-P3-010 ✅, DEF-P3-011 ✅, DEF-P3-012 ✅ |
| Group 15A | DEF-P3-013 fix: AILogEntry car_id/track fields + threading through all AI callers | **Complete 2026-06-23 — AWR-063 CLOSED; 56 tests pass** | DEF-P3-013 ✅ |
| Group 16 | Phase 2: Per-lap telemetry in practice prompt (2-A), fuel sequence trend in strategy prompt (2-B), compound degradation sequences in strategy prompt (2-C), tyre temp per lap schema migration v3 + recorder + DB (2-D) | **Complete 2026-06-23 — 74 tests pass** | — |
| Group 17 (user: Group 16) | Corner-Level Telemetry Learning: CornerIssue model, PATH A (event_positions_json), PATH B (frame-level), fix verification, setup advice bridge, AI prompt injection, schema v4 (corner_issues table) | **Complete 2026-06-23 — 64 tests pass** | — |
| Group 18 | DEF-P3-014: Startup residual strategy/race config activation — removed auto-set_plan() and auto-set_race_config() from main.py startup; removed tracker push from _update_race_config(); fixed telemetry.tracker → telemetry.state import in _on_event_set_active() | **Complete 2026-06-23 — 21 tests pass** | DEF-P3-014 ✅ |
| Group 17A | Track Intelligence Seed Loader: `data/track_intelligence.py` — TrackModellingStatus enum (9 values + helper methods), typed dataclasses, seed loader with validation, 5 query functions, AI prompt context helper, caching | **Complete 2026-06-24 — 63 tests pass** | — |
| Group 17B | Track Modelling UI tab (tab #12): `ui/track_modelling_vm.py` view model (11 functions, no PyQt6), `_build_track_modelling_tab()` in dashboard.py — QSplitter with search, cascaded combos, 5 detail panels (facts/readiness/car/prompt), lazy seed load on tab show | **Complete 2026-06-24 — 101 tests pass** | — |
| Group 17C | Calibration Lap Capture + Reference Path Builder: `data/track_calibration.py` — TelemetrySample, CalibrationLap, CalibrationSession, ReferencePathPoint, ReferencePath, CalibrationBuildResult, quality evaluator (7 reject rules), 6 distance/progress helpers, `build_reference_path()`, JSON export/import; disabled UI placeholders in Track Modelling tab | **Complete 2026-06-24 — 102 tests pass** | — |
| Group 17D | Live Telemetry Calibration Session Wiring: `data/track_calibration_runtime.py` — `can_capture_calibration_sample`, `infer_lap_number`, `packet_to_calibration_sample` adapter, `CalibrationCaptureState` enum, `TrackCalibrationCaptureController` state machine (5 states, 7 public methods, 5 button-state properties); `SignalBridge.calibration_packet` signal; 4 live buttons + 5 status labels in Track Modelling tab; `_cal_pkt_counter` 10 Hz subsampling in `main.py` | **Complete 2026-06-24 — 81 tests pass** | — |
| Group 17E | Automatic Track Segment Detection: `data/track_segment_detection.py` — 3 enums (`TrackSegmentType` 12 values, `TrackSegmentDirection`, `TrackSegmentDetectionConfidence`), 3 dataclasses (`DetectedTrackSegment`, `SegmentDetectionResult`, `SegmentDetectionConfig`), 7 private helpers, per-lap detection (`detect_segments_from_lap`), multi-lap aggregation (`detect_track_segments`), corner numbering (`assign_corner_numbers`), 4 auxiliary detectors (limiter, kerb, gear, fuel-save), JSON export/import; "Detect Segments" button + 3 status labels in Track Modelling tab | **Complete 2026-06-24 — 99 tests pass** | — |
| Group 17F | Segment Review and Track Model Approval: `data/track_segment_review.py` — 2 enums (`SegmentReviewStatus` 8 values, `SegmentReviewAction` 7 values), 2 dataclasses (`ReviewedTrackSegment`, `TrackModelReviewResult`), 7 action functions, `review_completion_pct`, `is_ai_ready` (5-blocker rule set), JSON export/import; 3 view-model helpers in `track_modelling_vm.py`; Segment Review QGroupBox (8-col table, 6 action buttons, save), Review Approval QGroupBox (7 stat labels) in Track Modelling tab | **Complete 2026-06-24 — 122 tests pass** | — |
| Group 17G | Approved Track Model Resolver and Modelling Status Promotion: `data/track_model_resolver.py` — 2 enums (`TrackModelSourceType` 6 values, `TrackModelResolutionStatus` 6 values), 2 dataclasses (`ResolvedTrackModel`, `TrackModelResolverResult`), 4 discovery functions, `resolve_best_track_model` (priority: engineer_validated > ai_ready > reviewed > seed > missing), `build_resolved_track_context_for_prompt` (not yet wired to AI); `modelling_status` added to reviewed JSON schema (backward-compatible); `format_resolver_summary` in vm; "Resolver Status" QGroupBox in Track Modelling tab (refreshes on layout select + after save) | **Complete 2026-06-24 — 68 tests pass** | — |
| Group 17H | Track Intelligence AI Prompt Integration: `strategy/track_context_prompt.py` — `get_track_context_for_ai(loc_id, lay_id)` helper (never raises, delegates to resolver); `track_location_id`/`layout_id` fields added to `RaceParams`; track context injected into `_build_race_prompt`, `_build_practice_prompt`, `_build_setup_from_scratch_prompt`, `build_car_setup`, `DrivingAdvisor._build_coaching_prompt`, `_build_setup_prompt`, `_build_combined_prompt`; `config["strategy"]["track_location_id"/"layout_id"]` set by Track Modelling tab selection; structured_payload debug metadata includes `track_context_included` flag; seed-only/not-AI-ready/missing context all preserved with appropriate warnings | **Complete 2026-06-24 — 56 tests pass** | — |
| Group 17I | Telemetry Issue to Segment Enrichment: `data/track_issue_enrichment.py` — `TrackIssueType` (10), `TrackIssuePhase` (7), `TrackIssueEnrichmentConfidence` (4) enums; `RawTelemetryIssue`, `EnrichedTelemetryIssue`, `TrackIssueEnrichmentResult` dataclasses; `enrich_telemetry_issues()` with priority matching (segment_id > lap_progress > distance > XYZ nearest > nearest midpoint > unresolved); confidence rules (model source × segment status × match method); deterministic implication mapping for 7 issue+segment combinations; `issues_from_lap_stats()` adapter (5 position list types); `issues_from_corner_issues()` adapter (decodes corner_id grid buckets); `summarise_enriched_issues_for_prompt()` (never invents corner names for unresolved); `DrivingAdvisor._get_enriched_issue_context()` wired into coaching/setup/combined prompts | **Complete 2026-06-24 — 76 tests pass** | — |
| Group 17J | Live Current Segment Resolver: `data/live_segment_resolver.py` — `LiveSegmentResolutionConfidence` (4), `LiveSegmentResolutionStatus` (6) enums; `LivePosition`, `LiveSegmentMatch`, `LiveSegmentResolverResult`, `LiveSegmentResolverConfig` dataclasses; `resolve_live_segment()` with priority matching (segment_id exact > lap_progress range > distance_along_lap_m via ref path > XYZ nearest via ref path > nearest midpoint > unresolved); GT7 limitations documented (no native lap_progress, road_distance is absolute — not used as distance_along_lap_m); `packet_to_live_position()` adapter (duck-typed, guards paused/loading/off-track/zero-xyz, never raises); `format_live_segment_for_engineer()` (compact wording, no invented names); `get_live_segment_context_for_prompt()` (AI block, returns "" for no_reviewed_model); `DrivingAdvisor._get_live_segment_context(live_position=None)` wired into coaching/setup/combined prompts via optional live_position param | **Complete 2026-06-24 — 78 tests pass** | — |
| Group 17K | Segment-Aware Live Coaching Rules: `data/live_segment_coaching.py` — `LiveCoachingCueType` (13 values incl. no_call), `LiveCoachingPriority` (4), `LiveCoachingSuppressionReason` (12) enums; `LiveCoachingCue`, `LiveCoachingDecision`, `LiveCoachingConfig` dataclasses; 25-entry cue template table covering brake_lock/wheelspin/oversteer/understeer/poor_exit_drive/wrong_gear/limiter_hit/fuel_save/tyre_wear (exact + fallback segment-type rules); `build_live_coaching_decision()` — deterministic rule engine with confidence gate (suppress on LOW/UNKNOWN by default), segment quality gate (seed_only/rejected/needs_more_laps → suppress), repetition gate (min_issue_repetitions=2), cooldown (suppress same cue_type+segment within N laps), max_cues_per_lap cap; `format_live_coaching_for_prompt()` (returns "" when suppressed, compact block with basis when cue fires); `get_live_coaching_debug_metadata()` (4 debug fields); `DrivingAdvisor._get_live_coaching_context(live_position, laps)` wired into coaching prompt extra_sections | **Complete 2026-06-24 — 78 tests pass** | — |
| Group 17L | Lap-Start Offset Calibration and Road-Distance Mapping: `data/lap_distance_mapper.py` — `LapDistanceMappingStatus` (6), `LapDistanceMappingConfidence` (4) enums; `LapStartOffsetCalibration`, `LapDistanceMappingResult`, `LapDistanceMapperConfig` dataclasses; `normalise_distance()` (modulo, handles negatives), `calculate_lap_start_offset()`, `map_road_distance_to_lap_distance()`, `map_road_distance_to_lap_progress()` (0.0–1.0 clamped); `create_offset_zero()`, `create_offset_from_reference_path()`, `load_offset_calibration_for_track()`; JSON export/import (`<loc>__<lay>__lap_offset.json`); `live_segment_resolver.py` updated: `LivePosition.road_distance_m` field added, `packet_to_live_position()` populates it, `enrich_position_with_road_distance()` added, `resolve_live_segment(offset_calibration=None)` adds Priority 3 road_distance mapping (confidence downgraded when LOW/UNKNOWN calibration) | **Complete 2026-06-24 — 69 tests pass** | — |
| Group 17M | Runtime UAT and Calibration Workflow Hardening: `ui/track_modelling_vm.py` extended — `_WORKFLOW_ERROR_MESSAGES` dict (11 keys), `get_workflow_error_message()`, `get_calibration_button_states()` (15-key dict, all 5 controller states, offset buttons), `format_calibration_status_extended()` (packet age, sample count, path info), `format_lap_offset_status()` (provisional/validated, warnings, notes), `format_live_resolver_status_summary()` (multi-line status string); `data/track_modelling_runtime_check.py` — `RuntimeCheckResult` dataclass, `run_track_modelling_runtime_check()` (never raises, duck-typed); `data/lap_distance_mapper.py` — `create_offset_zero()` updated to default source="zero_offset" and validate positive length; `ui/dashboard.py` — packet age label, Lap Offset Calibration QGroupBox (Create/Load/Save buttons, status/detail/warning labels), `_tm_last_packet_time` tracking, `_tm_get_track_length_m()`, `_tm_update_packet_age_label()`, `_tm_update_offset_status()`, `_tm_create_zero_offset()`, `_tm_load_offset()`, `_tm_save_offset()`, signal connections; `docs/TRACK_MODELLING_RUNTIME_UAT.md` — 15-section manual UAT checklist | **Complete 2026-06-24 — 94 tests pass** | — |
| Group 17M UAT | DEF-17M-UAT-001/002/003 remediation: `ui/track_modelling_vm.py` — `format_lap_count_info()` (captured/quality/explanation text), `format_file_audit_status()` (saved/detail/load_status/extras text); `data/track_calibration.py` — `reference_path_filename()`, `TrackModelFileAudit` dataclass, `audit_track_model_files()` (never raises); `ui/dashboard.py` — `_tm_update_cal_status()` uses format_lap_count_info, `_tm_detect_segments()` split into outer catcher + `_tm_detect_segments_safe()` with seed_result.layouts crash fixed, `_tm_on_layout_changed()` calls `_tm_audit_and_show_saved_files()`, new `_tm_audit_and_show_saved_files()` method | **Complete 2026-06-25 — 49 tests pass** | DEF-17M-UAT-001 ✅, DEF-17M-UAT-002 ✅, DEF-17M-UAT-003 ✅ |
| Group 17N UAT-005 | DEF-17N-UAT-005 remediation — actionable rejection diagnostics: `data/track_calibration.py` — `build_reference_path()` now mutates `CalibrationLap.quality/quality_reasons` after assessment; `diagnose_calibration_session()` added; `data/track_segment_detection.py` — `_build_no_usable_laps_errors()` helper + `assess_session_laps` import; `ui/track_modelling_vm.py` — `format_build_failure_diagnostics()`, `_min_samples()`; `ui/dashboard.py` — `_tm_build_path()` uses full diagnostics dialog | **Complete 2026-06-25 — 32 tests pass** | DEF-17N-UAT-005 ✅ |
| Group 17N UAT | DEF-17N-UAT-004 remediation — Detect Segments from persisted data after restart: `data/track_calibration.py` — `calibration_laps_filename()`, `export_calibration_laps_json()` (USABLE laps + all TelemetrySample fields), `import_calibration_laps_json()` (reconstructs CalibrationSession), `TrackModelFileAudit` extended with `calibration_laps_exists/usable_count`, `can_detect_segments` property, `is_legacy_ref_path_only` property, audit + summary_line updated; `data/track_calibration_runtime.py` — `save_reference_path()` writes calibration laps JSON alongside ref path (best-effort); `ui/dashboard.py` — `_tm_detect_segments_safe()` three-path logic (active/disk/legacy), `_tm_audit_and_show_saved_files()` laps-aware enable logic and save-path label; `ui/track_modelling_vm.py` — `format_file_audit_status()` includes laps count in detail_text, distinguishes ready/legacy in load_status | **Complete 2026-06-25 — 41 tests pass** | DEF-17N-UAT-004 ✅ |
| Group 17O | Seeded 1m Track Map, Width Corridor, Map Matching, Visual Verification: `data/track_station_map.py` — `StationPoint`, `SeededCorner`, `TrackStationMap`, `resample_path_to_uniform_spacing`, `build_track_station_map`, curvature-based corner detection with placeholder filling for corners_expected guarantee, `export/import_station_map_json`; `data/track_map_matching.py` — `MapMatchConfidence`, `MapMatchResult`, `find_nearest_station_idx`, `match_position_to_map`, `is_likely_outlap`, `map_match_samples`; `data/track_width_model.py` — `WidthObservation`, `WidthEstimate`, width learning from calibration laps, `is_near_left/right_edge`, `unused_track_width_pct`; `ui/track_map_vm.py` (no PyQt6) — `MapPoint`, `CornerLabel`, `CarDot`, `TrackMapDrawData`, `build_track_map_draw_data`, `project_to_screen`; `ui/dashboard.py` — `TrackMapWidget(QWidget)` QPainter canvas, map widget on Track Modelling tab and Live tab, `_tm_try_build_station_map()`, `_tm_update_live_map_dot()` | **Complete 2026-06-25 — 76 tests pass** | DEF-17O-UAT-001/002/003 fixed (UAT Remediation Round 1) |
| Group 17O UAT Round 2 | DEF-17O-UAT-004/005/006/007/008 remediation: `ui/dashboard.py` — fixed `_tm_try_build_station_map()` seed lookup (line 2770 bug: `self._tm_seed_result.layouts` → `get_selected_layout(seed_result, loc_id, lay_id)`); added BRAKING_ZONE/TRACTION_ZONE to `_TELEMETRY_OVERLAY_SEG_TYPES` (car-specific, Porsche RSR warnings); auto-save station map JSON via `export_station_map_json()` after build; added `_tm_try_load_station_map_from_disk()` auto-loaded when layout changes; updated `_tm_lbl_build_info` to show "Path: N pts | Conf: X | Map: N stations / N corners"; updated `_tm_refresh_seg_table()` to populate Turn column from station map SeededCorner by lap_progress proximity (< 15% threshold); updated test file to add 17 new tests for DEF-17O-UAT-004/005/007/008 classes; updated existing DEF-17O-UAT-002 tests to reclassify BRAKING_ZONE/TRACTION_ZONE as overlays | **Complete 2026-06-25 — 40 tests pass** | DEF-17O-UAT-004/005/006/007/008 ✅ |
| Group 17P | Seed-to-Telemetry Track Model Alignment and Whole-Model Acceptance: `data/track_station_map.py` — `_detect_corners()` now caps at corners_expected (no more T13-T36 for Daytona); returns `(official, extra_peaks)` tuple; `TrackStationMap.extra_curvature_peaks` field; JSON I/O updated; `data/track_model_alignment.py` (NEW) — `TrackModelMatchStatus` enum (5 values), `CornerAlignmentResult`, `SectorAlignmentResult`, `TrackModelAlignmentResult` dataclasses, `align_track_model()`, `get_alignment_blockers()`, accepted model JSON persistence; `ui/track_model_alignment_vm.py` (NEW) — `format_alignment_summary()`, `get_acceptance_button_states()`, `format_mismatch_reasons()`, `manual_approval_buttons_enabled()`; `ui/track_map_vm.py` — `TrackMapDrawData.seed_overlay_note` field; `ui/dashboard.py` — Segment Review renamed Segment Diagnostics; 6 per-segment approval buttons hidden; Review Approval panel replaced by Track Model Alignment panel with Accept Track Model button, Rebuild button, alignment metric labels; `_tm_run_alignment()`, `_tm_refresh_alignment_panel()`, `_tm_accept_track_model()`, `_tm_rebuild_model()`, `_tm_try_load_accepted_model()` methods; `tests/test_group17p_alignment.py` (NEW) — 34 tests | **Complete 2026-06-25 — 34 tests pass** | DEF-17P-UAT-001/002/003/004/005/006 ✅ |
| Group 17Q | Seed Corner Position Matching and Acceptance Integrity: `data/seed_corner_matching.py` (NEW) — `CornerMatchStatus` (6 values), `CornerCandidateMatch`, `match_peaks_to_seed_windows()` greedy algorithm; `data/track_intelligence.py` — `SeedCornerDefinition` dataclass, `TrackLayoutSeed.corner_definitions` field, `_parse_corner_def()`, YAML `corners:` key support; `data/track_station_map.py` — `TrackStationMap.seed_corner_positions_available` field, `build_track_station_map()` branches on corner_defs presence (window-based vs top-N); `data/track_model_alignment.py` — 4 new fields on `TrackModelAlignmentResult` (`seed_corner_positions_available`, `corner_position_match`, `corners_matched`, `corner_candidate_matches`), `align_track_model()` computes per-corner MATCHED/NO_CANDIDATE/UNAVAILABLE, caps ACCEPTABLE_MATCH at GOOD_MATCH when no seed positions; `ui/track_model_alignment_vm.py` — 4 new summary keys; `ui/dashboard.py` — 3 new label rows; `tests/test_group17q_seed_corner_matching.py` (NEW) — 29 tests | **Complete 2026-06-26 — 29 tests pass** | DEF-17Q-001/002/003/004/005 ✅ |
| Group 17R | Seed Map Overlay, True Map Alignment, Recalibration Workflow: `ui/track_model_alignment_vm.py` — `format_alignment_summary()` returns explicit `seed_position_status`, `seed_truth_source` keys; `ui/track_map_vm.py` — `seed_overlay_note` field in `TrackMapDrawData`, propagated through `project_to_screen()`; `ui/dashboard.py` — "Seed truth source" alignment row, `_tm_refresh_alignment_panel()` wiring, `_tm_rebuild_model()` clears station map + shows dialog, warning suppression in `_tm_detect_segments_safe()`, tooltip + lap offset note updates; `tests/test_group17r_seed_overlay_alignment.py` (NEW) — 38 tests | **Complete 2026-06-26 — 38 tests pass** | DEF-17R-001/002/003/004/005/006 ✅ |
| Group 17S | Seed Track Definition Authoring, Corner Complexes, True Alignment Gate: `data/track_intelligence.py` — `SeedSectorDefinition`, `CornerComplexDefinition`, `SeedAuditResult` dataclasses, `audit_layout_seed()`, `_parse_sector_def()`, `_parse_complex_def()`, `TrackLayoutSeed.sector_definitions/corner_complexes` fields; `docs/track_modelling_seed/track_modelling_seed.yaml` — Daytona Road Course enriched with 12 corner windows (T1–T12, source:estimated), 3 sector_definitions (S1/S2/S3), 2 corner_complexes (BusStop=T1+T2, T10T11=T10+T11/Horseshoe); `data/track_model_alignment.py` — lap delta > 5% is now a BLOCKER (was a warning); `ui/track_model_alignment_vm.py` — `format_seed_audit_summary()`, `format_alignment_summary(layout_seed=None)` optional param, `seed_audit` key; `ui/dashboard.py` — "Seed data available" alignment row, `_tm_refresh_alignment_panel()` resolves layout_seed for audit, seed-window-based turn assignment in `_tm_refresh_seg_table()`, `_tm_refresh_seg_diagnostics_labels()` called on station map load/build; `tests/test_group17s_seed_definition_authoring.py` (NEW) — 36 tests | **Complete 2026-06-26 — 36 tests pass** | DEF-17S-001/002/003/004/005/006 ✅ |
| Group 17T | Seed Coordinate Map Import and Full-Lap Geometry Alignment: `data/track_seed_coordinate_map.py` (NEW) — `SeedMapStation`, `SeedCoordinateMap`, find/load/export/import/resample; `data/track_map_geometry_alignment.py` (NEW) — `TrackMapGeometryAlignmentResult`, `align_maps_geometry()`, transform estimation, missing-section detection, corner/sector coordinate matching; `ui/track_map_vm.py` — `seed_centreline` defaulted field; `ui/track_model_alignment_vm.py` — `format_geometry_alignment_summary()`, `format_alignment_summary()` accepts `geo_result`; `ui/dashboard.py` — "Geometry match" row, `_tm_refresh_alignment_panel()` computes geometry alignment; `tests/test_group17t_seed_coordinate_map.py` (NEW) — 55 tests | **Complete 2026-06-26 — 55 tests pass** | — |
| Group 17U | Track Library Schema and Seed Data Registry: `data/track_library.py` (NEW) — full dataclass hierarchy + resolver/loader/audit functions; `data/track_library/` directory (NEW) — `index.json`, Daytona track metadata, layout manifest with 12 corners/3 sectors/2 complexes, validation rules, source manifest; `data/track_intelligence.py` — `SeedAuditResult` extended with `seed_source/library_manifest_loaded/validation_rules_loaded`; `ui/track_model_alignment_vm.py` — `"seed_source"` key in `format_alignment_summary()`; `ui/dashboard.py` — "Seed source" row, library-first `resolve_seed_coordinate_map()`; `docs/TRACK_LIBRARY_SCHEMA.md` (NEW); `tests/test_group17u_track_library_schema.py` (NEW) — 83 tests | **Complete 2026-06-26 — 83 tests pass** | — |
| _(Groups 17V–38, A–E, Qualifying Mode, Setup Brain + Strategy Outcome integration)_ | Catalogued in `MASTER_TESTING_REGISTER.md` ("Groups 26–38 + Lettered Groups" and "Integration — Setup Brain + Strategy Outcome") and `docs/CURRENT_CLAUDE_HANDOFF.md`, not re-tabulated here | **Complete — see register** | — |
| Group 18A | Track Truth Library, Calibration Wizard, Station-Based Map Matching Foundation: builds a proper Track Truth spine so the app stops treating curvature-only corners as authoritative (principle: no mapped-corner confidence ⇒ no high-confidence rec). `data/track_truth.py` (NEW) — 4 str-Enums, 8 dataclasses (`TrackStation`/`CornerWindow`/`CornerComplex`/`SectorMarker`/`PitLaneDefinition`/`TrackTruthManifest`/`TrackTruthModel`/`TrackTruthValidationResult`), schema `track_truth_model_v1`+`track_truth_manifest_v1` (runtime-built from existing library manifest+semantic_model — no new file), `resolve_track_truth_model()`, `validate_track_truth_model()` (tiered gates is_accepted → is_usable_for_live_mapping → is_usable_for_ai_corner_context; NO_COORDINATE_GEOMETRY blocker), `can_use_track_truth_for_ai_corner_context()` AI guard; `data/track_truth_matcher.py` (NEW) — `match_track_truth_position()` weighted station scoring scaffold (swappable for HMM/Viterbi), confidence bands mirror `track_map_matching.py`; `data/track_truth_calibration.py` (NEW) — `TrackTruthWizardStage` (8) + `TrackTruthCalibrationWizard`, illegal transitions are no-ops, geometry DELEGATED to Group 17V's `data/track_geometry_builder.build_seed_geometry` (no duplicate algorithm), accept persists via `save_seed_geometry_to_library`; `ui/track_modelling_vm.py` — `format_track_truth_status()` (20-key display dict); `ui/track_modelling_ui.py` — "Track Truth / Mapping" panel + `_tm_refresh_track_truth_panel()`; `tests/test_group18a_track_truth*.py` (NEW ×3) — 45 tests. Daytona stays AI-BLOCKED (no seed_map → zero stations → NO_COORDINATE_GEOMETRY). Setup/Strategy/Live-Engineer rewiring deferred. | **Complete 2026-07-03 — 45 tests pass** | — |
| DEF-17U-UAT-007 | Time Trial calibration laps falsely classified as pit-in / unusable. GT7 has no reliable per-sample pit-lane flag, so geometric pit-in inference false-positived on clean Time Trial laps, and mid-lap start/stop partials poisoned the session median. Fix: pit-in detection DISABLED by default (`build_reference_path(..., pit_detection_enabled=False)`); new `PARTIAL_START` / `PARTIAL_STOP` `CalibrationLapQuality` (first/last lap below 0.5× interior-median path length, ≥ 50 samples, sessions > 2 laps) excluded from the build and not counted as rejected; session median from complete laps only; count-based build diagnostics that never say "pit-in" or "Drive a clean lap first" when pit detection is off. `data/track_calibration.py`, `ui/track_modelling_vm.py`, `ui/track_modelling_ui.py`, `data/track_segment_detection.py`; new `tests/test_def17u_uat007_calibration_build.py` (~35), `tests/test_def17u_uat007_partial_laps.py` (44). | **Complete 2026-07-03 — 4200+ pass (1 pre-existing unrelated failure)** | DEF-17U-UAT-007 ✅ |
| State Consolidation 1 — EventContext | Canonical event/race read model (no feature added, no backend removed, no UI rebuilt). `data/event_context.py` (NEW, pure — `EventContext` immutable dataclass, `EventContextSource`, `EventContextValidationResult`, `build_event_context()` DB-event-first + strategy overlay, `validate_event_context()`, `compute_change_hash()`, `flow_flags()` bridge to `ui/product_flow.py`); `docs/EVENT_CONTEXT_MIGRATION.md` (NEW — all ~35 `config["strategy"]` read sites classified EVENT-CONFIG vs NON-EVENT, migration + StrategyContext/SetupContext plan); `ui/dashboard.py` (`_build_event_context()` helper; `_refresh_telemetry_context()` migrated to read event/car/track from EventContext, DEF-P1-011 preserved); `tests/test_event_context.py` (NEW, 38). `config["strategy"]` retained as legacy compatibility; other ~34 read sites unchanged. | **Complete 2026-07-03 — 4173 pass / 6 skip / 0 fail** | — |
| AI Snapshot Migration — Frozen Context Inputs | Threads frozen, owner-documented snapshots of the four canonical contexts into every migrated AI-input path so prompts receive consistent, non-stale, non-mixed state (no prompt wording changed, no intelligence changed, no legacy store removed). `data/ai_context_snapshot.py` (NEW, pure — `AIContextSnapshot` core with combined `snapshot_id` + four component change markers + CONTEXTS/LEGACY_ONLY/EMPTY source + build/stale warnings; `StrategyAISnapshot` + `PracticeAnalysisSnapshot` frozen race-params preserving each path's exact legacy defaults incl. the DEF-P1-005 practice tuning-absent→LOCKED regime; `SetupAISnapshot` with the build-setup 0.0 refuel/pit-loss defaults; staleness detection strategy-vs-event / setup-vs-event / track-vs-event; LEGACY_ONLY fallback = exact legacy expressions + warning); `docs/AI_SNAPSHOT_MIGRATION.md` (NEW — all 11 AI input paths, owners per input, byte-identity proof, 4 documented intentional differences, deferred deps); `ui/dashboard.py` (`_build_strategy_ai_snapshot`/`_build_practice_ai_snapshot`; migrated `_assemble_strategy_inputs`, `_run_ai_analysis` incl. snapshot `config_id`, `_run_practice_analysis` with snapshot warnings in GT7_AI_DEBUG); `ui/setup_builder_ui.py` (`_build_setup_ai_snapshot`; migrated `_run_build_setup` + frozen worker-thread rec metadata, `_setup_analyse_ai`); `tests/test_ai_context_snapshot.py` (NEW, 41 — verbatim legacy expressions vs snapshots incl. byte-identical `_build_race_prompt` text; snapshot id semantics per context; frozen-after-mutation; staleness; legacy fallback; source scans); 20 legacy source-scan tests updated in place (groups 2/7/10/12a/15/36 — same invariants at the new home). | **Complete 2026-07-03 — 4402 pass / 6 skip / 0 fail** | — |
| Tab Navigation Refactor — Named Tab Lookup | Retires the hard-coded tab-index risk flagged since the Product Consolidation audit; navigation infrastructure only (tab order byte-identical, Home stays appended at 13, per-tab activation behaviour a 1:1 translation, no logic/prompt/mapping/PTT/voice/persistence/fan-out change — pinned by source-scans). `ui/tab_registry.py` (NEW, pure — stable keys `TAB_LIVE`/`TAB_EVENT_PLANNER`/`TAB_GARAGE`/`TAB_SETUP_BUILDER`/`TAB_PRACTICE_REVIEW`/`TAB_STRATEGY_BUILDER`/`TAB_TELEMETRY`/`TAB_DIAGNOSTICS`/`TAB_GUIDE`/`TAB_SETTINGS`/`TAB_HISTORY`/`TAB_AI_LOG`/`TAB_TRACK_MODELLING`/`TAB_HOME`; `DEFAULT_TAB_ORDER` = visual order 0–13 in one place, source-scan-paired with the real addTab sequence + runtime count warning; never-raising `TabRegistry` (`index_of`→-1, `key_at`→None, duplicate register no-op); `key_for_title()` strips the ⚙ decoration — registry lookup itself is positional so decorated labels can never break it; `TAB_BASE_TITLES` cross-checked vs `product_flow.TAB_ROLES`). `ui/dashboard.py`: `_on_tab_changed` dispatches by key; helpers `get_tab_index`/`has_tab`/`current_tab_key`/`select_tab` (only sanctioned `setCurrentIndex` site, safe on unknown keys); jumps `setCurrentIndex(4/3/1)` → `select_tab(TAB_PRACTICE_REVIEW/TAB_SETUP_BUILDER/TAB_EVENT_PLANNER)`; guards `currentIndex()!=11` → `current_tab_key()!=TAB_AI_LOG`, `_home_tab_index` → `TAB_HOME` (attribute retired). Enables next sprint's Home-to-index-0 move as an addTab+DEFAULT_TAB_ORDER-only change. `docs/TAB_NAVIGATION_REFACTOR.md` (NEW); `tests/test_tab_navigation_registry.py` (NEW, 33); 6 legacy tests updated in place (group12c, group14 ×2, group3, diagnostic-cleanup, home-dashboard — same invariants, key-based homes). | **Complete 2026-07-03 — 4512 pass / 6 skip / 0 fail** | — |
| OFR-2 — Separate Race vs Qualifying Telemetry Disciplines (Core split) | Second feature-factory run. The setup-BUILD and practice-analysis prompts now feed **discipline-aware telemetry**: QUALIFYING = peak metrics (best lap [measured], peak lateral G [estimated]+derivation note, lock-ups, brake consistency, oversteer rotation split) + an explicit "steering corrections and rival traffic/dirty-air are not measured" line; RACE = consistency/efficiency (fuel/lap [measured], lock-up/wheelspin/snap-throttle rates, lap-time std-dev w/ "N/A (1 lap)", per-corner tyre temps w/ "— not recorded"). **Every other purpose (unknown/practice/test) keeps the generic block byte-for-byte** (None-sentinel contract; a validator CRITICAL caught practice/test falling through to the RACE block — free-practice sessions are stored as "practice"). Brief approved with two orchestrator-flagged corrections: RF1 (discipline resolved SOLELY from the analysed session's stored type via new `db.get_session_type` — the live-mode combo would be wrong for historical sessions) and RF2 (real recent laps wired into the setup path — the brief's empty list would have left the headline surface dead). Validator also caught `limit=5` returning full-fuel OPENING laps → `get_session_laps(latest=True)` returns the recent stint ascending. Snapshots gain hash-excluded defensive `discipline` (Setup+Practice only). Strategy prompts, OFR-1, golden vectors, frozen allowlist all untouched (verified). Honestly scoped out: steering (no packet signal), rival data (none), per-corner exits (7-A), strategy telemetry (2-B/C), tyre radius. `docs/OFR2_SEPARATE_DISCIPLINES.md` (NEW). 269 new tests across 6 files incl. 114 acceptance (all 11 ACs + 6 edge classes PASS). | **Complete 2026-07-04 — 5217 pass / 6 skip / 0 fail** | — |
| OFR-1 — Between-Race Learning Loop (Loop 1: setup self-scoring) | First product feature on the consolidated architecture, built via the **/feature-factory** chain (researcher → story approved → brief approved w/ one design correction → backend-builder → frontend-builder → 43-test acceptance verification, all 11 ACs PASS → implementation-validator → fix round → re-verified; checkpoints committed before each verifier stage). After each session the app self-scores applied AI setup recommendations against measured before/after telemetry: `data/recommendation_scoring.py` (NEW pure — clean-lap windows, handling-vs-laptime target classification, verdict thresholds −200/+300 ms + 0.6/0.3 handling agreement + mixed-signal override, evidence-quality confidence with attribution split ×1/N + feedback bonus, honesty gates: missing before_metrics or <3 clean laps → insufficient_data conf 0.0, NO tyre-radius signal, §6.4 plain-English formatter). session_db migration v9 (`score_confidence`/-1.0 sentinel, `score_verdict`, `score_details`) + 6 methods (write-once `persist_score`, cross-layout guard, `get_previous_session_id`, `has_learning_for_car_track`, `get_scored_recs_for_prompt` ≥0.5/LIMIT 5). Trigger `_trigger_scoring_pass` at both session-open sites (after-session = just-finished session per the approved correction; own-session recs skipped; never blocks; zero config["strategy"] reads); Home journey **step 13 `learning_saved` finally live** (DB-derived). `driving_advisor` injects the scored block FIRST, replacing free-text history (exact fallback preserved). Validator findings fixed: dead mixed-signal branch (now reachable+tested), feedback bonus wired (`get_recent_feedback`), forbidden config read removed + **allowlist scan extended to driving_advisor** (15 pre-existing bridge entries frozen — the gap closed). `docs/OFR1_BETWEEN_RACE_LEARNING.md` (NEW). 171 new tests across 4 files (+ schema pins 8→9). | **Complete 2026-07-04 — 4948 pass / 6 skip / 0 fail** | — |
| Fan-Out Rule-Cache Deletion | **The Product Consolidation audit's original SSOT violation is deleted** (explicit product decision: "delete the rule cache"; full schema migration declined). `_fanout_event_to_strategy` no longer writes the 12 event-RULE fields — the helper writes only the legitimate **working-config core** (track, race_type, laps/total_laps, race_duration_minutes, event_id). Invisibility proven by test: EventContext resolves every rule DB-event-first per field (fallback only on `None`; the DB record and the `config["events"]` mirror carry all rules) → identical rules with fresh/stale/no cache, field-by-field; AI snapshots' CONTEXTS source yields identical frozen race_params with/without legacy rule keys (LEGACY_ONLY fires only with no event — where the fan-out never ran); the match-key hash reads core fields only (golden vectors green). Touch-ups: `_on_event_set_active`'s writer-internal permission call DELETED (redundant since Phase 3 — the sync applies permissions from the just-saved DB event, identical values); driving-advisor no-DB fallback hardened (`_evt_full or self._active_event() or strat`). Existing configs keep old rule keys as harmless unread leftovers (pinned: neither refreshed nor removed). Residual edge (accepted): ancient DB rows with NULL rule columns fall back to frozen leftovers; any event re-save heals. Remaining `config["strategy"]` (all allowlisted): working-config core writer + TM combo ids, plan-state persistence (item 4 remainder — schema decision, not correctness), bridge inputs, cosmetic reads. `docs/FANOUT_RULE_CACHE_DELETION.md` (NEW). `tests/test_fanout_rule_cache_deletion.py` (NEW, 16); **12 legacy pins updated in place** (group7 ×6 +1 core pin, group12a ×2, group4 ×1, phase_1/3/4 — invariant evolved to "rules NOT cached, DB-only"). | **Complete 2026-07-04 — 4777 pass / 6 skip / 0 fail** | — |
| Working Race Config Read Model | Retirement-map item 3, **reader half** (explicit product decision: "read model + readers"; the writer half — writers write a typed first-class object, dict fields become derived — is deferred with item 4). NEW `data/working_race_config.py` (pure): `WorkingRaceConfig` frozen dataclass naming the concept 6b identified (usually mirrors the active event; deliberately holds a restored historical session's config during a lap-bank restore — why it exists apart from EventContext). `from_strategy()` verbatim incl. the hash's own 25/60 absent-key defaults (distinct from EventContext's 0) + one documented hardening (garbage lengths coerce instead of raising). **Owns the match-key algorithm** (`length_key`/`hash_raw`/`compute_config_id()` = `sha256[:10]`) — the 6b golden vectors still exercise the REAL dashboard method through the delegation, proving byte-identity end-to-end. Migrated readers via the new `_working_race_config()` bridge builder: `_compute_race_config_id` (delegates), `_update_race_config` label + `race_configs` snapshot values (`length_text()`; the config_id WRITE stays), `_sync_strategy_from_event` no-event checks, `_save_session_to_db` session tagging. `FROZEN_ALLOWLIST` reshaped: net −3 direct readers (+1 bridge). **The reader side of the whole consolidation is complete** — remaining dict access = writers, bridges, plan-state, and cosmetics, all allowlisted. `docs/WORKING_RACE_CONFIG.md` (NEW). `tests/test_working_race_config.py` (NEW, 25); `tests/test_race_config_id_hash.py` updated in place (stub binds the real builder; source pin moved to the model — same invariant, new home). | **Complete 2026-07-04 — 4760 pass / 6 skip / 0 fail** | — |
| Legacy Fan-Out Removal Phase 6b — config_id Hash Byte-Stability Proof | Retirement-map item 2 (`docs/LEGACY_FANOUT_PHASE_5.md` §4) delivered as **proof + pins**; the migration half is provably **blocked** and folds into items 3/4. Blocker (restore-divergence): `_load_session_config` deliberately writes a historical session's track/car+params into the working `config["strategy"]` WITHOUT changing the active event, then recomputes the id — the id must follow the RESTORED session; DB-first `EventContext.track`/rules would pin it to the active event mid-restore and silently break the lap-bank restore. Outside restores the sources are provably identical (post-Phase-4 always in sync — tested); `car` alone is always-safe (strategy-first) but hash inputs move together. Purely additive — **no production code changed**. `tests/test_race_config_id_hash.py` (NEW, 17 — 5 frozen golden vectors exercised through the REAL `_compute_race_config_id` on a widget-free stub, incl. the field-observed `'||l25' → 05e6d2f288`; regenerating vectors on failure is forbidden by the test header (a mismatch = history re-keying; fix the code); `DEFAULT_CONFIG` → empty-vector pin; 10-hex shape/stability/per-input sensitivity; the algorithm's own `l25`/`t60` defaults pinned (distinct from EventContext's 0 defaults); unknown race-type → lap; source-level verbatim pin of the raw-string format/`sha256[:10]`/defaults/input source; in-sync EventContext equivalence + the restore-divergence demonstration; allowlist-untouched/Home-first/guardrail invariants). `docs/LEGACY_FANOUT_PHASE_6B.md` (NEW — corrected retirement map). | **Complete 2026-07-04 — 4738 pass / 6 skip / 0 fail** | — |
| SessionContext Real Connection Signal | The one-place change promised by the SessionContext sprint, delivered: `MainWindow(udp_listener=...)` (duck-typed `.connected`/`.total_received`/`.parse_errors`/`.packet_rate` — real `UDPListener` properties; `connected` True on packet receive, False after 3 s silence; plain attrs → GIL-atomic cross-thread reads, no locks). `main()` passes the listener. `_build_session_context` prefers the listener for connected+packet_count (legacy tracker-getattr fallbacks retained verbatim — byte-identical old always-False/0 behaviour for tests/legacy constructions; existing SessionContext suite passes unchanged) → **Home `live_active`, the journey step-12 flow gate, and `_refresh_telemetry_context` labels become real** through the existing context plumbing. Wider latent bug found+fixed: `_update_telemetry_labels` (diagnostics panel) read FOUR phantom tracker attrs (`_connected`/`_packet_count`/`_error_count`/`_packet_rate_hz` — never existed; panel frozen at "Disconnected / 0 / — Hz / Not started") — now reads the listener's four real stats with the old fallbacks preserved. Intended behaviour change: with SimHub streaming the Home/Telemetry surfaces show Connected + live counts. `tests/test_session_connection_signal.py` (NEW, 18 — real methods on widget-free stubs incl. lit-vs-frozen panel, listener-beats-tracker, missing-attr safety, fallback pins, wiring scans, `UDPListener` property contract, allowlist-still-exact + standing invariants). Docs: `SESSION_CONTEXT_MIGRATION.md` §5a; HOME_DASHBOARD_BUILD + PHASE_6A cross-refs. | **Complete 2026-07-04 — 4721 pass / 6 skip / 0 fail** | — |
| Legacy Fan-Out Removal Phase 6a — Dispatcher SessionTag Snapshot | Retirement-map item 1 (`docs/LEGACY_FANOUT_PHASE_5.md` §4): removes the telemetry-path `config["strategy"]` reads. NEW `data/session_context.SessionTag` (frozen `track`/`car`/`config_id`/`event_id`; `from_strategy()` reproduces the dispatcher's original reads verbatim; coercing `build_session_tag()`; immutable → attribute swap atomic under the GIL, no lock between the UI writer thread and dispatcher reader thread). `EventDispatcher` (main.py): seeds the tag at construction from the config it receives (one-time, pre-thread — the single remaining main.py bridge read; allowlist `("main.py","__init__"):1` replaces `("main.py","_dispatch"):2`); `set_session_tag()` None-safe swap; `_dispatch` reads only the tag at both sites (per-lap `write_lap` `event_id`; fallback race-session open); **`self._config` removed entirely**. `MainWindow._push_session_tag()` (NEW — builds from EventContext + `_active_config_id()`, byte-identical per the Phase 5 proofs; never raises) pushed at: end of `_update_race_config` (Set-as-Active, garage car select, and the session-config restore all funnel through it), `_on_event_save`'s active-event re-sync branch (ordering after `_fanout_event_to_strategy` pinned), and end of `__init__` (before `dispatcher.start()`). Dead-default note pinned by test: the old fallback `strat.get("track","Unknown")` never fired — DEFAULT_CONFIG has always materialised `strategy.track=""`; real behaviour preserved. `docs/LEGACY_FANOUT_PHASE_6A.md` (NEW). `tests/test_legacy_fanout_phase_6a.py` (NEW, 21 — SessionTag pure model; context-tag == strategy-tag; the REAL EventDispatcher exercised without starting its thread: seed, swap, RACE_STARTED opens with exactly the tag fields, LAP_COMPLETED writes event_id from the tag, updated tag used next event; source-scans; invariants); Phase 5 `FROZEN_ALLOWLIST` consciously shrunk in the same commit (the guard held). | **Complete 2026-07-04 — 4703 pass / 6 skip / 0 fail** | — |
| Legacy Fan-Out Removal Phase 5 — Functional Readers + Frozen Allowlist Guard | Completes the reader side of the fan-out series (scope: product decision "Functional + guard"; full writer retirement re-audited and **blocked** — telemetry-path `main.py _dispatch` reads (would mean a DB query per lap event), the `_compute_race_config_id` **hash** (byte-stability), restore writers (`_load_session_config` etc.), plan-state persistence (stops/fuel/tolerances/config_id have no DB home), context-builder bridges — all mapped in the doc's Phase 6 retirement plan). Migrated byte-identically (in-sync proven; post-Phase-4 always in sync): live-session open tagging in `_on_live_mode_changed` (EventContext `track`/`car`/`event_id` + StrategyContext `config_id`); degradation params (`tyre_wear_multiplier` EventContext + `degradation_consecutive_laps` StrategyContext, still read pre-worker on the UI thread); BoP checks (`_get_bop_data_for_car` + reload gate → `bop_enabled`/`car`); `_current_setup_dict` event-identity fields (car w/ "Unknown Car" fallback, track, weather→condition map, bop — voice-thread-safe: SessionDB `check_same_thread=False` + lock); setup-save `event_id`. **No product decision reads the legacy dict any more.** NEW **frozen allowlist** (`FROZEN_ALLOWLIST`, 41 sites / 40 (file,method) entries, each annotated writer/bridge/hash/plan/restore/cosmetic/telemetry) — exact-equality source scan fails on any NEW consumer (with a pointer to the contexts) or any silent removal. `docs/LEGACY_FANOUT_PHASE_5.md` (NEW). `tests/test_legacy_fanout_phase_5.py` (NEW, 15); `test_group4_fixes` `TestBoPSourceOfTruth` ×2 updated in place ("BoP from event state, never a widget" — source now EventContext). | **Complete 2026-07-03 — 4682 pass / 6 skip / 0 fail** | — |
| Legacy Fan-Out Removal Phase 4 — Divergence Elimination + Last Readers | Eliminates the DB-vs-fan-out divergence and finishes the named reader migrations. NEW `dashboard._fanout_event_to_strategy(evt_name)` — the Set-as-Active strat-write block extracted **verbatim** (event-RULE fields only: track/race-type/length/wear/fuel/stops/weather/damage/refuel/tyres+mandatory_compounds names/bop/tuning/cats/event_id; **config-dict only** — no tracker/advisor/query-listener/sync/persist side effects; never touches car/config_id/stops/fuel-plan fields). `_on_event_set_active` behaviour unchanged (save → helper → all activation side effects). **`_on_event_save` re-syncs the fan-out when `name == active_event_id`** (before its existing persist; saving a non-active event changes nothing) → after edit+Save, DB-first AND legacy readers agree immediately; derived config_id still refreshes on the next strategy-tab sync (same timing as before). Last readers migrated byte-identically: `_get_mandatory_compounds` (EventContext.required_tyres codes → display names via data.tyres.get_by_code — the same mapping the writer used), setup-tab refuel (`int(ev_ctx.refuel_rate_lps)`)/required/available labels + car rebind; `_sync_setup_builder_from_event` reads NO config["strategy"] (dead `sc` removed). **Writer retirement investigated + deferred to Phase 5** — retiring now breaks the app: car/config_id/stint plan live ONLY in the fan-out and ~25 readers remain (live-session open, BoP, degradation, config_id hash, restore paths, AI-snapshot bridges); with re-sync it can't go stale, so retirement is mechanical (re-home fields → migrate reads → delete writer). `docs/LEGACY_FANOUT_PHASE_4.md` (NEW). `tests/test_legacy_fanout_phase_4.py` (NEW, 18 — real helper on a widget stub: all rule fields written, plan fields preserved, live-dict returned, no side effects, race-type normalisation; save-guard before persist + save stays config-only + Set-as-Active keeps side effects + no inline fan-out left; reader byte-identity; TM-writer/Home-first/guardrail invariants); 11 legacy pins updated in place (group7 ×7, group12a ×3, group4 ×1 — same invariants, new home). | **Complete 2026-07-03 — 4667 pass / 6 skip / 0 fail** | — |
| Legacy Fan-Out Removal Phase 3 — Functional Gating / Validation Migration | Migrates the two remaining **functional** `config["strategy"]` consumers to DB-first EventContext (explicit product sign-off: "flip reads only"; fan-out writers untouched — Phase 4). (1) **Setup-permission gating** — `_sync_setup_builder_from_event` feeds the unchanged `_on_bop_toggled` + `_apply_setup_permissions` from `ev_ctx.bop_enabled` / `.tuning_allowed` / `list(.allowed_tuning_categories)` (was `bool(sc.get("bop"/"tuning",…))` / `sc.get("allowed_tuning_categories", [])`). (2) **DEF-P3-012 strategy-options tuning validation** — `_strat_locked`/`_strat_allowed` from `_build_event_context()` (`tuning_locked`/`allowed_tuning_categories`); `validate_ai_setup_response` call unchanged. `_on_event_set_active`'s writer-internal permission call deliberately still reads the just-written `strat` (fresh by construction, pinned). Byte-identical in-sync (field-by-field across unrestricted/BoP-on/locked/partially-restricted + empty defaults); diverged case = the signed-off change: editable fields + validation follow the fresh DB truth, removing the Phase 2 inconsistency (labels showed DB truth, lock state enforced the stale fan-out). **Reader consistency complete** — AI inputs, labels, gating, validation all DB-first; fan-out remains only for writers + minor fallbacks (refuel/req/avail labels, car rebind, `_get_mandatory_compounds`, no-event branch) + the context-builders' bridge inputs. `docs/LEGACY_FANOUT_PHASE_3.md` (NEW). `tests/test_legacy_fanout_phase_3.py` (NEW, 20 — byte-identity vs verbatim old expressions; DB-first divergence; source-scans incl. zero raw gating reads left + `_apply_setup_permissions` body pinned + writer-internal call intact; writers/Home-first/config-guardrail invariants); 2 Phase 2 pins updated in place. | **Complete 2026-07-03 — 4649 pass / 6 skip / 0 fail** | — |
| Legacy Fan-Out Removal Phase 2 — Event-Rule Display-Label Migration | Migrates the Strategy/Setup event-context **display readout labels** from the strategy-first `config["strategy"]` fan-out to **DB-first EventContext** (consistent with the strategy/setup AI, which already reads DB-first). Scope = **display labels only** (user decision): the functional paths — setup-permission gating (`_apply_setup_permissions`), BoP toggle (`_on_bop_toggled`), spinbox rebind — still read the fan-out, so **which setup fields are editable is unchanged**. Byte-identical in the normal (in-sync) case: all event fields are integer `QSpinBox` values, so the labels wrap `int()` ("2×" not "2.0×"); `race_type` normalisation handles DB "Timed Race" vs config "timed". Verified the full rendered Strategy line + Setup labels byte-identical for an in-sync pair. Behaviour change (documented + tested): after an event is edited + Saved but not re-activated (DB fresh, fan-out stale) the **labels** now show the DB truth; editable fields don't change. `dashboard._sync_strategy_from_event` (`_lbl_strategy_event_ctx` + `_lbl_fuel_mult_display`; `_update_race_config` writer + `_get_mandatory_compounds` + no-event branch untouched); `setup_builder._sync_setup_builder_from_event` (`_lbl_setup_event_ctx` + `_lbl_rc_*` race_type/length/fuel/wear/mand_pits/weather/damage/bop/tuning; left refuel/req/avail labels + `_bop`/`_tuning`/`_cats` functional reads on the fan-out). `docs/LEGACY_FANOUT_PHASE_2.md` (NEW). `tests/test_legacy_fanout_phase_2.py` (NEW, 15 — in-sync byte-identity + int-format guard; DB-first divergence; source-scans that display reads use EventContext while functional gating still reads `config["strategy"]`; writer/Home-first/config-guardrail invariants). | **Complete 2026-07-03 — 4629 pass / 6 skip / 0 fail** | — |
| SessionContext / TelemetryContext | Adds the telemetry-layer canonical read model (peer of Event/Strategy/Setup/Track contexts) so live-session status stops being read from volatile tracker attributes / the legacy config fuel fallback. Additive + byte-identical — no telemetry/PTT/voice/live-race/setup/strategy/track/AI/tab-order change; `config["strategy"]` + both fan-out writers preserved. `data/session_context.py` (NEW, pure — `SessionContext`: connected/packet_count/laps_recorded/active_session_id/is_recording/live_active/live_mode/telemetry_avg_fuel_per_lap/fuel_burn_per_lap+fuel_burn_source (LOADED_SESSION/TELEMETRY/CONFIG_FALLBACK)/has_practice_laps/has_valid_laps/source; `build_session_context` never raises; `connection_text`/`recording_text`/`flow_flags`; byte-identity: `fuel_burn_per_lap` reproduces `_computed_fuel_burn_lpl`'s 3-tier fallback, `connected` reproduces the `tracker._connected` getattr). `ui/dashboard.py` `_build_session_context()` helper; migrated `_computed_fuel_burn_lpl` (→ context; config fuel read now only in the builder), `_build_home_dashboard_state` (live_active/has_practice_laps/has_valid_laps), `_refresh_telemetry_context` (connection/packet/recording/telemetry-fuel labels). `_home_has_practice_laps` still owns the DB query; `has_valid_laps` still approximated; live-render tracker reads (tyre labels/fuel bar/countdown) left alone. `docs/SESSION_CONTEXT_MIGRATION.md` (NEW). `tests/test_session_context.py` (NEW, 25 — fuel byte-identity + source class; connection/recording/live semantics; coercion; source EMPTY/LIVE; garbage safety; ownership boundary; flow_flags; purity; consumer source-scans; Home-first/config-guardrail invariants). | **Complete 2026-07-03 — 4614 pass / 6 skip / 0 fail** | — |
| Legacy Fan-Out Removal Phase 1 — Read-Only Consumer Migration | Reduces dependence on the legacy `config["strategy"]` fan-out cache by migrating low-risk **read-only** consumers to the canonical read models — every migrated read is byte-identical to the expression it replaces (proven by test); no behaviour change; `config["strategy"]` and both fan-out writers (Event Planner "Set as Active" `_on_event_set_active`; Track Modelling combo `track_location_id`/`layout_id`) preserved and pinned. Migrated: `config_id` → `StrategyContext.config_id` via new `dashboard._active_config_id()` (setup_builder `_refresh_setup_history_combo` + `_on_setup_history_selected` read-only lookups, `_display_setup_result` + `_run_build_setup` history-save keys — 0 raw config_id reads left in setup_builder_ui.py); `car` → `EventContext.car` in `dashboard._sync_practice_from_event`. Deferred (documented): DB-event-first event-rule reads (track/tyre_wear/fuel_mult/tuning/bop/race-length — not byte-identical vs strategy-first raw reads), the `config_id` hash `_compute_race_config_id`, telemetry-owned `_computed_fuel_burn_lpl`, and all AI-input reads (already snapshot-migrated). `docs/LEGACY_FANOUT_PHASE_1.md` (NEW — every remaining reader classified EVENT_CONFIG/STRATEGY_PLAN/TRACK_IDENTITY/SETUP_STATE/AI_INPUT/LEGACY_REQUIRED/WRITER). `tests/test_legacy_fanout_phase_1.py` (NEW, 22 — byte-identity + source-scans + writer preservation + no-new-fanout + Home-first/config-guardrail invariants). | **Complete 2026-07-03 — 4589 pass / 6 skip / 0 fail** | — |
| Config Safety Guardrails | Makes it impossible for tests/headless smoke runs/automated app construction to read from or write to the user's real `config.json` unless explicitly requested (response to the Home Dashboard Promotion smoke run clobbering the local config). Safety + test-isolation only — no setup/strategy/track/AI/telemetry/PTT/voice/workflow change; `config["strategy"]` + both fan-outs untouched; only config-schema change is materialising the already-effective `strategy.degradation_consecutive_laps: 2`. `config_paths.py` (NEW, pure — `DEFAULT_CONFIG` moved from `main.py` + re-exported; `resolve_config_path` `--config`→`NGR_CONFIG_PATH`→`config.json`; `is_test_environment`/`is_real_config_path`/`real_config_access_blocked`; `load_config` refuses real read under tests→defaults, no secret exposure; `save_config` refuses real write under tests→`ConfigSafetyError`, serialise-first no-partial + `.bak` + atomic `os.replace`; `write_default_config`); `main.py` (`resolve_config_path`); `ui/dashboard.py` `_persist_config` → guarded atomic `save_config` (catches `ConfigSafetyError`, never crashes; normal runs unchanged); `.gitignore` +`config.json.bak`/`.tmp`; `tests/conftest.py` (NEW — `temp_config_path` fixture + session-autouse `_guard_real_config` SHA-256 net); `docs/CONFIG_SAFETY_GUARDRAILS.md` (NEW); UAT SMK-001 landing-tab fixed + config-safety dev note. `tests/test_config_safety_guardrails.py` + `tests/test_config_safety_smoke.py` (NEW, 34; smoke is `importorskip` PyQt6 + offscreen, proves construction leaves real config byte-identical). | **Complete 2026-07-03 — 4567 pass / 6 skip / 0 fail** | — |
| Home Dashboard Promotion — Move Home to Index 0 and Add Click Navigation | Makes the Home Dashboard (Race Engineer Command Centre) the first tab + default landing page and lights up the click-to-navigate deferred with it (UI navigation only — no setup/strategy/track-mapping/AI-prompt/AI-snapshot/telemetry/PTT/voice/calibration/persistence/context-ownership change; no `config["strategy"]` fan-out removed; no new hard-coded index; `select_tab` still the only `setCurrentIndex` site — pinned by source-scans). `ui/tab_registry.py` (`DEFAULT_TAB_ORDER` leads with `TAB_HOME`, comments renumbered 0–13, every non-Home tab keeps its relative order — order-only edit, positional registry re-derives indices); `ui/dashboard.py` (Home `addTab` first; `select_tab(TAB_HOME)` ends `_setup_ui`; guarded `_home_refresh()` ends `__init__` for the first render; `_build_home_tab` per-card "Open <Tab>" buttons + next-action button; new `_home_navigate`/`_home_navigate_next_action`/`_home_update_next_action_button`/`_home_nav_button_text` + `_HOME_NAV_BTN_QSS`; `_home_refresh` updates the next-action button; Guide "last tab"→"first tab"); `ui/home_dashboard_vm.py` (`CARD_TAB_KEYS` + `tab_key_for_card()` — Race Setup→Event Planner, Track Intelligence→Track Modelling, Setup Brain→Setup Builder, Strategy Brain→Strategy Builder, AI Input Safety→AI Log; stable keys only, still no PyQt6); `ui/product_flow.py` ("index 13"→"first tab"). Navigation is tab-change only (`select_tab`, `has_tab`-guarded, never raises). `docs/HOME_DASHBOARD_PROMOTION.md` (NEW); `tests/test_home_dashboard_promotion.py` (NEW) + `test_tab_navigation_registry`/`test_home_dashboard_vm`/`test_diagnostic_tab_cleanup`/`test_consolidation_product_flow` order-pins updated in place. Headless smoke confirmed 14 tabs, tab 0 = Home, opens on Home, card + next-action nav work. | **Complete 2026-07-03 — 4533 pass / 6 skip / 0 fail** | — |
| Diagnostic Tab Cleanup — Low-Risk UI Dags Removal | Executes the audit's §9 items 1/3/4 — the whole diff is deletions of dead UI, label text and Guide HTML (no logic/prompt/mapping/PTT/voice/persistence/tab-order/fan-out change, pinned by source-scans). DELETED: the 7 hidden, never-`clicked.connect`-ed legacy per-segment review buttons + save-path label + 4 never-applied style strings + the unreachable `_tm_review_confirm/rename/reject/needs_laps/split/merge/save` handlers + `_tm_refresh_review_buttons` + no-op `_tm_refresh_approval_panel` + 8 dead imports (`ui/track_modelling_ui.py`; pure functions in `data/track_segment_review.py` and `ui/track_modelling_vm.get_review_button_states` retained with own coverage); dead never-rendered `_TELEMETRY_REFERENCE_HTML` (~143 lines, `ui/dashboard.py`). RENAMED: "Race Config ID:"→"Session Match Key:" (+plain tooltip; `config_id` mechanics untouched); Diagnostics "Rem(clk):"→"Time left:", "rem_ms(raw):"→"remaining_time_ms:", "Ann queue:"→"Voice queue:" (creation+setText sites); window title + Guide h1 → "Next Gear Racing Pit Crew". GUIDE: phantom "Dashboard" Step 8 → real Home tab; API-key bullet → Strategy Builder field (audit's "Settings duplicate" corrected — no Settings key field exists; relocation deferred); "Tool tabs (⚙)… safe to ignore" note; pip-install tooltip line removed. Deferred: TM jargon glossary, Telemetry raw-row hiding, API-key relocation, both `config["strategy"]` fan-outs (pinned by test). `docs/DIAGNOSTIC_TAB_CLEANUP.md` (NEW); `tests/test_diagnostic_tab_cleanup.py` (NEW, 25); `test_group24` `_tm_` floor 54→46. | **Complete 2026-07-03 — 4479 pass / 6 skip / 0 fail** | — |
| Home Dashboard Build — Race Engineer Command Centre | The missing REQUIREMENTS §12.2 home/next-action surface (audit §1.1), display-only (no race/setup/strategy/track-mapping/AI-prompt/PTT/voice change, no tab reordered/removed, owns no state). `ui/home_dashboard_vm.py` (NEW, pure — `HomeDashboardState`/`HomeDashboardCard`/`HomeDashboardStatus`/`HomeDashboardWarning`/`HomeDashboardNextAction`; `build_home_dashboard_state()` never raises, garbage-safe per section; five cards Race Setup / Track Intelligence / Setup Brain / Strategy Brain / AI Input Safety + Next Best Action from `build_flow_state_summary()`; `build_flow_flags()` context→gate bridge (`has_strategy` requires a stint plan); pure HTML renderers with escaping); `ui/dashboard.py` (Home tab **appended at index 13** — indices 0–12 + `_on_tab_changed` dispatches unchanged, pinned by source-scan; `_build_home_tab`/`_build_home_dashboard_state` (reads the four context builders + `_last_setup_context` + `_build_strategy_ai_snapshot` — pure computation, no AI call)/`_home_has_practice_laps` (read-only DB)/`_home_refresh`/`_home_refresh_if_visible`; hooks at end of `_on_event_set_active` + `_update_race_config`); `ui/setup_builder_ui.py` + `ui/track_modelling_ui.py` (hasattr-guarded refresh hooks); `ui/product_flow.py` ("Home" = workflow role); `docs/HOME_DASHBOARD_BUILD.md` (NEW); `tests/test_home_dashboard_vm.py` (NEW, 52). Deferred: setup-card persistence across restarts, click-to-navigate, SessionContext-owned `has_valid_laps`/`live_active`. | **Complete 2026-07-03 — 4454 pass / 6 skip / 0 fail** | — |
| State Consolidation 4 — TrackContext | Canonical track/layout read model separating track truth from EventContext/StrategyContext/SetupContext, dashboard UI state, seed files, reference paths, station maps and legacy controller state (no track mapping feature added, no UI rebuilt, no persistence format changed, no Daytona accuracy claims). `data/track_context.py` (NEW, pure — `TrackIdentity` (ids/display names/`combined_id` matching `<loc>__<lay>` file conventions), `TrackMapAvailability` (seed metadata/corner windows/coordinate geometry/reference path/calibration laps/station map/reviewed+accepted model/lap offset — flags echo existing audits, never invent accuracy), `TrackGeometryStatus` (modelling status resolver-first, ai_ready, resolver outcome, track-truth gates echoed tri-state), `TrackAlignmentStatus`, `TrackContextSource` (identity priority: TM combos → EventContext → config ids → seed), `TrackContextValidationResult` (identity vs availability vs staleness warnings); helpers `matches_event` tri-state / `mismatches_event` / `is_stale_for_event` / `can_attempt_live_mapping` / `live_mapping_blockers`; `build_track_context()` duck-typed over existing loader results, no file I/O, never raises; `compute_change_hash()` over identity+availability+status; splat-safe `flow_flags()`); `docs/TRACK_CONTEXT_MIGRATION.md` (NEW — 16-item SSOT audit with duplication verdicts, deferred consumers, risks, AI-Snapshot/Home-Dashboard next plan); `ui/track_modelling_ui.py` (`_build_track_context()` helper; `_tm_refresh_track_truth_panel()` identity via TrackContext, combo-sourced only — behaviour-preserving; `_last_track_context` captured); `tests/test_track_context.py` (NEW, 68). Live-map-dot identity, AI id reads, and the Group 17H combo→config fan-out writer intentionally deferred. | **Complete 2026-07-03 — 4361 pass / 6 skip / 0 fail** | — |
| State Consolidation 3 — SetupContext | Canonical setup-recommendation read model that separates setup truth from event truth (EventContext), strategy truth (StrategyContext), and legacy config/DB storage, keyed to `EventContext.change_hash` + `StrategyPromptSnapshot.snapshot_id` so stale setups are detectable (no feature added, no backend removed, no UI rebuilt, no PTT/voice change). `data/setup_context.py` (NEW, pure — `SetupContext` immutable dataclass owning setup_id/config_id/label, `purpose` (QUALIFYING/RACE/PRACTICE/TEST/UNKNOWN via `normalise_purpose`), `source` (AI/GENERATED/MANUAL/SAVED_DB/LEGACY_CONFIG), adjustments (`SetupChangeEntry` round-tripping the AI `changes` shape), changed fields, frozen baseline+target setups, reason/primary_issue/confidence, validation warnings, applied state, `change_hash` + `event_change_hash`/`strategy_snapshot_id`/`telemetry_diagnosis_hash`; keying helpers `matches_event`/`is_stale_for_event`/`is_stale_for_strategy`/`is_missing_identity`/`matches_purpose`; `SetupContextValidationResult` separating setup-input vs staleness warnings; `SetupPromptSnapshot`+`build_setup_prompt_snapshot()` value-copied freeze stable under later config mutation; `build_setup_context()` reads event/strategy only as keys, never raises); `docs/SETUP_CONTEXT_MIGRATION.md` (NEW — every setup store + writers/readers, migrated + deferred consumers, risks, TrackContext/AI-input next plan); `ui/setup_builder_ui.py` (`_build_setup_context()` helper; `_setup_type_prefix()` purpose via SetupContext; `_display_setup_result()` captures `_last_setup_context`); `tests/test_setup_context.py` (NEW, 67). Legacy setup config/DB storage retained; AI setup-prompt + apply/save writers deferred. | **Complete 2026-07-03 — 4293 pass / 6 skip / 0 fail** | — |
| State Consolidation 2 — StrategyContext | Canonical strategy-plan read model that separates strategy state from the event/race config owned by EventContext (no feature added, no backend removed, no UI rebuilt). `data/strategy_context.py` (NEW, pure — `StrategyContext` immutable dataclass owning `config_id`/stint plan/planned stops/pit laps/fuel burn/optional starting-fuel+margin+refuel-required/degradation assumptions/tolerances/`change_hash`/`event_change_hash`; `StintPlanEntry` round-tripping the legacy `stops` shape; `StrategyContextSource` EMPTY/LEGACY_STRATEGY/GENERATED; `StrategyContextValidationResult` separating `strategy_*` from `event_*` warnings; `StrategyPromptSnapshot`+`build_strategy_prompt_snapshot()` value-copied freeze of EventContext race config + StrategyContext plan; `build_strategy_context()` reads event rules from EventContext, ignores event fields in the strategy dict, never raises; `compute_change_hash()` over strategy fields only); `docs/STRATEGY_CONTEXT_MIGRATION.md` (NEW — ownership boundary, every strategy field + writer/readers, migrated + deferred consumers, risks, SetupContext plan); `ui/dashboard.py` (`_build_strategy_context()` helper; `_refresh_lap_bank()` config_id ★ marker migrated); `tests/test_strategy_context.py` (NEW, 53). `config["strategy"]` retained as legacy compatibility; AI-input path deferred. | **Complete 2026-07-03 — 4226 pass / 6 skip / 0 fail** | — |
| Product Consolidation Sprint | Audit + safe first-pass UI clean-up (no feature added, no backend removed, no tab reordered). `docs/PRODUCT_CONSOLIDATION_AUDIT.md` (NEW — per-tab KEEP/MOVE/RENAME/MERGE/DELETE/HIDE verdicts, 14-item single-source-of-truth table + ranked violations, 9-context target architecture, next-sprint plan); `ui/product_flow.py` (NEW, pure — tab roles, 13-step journey, `build_flow_state_summary()` next-action logic, tab-title decoration); `ui/dashboard.py` ("Debug"→"Diagnostics" tab, `_apply_product_flow_tab_markers()` ⚙ marks Telemetry/Diagnostics/AI Log/Track Modelling); `ui/track_modelling_ui.py` ("5. Track Model Alignment"→"5. Seed Geometry", "Resolver Status"→"Track Model Status"); `tests/test_consolidation_product_flow.py` (NEW, 27), `tests/test_group23b_ui_cleanup.py` (updated). Deferred SSOT refactors (event fan-out, track/layout split, setups dual-residence) documented, not started. | **Complete 2026-07-03 — 4135 pass / 6 skip / 0 fail** | — |

---

## Root Causes Identified (Post-UAT)

| ID | Name | Status | Cascades To |
|----|------|--------|-------------|
| Root Cause A | Event persistence/reload broken | **Fixed (Group 7)** | DEF-P1-009, DEF-P1-005, tyre wear, fuel mult, avail/req tyres |
| Root Cause B | Session reload mapping incomplete | **Fixed (Group 8)** | DEF-P2-013, DEF-P2-014, DEF-P2-009 |
| Root Cause C | AI Debug/Log view not visible | **Fixed (Group 9)** | DEF-P1-010 — unblocks DEF-P4-002, DEF-P2-007, DEF-P2-016 verification |
| Root Cause D | AI prompt not using Event BoP/tuning | **Verified by Group 10 tests** | DEF-P1-005 — runtime retest via AWR-031/AWR-032/AWR-037/AWR-038 |

---

## Defect Summary

### Open Defects (implementation required)

| ID | Priority | Title | Group |
|----|----------|-------|-------|
| DEF-P2-018 | P2 | Outlap row has no visual identification in Practice Review | Group 15 |
| DEF-P3-005 | P3 | Pit window is static, not recalculated on deviation | Deferred |
| DEF-P3-007 | P3 | Disabled race type field not visually dimmed | Group 15 |
| DEF-P3-008 | P3 | Top speed target never populated from valid practice telemetry | Group 15 |
| ~~DEF-P3-013~~ | — | ~~AILogEntry missing car_id/track~~ | Fixed Group 15A |

### Awaiting Retest (fix complete, runtime verification needed)

| ID | Priority | Title | AWR |
|----|----------|-------|-----|
| DEF-P1-001 | P1 | Session opens on mode selection | AWR-001 |
| DEF-P1-002 | P1 | Outlap recording | AWR-002/003 |
| DEF-P1-003 | P1 | Save Session crash | AWR-004/005 |
| DEF-P1-004 | P1 | Timed race in AI prompt | AWR-006 |
| DEF-P1-005 | P1 | AI prompt BoP/tuning restrictions | AWR-031/AWR-032/**AWR-042** |
| DEF-P1-006 | P1 | Compound lap counts in AI | AWR-013 |
| DEF-P1-007 | P1 | Fuel burn single source | AWR-014 |
| DEF-P1-008 | P1 | Practice RACE_FINISHED guard | AWR-011 |
| DEF-P1-009 | P1 | Event load restores saved variables | **AWR-031** |
| DEF-P1-010 | P1 | AI Debug / AI Log tab not visible | **AWR-036** |
| DEF-P1-005 | P1 | BoP On + Tuning Off prompt contains locked block | **AWR-037** |
| DEF-P1-005 | P1 | Partial tuning — locked fields not sent as editable | **AWR-038** |
| DEF-P2-016 | P2 | Practice Analysis blocks AI call on bad input data | **AWR-039** |
| DEF-P1-011 | P1 | Fuel Burn Auto shows stale value after event switch | **AWR-040** |
| DEF-P2-021 | P2 | AI Log list auto-select + timestamp + status format | AWR-041/**AWR-044** |
| DEF-P2-023 | P2 | No-refuel pit stop detected as pit lap | **AWR-045** |
| DEF-P2-024 | P2 | Outlap metadata (is_out_lap) persists after Save Session + reload | **AWR-046** |
| DEF-P2-025 | P2 | Fuel Start/End preserved after Save Session + reload | **AWR-047** |
| DEF-P2-019 | P2 | Compound propagation stops at pit lap boundary | **AWR-048** |
| DEF-P2-020 | P2 | Live tyre shows race plan/setup compound, not mandatory tyre | **AWR-049** |
| DEF-P1-012 | P1 | BoP practice prompt provides setup changes when tuning locked | **AWR-050** |
| DEF-P2-029 | P2 | Outlap metadata dropped when write_lap stats=None | **AWR-051** |
| DEF-P2-030 | P2 | Save Session creates duplicate session when live session open | **AWR-052** |
| DEF-P2-031 | P2 | Qualifying outlap calming phrase never fires | **AWR-053** |
| DEF-P2-032 | P2 | Pit/fuel alerts suppressed in qualifying (regression guard) | **AWR-054** |
| DEF-P2-033 | P2 | AI Log auto-select fires on hidden widget | **AWR-055** |
| DEF-P2-034 | P2 | AI Log timestamps in UTC instead of local time | **AWR-056** |
| DEF-P2-035 | P2 | Garage shows no DB setups; exceptions swallowed | **AWR-057** |
| DEF-P1-013 | P1 | Strategy race_params missing race_type/tuning/bop/avail_tyres | **AWR-058 CLOSED** |
| DEF-P1-014 | P1 | Practice worker car_id=0 + new DB connection | **AWR-059 CLOSED** |
| DEF-P2-038 | P2 | Practice race_params missing bop | **AWR-060 CLOSED** |
| DEF-P2-039 | P2 | avail_tyres missing from race_params and prompts | **AWR-061 CLOSED** |
| DEF-P2-040 | P2 | Driver feedback not in Practice AI prompt | **AWR-062 CLOSED** |
| DEF-P2-041 | P2 | Prev AI recs not in Practice AI prompt | **AWR-063 PARTIAL — DEF-P3-013** |
| DEF-P2-036 | P2 | PTT coaching missing car_name/car_specs/compound | **AWR-064 CLOSED** |
| DEF-P2-037 | P2 | PTT setup_advice reads stale config setup | **AWR-065 CLOSED** |
| DEF-P3-009 | P3 | Race prompt hardcodes laps for timed race | **AWR-066 CLOSED** |
| DEF-P3-010 | P3 | build_car_setup missing race context params | **AWR-067 CLOSED** |
| DEF-P3-011 | P3 | _DATA_QUALITY_NOTE absent from ai_planner prompts | **AWR-068 CLOSED** |
| DEF-P3-012 | P3 | Strategy results not validated for tuning violations | **AWR-069 CLOSED** |
| DEF-P3-013 | P3 | AILogEntry missing car_id/track — get_recent_ai_recommendations() always returns empty | **OPEN — raised 2026-06-23** |
| DEF-P2-013 | P2 | Pit flag persists after History reload (post-Group-8 sessions) | AWR-033/**AWR-043** |
| DEF-P2-014 | P2 | Fuel start/end preserved after reload (post-Group-8 sessions) | AWR-035/**AWR-043** |
| DEF-P2-009 | P2 | Fuel Burn Auto updates on History reload | **AWR-034** |
| DEF-P2-003 | P2 | Required tyres checkbox grid | AWR-016 |
| DEF-P2-007 | P2 | AI setup restriction banner | AWR-022 |
| DEF-P2-010 | P2 | Driver feedback in Practice Review | AWR-030 |
| DEF-P2-011 | P2 | Outlap excluded from best-lap calc | AWR-008 |
| DEF-P2-012 | P2 | Tyre wear in AI prompt | AWR-015 |
| DEF-P2-015 | P2 | Top speed 11 km/h artefact guard | AWR-029 |
| DEF-P2-016 | P2 | Practice Analysis validation gate | AWR-023 |
| DEF-P2-017 | P2 | Qualifying RACE_FINISHED guard | AWR-024 |
| DEF-P3-001 | P3 | Brake balance step = 1 | AWR-009 |
| DEF-P3-002 | P3 | Live tyre compound label | AWR-010 |
| DEF-P3-003 | P3 | New lap inherits compound from previous | AWR-012 |
| DEF-P3-004 | P3 | Race type mutual exclusivity | AWR-025 |
| DEF-P3-006 | P3 | Session summary recalculates after reload | AWR-028 |
| DEF-P4-001 | P4 | PTT on Live tab | AWR-007 |
| DEF-P4-002 | P4 | AI model is claude-opus-4-8 | AWR-019 |
| DEF-P4-003 | P4 | Fuel formula percentage multiplier | AWR-020 |

### User Verified (UAT passed)

DEF-P1-001 (partial), DEF-P1-003 ✅, DEF-P1-004 ✅, DEF-P1-007 (in-session only), DEF-P1-008 ✅,
DEF-P2-001 ✅, DEF-P2-002 ✅, DEF-P2-004 ✅, DEF-P2-006 ✅, DEF-P2-008 ✅, DEF-P2-010 ✅,
DEF-P2-012 ✅, DEF-P2-QRF ✅, DEF-P3-001 ✅, DEF-P3-006 (partial), DEF-P4-001 ✅, DEF-P4-003 ✅,
DEF-P1-006 ✅

---

## Group 12 — What Was Fixed (2026-06-22)

### 12a — DEF-P1-005: BoP/tuning default bug

**Root cause confirmed:** `_run_practice_analysis()` line 3183 used `_psc.get("tuning", True)`. When the "tuning" key was absent from `config["strategy"]` (old config, or silent exception in `_on_event_set_active()` before write), this default returned `True`, making `tuning_locked = not bool(True) = False`. Practice analysis ran with full setup regardless of event configuration.

**Fix 1 — `_run_practice_analysis()` line 3183:** `_psc.get("tuning", True)` → `_psc.get("tuning", False)`. Absent key now produces `tuning_locked = True` (locked = safe default).

**Fix 2 — `_on_event_set_active()` except block:** `except Exception: pass` → `except Exception: import traceback; traceback.print_exc()`. Previously silently swallowed all failures including the TypeError that prevented `strat["tuning"]` from being written.

**Fix 3 — Debug context print:** When `GT7_AI_DEBUG=1` is set, prints `bop`, `tuning` (raw value), `tuning_locked`, `allowed_tuning`, `race_type`, `fuel_mult`, `tyre_wear` to stdout immediately after `race_params` is built.

### 12b — DEF-P2-022/013/014: Investigation (no code change)

**Conclusion:** Root cause hypothesis (different data sources) was **incorrect**. Both `_on_history_load_session()` and `_import_bank_session()` call the same `get_session_laps()` which correctly SELECTs `fuel_start`, `fuel_end`, `is_pit_lap`, `is_out_lap`. Both correctly pass all fields to `_add_bank_lap_row()`. `write_lap()` in `main.py` correctly writes them from `LapRecord`. The History detail panel only shows `fuel_used` (not `fuel_start`/`fuel_end`) — the user's comparison was between different columns. Zero values in AWR-040 were pre-Group-8 session data. DEF-P2-022 closed. DEF-P2-013/014 awaiting retest with a post-Group-8 session.

### 12c — DEF-P2-021: Three remaining AI Log display issues

**Fix 1 — Timestamp:** `entry.timestamp[11:19]` (showed HH:MM:SS only) → `entry.timestamp[:19].replace("T", " ")` (shows YYYY-MM-DD HH:MM:SS).

**Fix 2 — Status text:** `"✓"/"✗"` → `"✓ OK"/"✗ FAIL"/"⊘ DRY-RUN"`. Dry-run detected when `duration_ms == 0 and entry.error_msg and "AI_DEBUG" in entry.error_msg`.

**Fix 3 — Deferred auto-select:** `_on_ai_log_entry()` now sets `self._ai_log_pending_select = True`. New `_flush_ai_log_pending_select()` helper calls `setCurrentRow(count - 1)` and clears the flag. `_on_tab_changed()` now handles index 11 (AI Log tab) by calling `_flush_ai_log_pending_select()` — ensures selection is applied when the tab becomes visible, not when the hidden widget received `setCurrentRow()`.

### Tests Added

- **`tests/test_group12a_bop_tuning_propagation.py`** — 12 tests in 4 classes
- **`tests/test_group12b_history_practice_mapping.py`** — 20 tests in 5 classes
- **`tests/test_group12c_ai_log_display.py`** — 12 tests in 3 classes

---

## Group 11 — What Was Fixed (2026-06-22)

### DEF-P1-011 — Fuel Burn Auto stale label after event switch

**Problem:** `_on_event_set_active()` calls `_sync_setup_builder_from_event()` which only updates `_lbl_fuel_burn_display` when `tracker.avg_fuel_per_lap > 0`. With no live telemetry the label was left showing `config["strategy"]["fuel_burn_per_lap"]` from a previous session (e.g. `"3.00 L/lap (last session)"`). The number coincidentally matched the event fuel multiplier (both 3×), causing confusion.

**Fix — `ui/dashboard.py` `_on_event_set_active()`:** Added reset block after `_sync_setup_builder_from_event()`. When `tracker.avg_fuel_per_lap <= 0 AND _loaded_session_avg_fuel <= 0`, resets `_lbl_fuel_burn_display` to `"— (complete practice laps to calibrate)"`. Live telemetry and loaded-session paths unchanged.

### DEF-P2-021 — AI Log list didn't auto-select new live entries

**Problem:** `_add_ai_log_list_item()` called `scrollToBottom()` after appending the new item. `bridge.ai_log_entry` uses QueuedConnection (cross-thread), so the slot fires after the timer tick — at which point the AI Log tab may not be the active tab and `scrollToBottom()` has no effect. Navigating to the tab showed the DB-loaded history at the top; the new entry at the bottom was not selected or visible. The user clicked an old DB-loaded entry and saw the Prompt sub-tab from that entry — explaining "Prompt tab populated. Prompt text visible" with "No visible AI log entry."

**Fix — `ui/dashboard.py`:**
- `_add_ai_log_list_item()` gains `auto_select: bool = False`. When `True`, calls `setCurrentRow(count - 1)` after `addItem()`.
- `_on_ai_log_entry()` (live signal handler) passes `auto_select=True`.
- `_on_ai_log_entry_dict()` (DB startup load) uses default `auto_select=False`.

### Tests Added
**`tests/test_group11_ui_display_fixes.py`** — 8 new tests in 2 classes.

---

## Group 9 — What Was Fixed (2026-06-22)

### Problem
`call_api()` in `strategy/_ai_client.py` raised `RuntimeError` in the `if _AI_DEBUG:` branch before reaching the `try/except` block that contains both `_fire_log_hook()` calls. The signal chain (`_ai_log_callback` → `bridge.ai_log_entry.emit()` → `dashboard._on_ai_log_entry()`) was fully wired and correct — but the entry was never created because the hook was never called.

When the user tested AI features with `GT7_AI_DEBUG=1` (as the test plan required), every intercepted AI call printed the prompt to stdout and then raised RuntimeError — leaving the AI Log tab permanently empty.

### Fix (1 file changed)

**`strategy/_ai_client.py` — `call_api()` debug branch:**
Added `_fire_log_hook(AILogEntry(...))` immediately before the `raise RuntimeError`. The dry-run entry captures:
- `success=False`
- `response="[AI_DEBUG dry-run — no API call made]"`
- `error_msg="AI_DEBUG mode active — prompt intercepted, no API call made"`
- `feature`, `model`, `prompt` from the actual call arguments
- `duration_ms=0`, `prompt_tokens=0`, `response_tokens=0`, `estimated_cost=0.0`

The entry is written to DB via the existing `_ai_log_callback` in `main.py` and emitted on the bridge signal. No other files required changes.

### Tests Added
**`tests/test_group9_ai_log.py`** — 18 new tests in 3 classes:
- `TestCallApiDebugFiresLogHook` (9 tests): Source-scan of `call_api()` confirming `_fire_log_hook` is called inside the `_AI_DEBUG` block, appears before `raise RuntimeError`, passes `success=False`, non-empty `error_msg`, `feature`, `model`, `prompt`. Also confirms success and failure paths in the real API call block both fire the hook.
- `TestDashboardAiLogWiring` (4 tests): Source-scan confirming `_connect_signals()` connects `ai_log_entry` signal, `_build_ai_log_tab()` loads DB history on startup, `_on_ai_log_entry` appends to list and calls `_add_ai_log_list_item`.
- `TestAiInteractionsDbRoundTrip` (5 tests): DB round-trip for `log_ai_interaction()` and `get_ai_interactions()` — confirms all fields survive the write/read cycle, failed entries (success=0) are stored correctly, results return newest-first, limit is respected.

---

## Group 8 — What Was Fixed (2026-06-22)

### Problem
`main.py` EventDispatcher called `write_lap()` without passing `is_pit_lap`, `is_out_lap`, `delta_ms`, or `session_type` from the `LapRecord`. These four parameters defaulted to 0/False/"" in the `write_lap()` signature. So the DB always stored `is_pit_lap = 0` and `is_out_lap = 0` regardless of what the telemetry actually recorded. The live Practice Review display reads from the `LapRecord` object in memory (correct), but after a History reload the display reads from the DB (always 0 → pit flag missing, outlaps included in fuel average).

Separately, after `_on_history_load_session()` computed `_loaded_session_avg_fuel`, the Strategy Builder `_lbl_fuel_burn_display` label was never refreshed — it still showed the value from app startup.

### Fix (2 files changed)

**`main.py` — `EventDispatcher._dispatch()` lines ~224–236:**
Added four missing fields to the `write_lap()` call:
- `is_pit_lap=bool(getattr(record, "is_pit_lap", False))`
- `is_out_lap=bool(getattr(record, "is_out_lap", False))`
- `delta_ms=int(getattr(record, "delta_ms", 0))`
- `session_type=(record.session_type.value if hasattr(record.session_type, "value") else str(...))`

**`ui/dashboard.py` — `_on_history_load_session()` and `_import_bank_session()`:**
Added fuel burn display refresh immediately after `_loaded_session_avg_fuel` is computed:
```python
if hasattr(self, "_lbl_fuel_burn_display") and self._loaded_session_avg_fuel > 0:
    self._lbl_fuel_burn_display.setText(
        f"{self._loaded_session_avg_fuel:.2f} L/lap (loaded session)")
```

No DB schema changes. No `session_db.py` changes.

### Tests Added
**`tests/test_group8_session_reload.py`** — 37 new tests in 4 classes:
- `TestGetSessionLapsColumns` (13 tests): DB round-trip for `is_pit_lap`, `is_out_lap`, `fuel_start`, `fuel_end`. Confirms pit flag survives write_lap → get_session_laps. Confirms fuel average excludes pit laps and outlaps.
- `TestMainWriteLapPassesPitFlag` (6 tests): Source-scan confirming `main.py` write_lap call includes `is_pit_lap`, `is_out_lap`, `fuel_start`, `fuel_end`, `delta_ms`, `session_type`.
- `TestHistoryLoadSessionMapping` (7 tests): Source-scan confirming `_on_history_load_session` passes pit/fuel fields and updates `_lbl_fuel_burn_display`.
- `TestImportBankSessionMapping` (5 tests): Source-scan confirming `_import_bank_session` does the same.
- `TestAddBankLapRowRendering` (6 tests): Source-scan confirming `_add_bank_lap_row` renders pit flag, fuel columns, and background colours correctly.

---

## Group 7 — What Was Fixed (2026-06-22)

### Problem
`_on_event_selected()` in `ui/dashboard.py` silently failed when populating form fields from the DB. The `tyre_wear`, `fuel_mult`, and `refuel_rate_lps` columns are `REAL` in SQLite, which SQLite's Python driver returns as Python `float`. The form widgets `_evt_tyre_wear`, `_evt_fuel_mult`, `_evt_refuel_rate` are `QSpinBox` (integer-only). PyQt6's `QSpinBox.setValue(float)` raises `TypeError`. The method's `except Exception: pass` swallowed this, leaving the spinboxes at 1 (the default minimum) and skipping all subsequent field population — fuel_mult, avail_tyres, req_tyres, tuning categories, and notes never loaded.

### Fix (1 file changed)
**`ui/dashboard.py` — `_on_event_selected()` lines ~7321–7331, ~7354–7359:**

1. `int(round(...))` cast applied to `tyre_wear`, `fuel_mult`, and `refuel_rate_lps` before `setValue()`.
2. `except Exception: pass` replaced with `except Exception: import traceback; traceback.print_exc()`.
3. Tuning permissions group visibility changed from `_bop_on and _tun_on` to `bool(_tun_on)`.

No DB schema changes. No `session_db.py` changes. No other files changed.

### Tests Added
**`tests/test_group7_event_persistence.py`** — 28 new tests in 4 classes:
- `TestEventDBRoundTrip` (15 tests): DB round-trip for all Group 7 fields via `upsert_event` + `get_all_events`. Documents that `tyre_wear` and `fuel_mult` return as Python `float` from DB.
- `TestEventSelectedFixApplied` (7 tests): Source-scan confirming `int(round(...))` is in `_on_event_selected`, exception handler prints traceback, tuning perms uses `bool(_tun_on)`.
- `TestEventSetActiveStratKeys` (7 tests): Source-scan confirming `_on_event_set_active` writes all required strategy keys.
- `TestPracticeAnalysisBoPContext` (2 tests): Source-scan confirming `_run_practice_analysis` passes `tuning_locked` and `allowed_tuning` to `race_params` (DEF-P1-005 auto-resolution path).

---

## Pending Implementation (Groups 8–11)

### Group 8 — Session Reload Mapping — COMPLETE (2026-06-22)
**Target:** DEF-P2-013 (pit flag lost), DEF-P2-014 (fuel start/end lost), DEF-P2-009 (stale fuel burn)
**Status:** Fixed — AWR-033, AWR-034, AWR-035 awaiting runtime verification

### Group 9 — AI Debug / Log Visibility — COMPLETE (2026-06-22)
**Target:** DEF-P1-010
**Status:** Fixed — AWR-036 awaiting runtime verification
**Fix:** `_fire_log_hook()` now called in `_AI_DEBUG` branch of `call_api()` before `raise RuntimeError`. Dry-run entries appear in AI Log tab with `success=False` and full prompt captured.

### Group 10 — AI Prompt BoP Context (P1)
**Target:** DEF-P1-005 runtime verification
**Dependency:** Group 7 runtime retest (AWR-031/AWR-032) must pass first.
**Note:** Code is already correct (`_run_practice_analysis` passes `tuning_locked` / `allowed_tuning`). May auto-resolve. Only needs code changes if AWR-032 shows the prompt is still missing the restriction block after Group 7 fix.

### Group 11 — Visual / UX Fixes (P2–P3)
**Target:** DEF-P2-018, DEF-P2-019, DEF-P2-020, DEF-P3-007, DEF-P3-008
**Files:** `ui/dashboard.py`
**Do not start until Groups 8–10 complete.**

---

## Key Files

| File | Role |
|------|------|
| `ui/dashboard.py` | Main UI — ~7700 lines — all Event Planner, Practice Review, Setup Builder, Live tab logic |
| `data/session_db.py` | SQLite persistence — events, laps, sessions, AI interactions, setups |
| `strategy/ai_planner.py` | Practice Analysis AI — `RaceParams`, `analyse_practice_session`, prompt builder |
| `strategy/driving_advisor.py` | PTT coaching AI — `DrivingAdvisor`, prompt builders |
| `strategy/_ai_client.py` | Anthropic API wrapper — `call_api`, `ai_interactions` table write |
| `telemetry/state.py` | `RaceStateTracker` — lap detection, session type, oversteer/kerb detection |
| `telemetry/recorder.py` | `LapTelemetryRecorder` — per-frame stats, `LapStats`, `TelemetryFrame` |
| `voice/announcer.py` | TTS engine — `VoiceAnnouncer`, session mode guard on pit/fuel alerts |
| `voice/query_listener.py` | PTT + speech-to-text — `QueryListener`, intent keywords |
| `data/tyres.py` | Tyre compound catalogue — `ALL_COMPOUNDS`, `normalise_code`, `get_by_code` |

---

## Dev Notes

- **No Qt in tests.** All tests use source-code scanning (`_method_body()` pattern) or in-memory `SessionDB(":memory:")`. Do not add `QApplication` to tests.
- **DB is authoritative for events.** Config.json still syncs as a fallback but DB is the primary store.
- **`isolation_level=None`** on the SQLite connection means autocommit — no explicit `commit()` needed.
- **Python 3.14 / PyQt6.** QSpinBox.setValue() enforces `int` strictly in PyQt6. DB `REAL` columns return Python `float`. Always cast before passing to QSpinBox.
- **`GT7_AI_DEBUG=1`** must be set as an env var before the Python process starts. In PowerShell: `$env:GT7_AI_DEBUG=1; python main.py`. In cmd: `set GT7_AI_DEBUG=1 && python main.py`.

## Engineering Brain Program 2 — Phases 48-50 (Event Preparation Cycle & Race Weekend)

- **Preparation cycle = Layer B** (`strategy/event_preparation_cycle.py`), groups every Practice session for one upcoming NGR round into one cumulative programme; references but never redefines the immutable event environment (Layer A) or execution/outcome authorities (C/D). See `NGR_EVENT_PREPARATION_ARCHITECTURE.md`.
- **DB v28** adds `event_preparation_cycles` / `event_preparation_activities` / `event_preparation_activity_sessions`. Sole writers: `upsert_preparation_cycle` / `upsert_preparation_activity` / `bind_session_to_activity`. `build_event_preparation_report` is read-only, constant query count (no N+1). Sessions are never auto-bound; viewing writes nothing.
- **Setup lock / strategy finalisation are explicit-confirm only**, never an Apply/auto bypass; `ActiveSetupAuthority.mark_applied` remains the sole setup-mutation route.
- **Future NGR Hub** (`strategy/ngr_event_manifest.py`) is a contract only — no network/API/auth; a Hub revision never rewrites completed history; offline manual event creation is never removed.
- **Each phase test suite pins the current DB schema version literally** and is bumped every slice (do not assume only a couple of files need updating on a DB_VERSION bump — sweep the whole `tests/` suite).

## Engineering Brain Program 2 — Phases 51-53 (Event Command Centre / Live Orchestration / Certification)

- **Home is now the Event Command Centre.** When an active cycle exists the app opens into the current NGR event (first widget of the Home tab), built off-thread via `SessionDB.build_event_command_centre_view` (read-only, constant query count). `strategy/active_cycle_resolution.py` resolves the active cycle explicitly and NEVER silently picks the newest row; several active cycles require explicit selection. A selected cycle is operational nav state (`config["active_cycle_id"]`), never an engineering write.
- **Live activity orchestration** (`strategy/live_activity.py`, `live_activity_modes.py`, `activity_binding.py`): start-readiness gates, low-density Practice/Qualifying/Race views (Race issues no pit/fuel commands), explicit binding (reuses `session_binding.rank_candidate_sessions`, never auto-binds), and a cumulative-update gate (invalid/mismatched/abandoned update nothing). An activity reaches COMPLETED only through explicit confirmations.
- **Resilience** (`strategy/programme_resume.py`, `event_revision_impact.py`, `setup_lock_reopen.py`): a restart never restores an interrupted activity as complete; telemetry dropout suppresses advisories and preserves (never strengthens) evidence; an event revision assesses impact without rewriting history; a locked setup resists noise but reopens on any of 8 explicit reasons (Audit C remediation).
- **Operational certification** (`strategy/operational_certification.py`): live/operational certification requires live-GT7 evidence and cannot come from automated/offscreen tests alone.
- **No new DB migration** in this slice (DB_VERSION stays 28). Home viewing/navigation/refresh writes nothing.
